<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 Sungwoo Kang -->

# Graph Auto Reader 2.0 readiness

## Current Goal 22 execution snapshot, 2026-09-09

The [marker reservation preflight](GOAL-22-MARKER-ANNULUS-RESERVATION-PREFLIGHT.json)
uses only the authenticated training-proposal cache and completes in 2.12 seconds.
It reserves 7,184 nearby negative examples across fixed distance bands, replacing
6,207 selected negatives while preserving all positives, protected examples and
the total training size. Component strata remain unchanged; 96 family replacements
cross strata, with every count change recorded. All 2,504 truths remain accounted
for, including 22 without runtime-domain support. The focused suite passes
23 checks. This prepares an isolated sampling experiment and establishes no
accuracy improvement or model approval.

The final integrated product remains **2.0.0**, with conditional authorization
for production activation and the paired GitHub installer/portable release
after all mandatory gates pass. No intermediate public release is authorized.
REV-004 and REV-006 are resolved; no maintainer decision is currently waiting.

The [in-memory execution boundary](GOAL-22-REAL-WORKFLOW-EXECUTION-BOUNDARY-OUTCOME.json)
passes 56 Python accounting/process checks and 32 final C# admission, runner,
handshake and argument checks. Earlier workflow checks establish identical
file-backed and in-memory CSV bytes, all 30 evaluator scenarios, and no saved
acceptance images or inference-cache entries. All 245 App checks pass.
The actual compiled worker also rejects malformed requests through the Python
parent without a payload read, result file or consumed budget. Final compilation
has zero warnings or errors. Corpus content is pinned on its first observed
accounted read and compared before later inference; historical content before
that observation remains unproven. Real-sealed admission deliberately remains
unavailable until authenticated marker-sealed and OCR full-metric source-result
adapters exist. No private accuracy or model approval follows from these checks.

The [tiled OCR input bridge](GOAL-22-TILED-OCR-INPUT-OUTCOME.json) verifies
all 23 sources, 37 actual panels and 892 text truths in 113.18 seconds.
The OCR suite passes 238 checks with 16 existing opt-in skips, and both input
splits pass nine focused checks. This original-Gray8 candidate composition
remains unapproved and unselected. Its tensors and targets are distinct from
the original BGR DB detector and cannot substitute for pretrained-head inputs.

The [original DB baseline](GOAL-22-ORIGINAL-DB-BASELINE-OUTCOME.json) scores
the saved unmasked detector outputs against all 892 source text truths.
Train has 131 correct regions, 494 false regions and 578 misses; dev has
71 correct regions, 137 false regions and 112 misses. Dev precision is 34.13%
and recall 38.80%. This is a different input route from the masked-consensus
diagnostic above. No new inference or private data was used for this score.

The [production head input bridge](GOAL-22-PRODUCTION-HEAD-INPUT-OUTCOME.json)
binds a retained executable snapshot and all 37 original BGR tensors. Independent
Python/OpenCV reconstruction matches all 147,849,216 tensor bytes exactly.
Actual manifest alignment is 128 pixels, so a nominal 960-pixel resize can
produce a 1024-pixel side. All 709 train and 183 dev truths remain represented;
16 collapsed train shrink targets are explicitly loss-masked. The bridge passes
13 focused checks. The [application candidate path](GOAL-22-ORIGINAL-DB-CANDIDATE-OUTCOME.json)
passes all 250 App tests, keeps original pixels and remains explicitly unapproved.

The [reviewed ONNX head route](GOAL-22-PRETRAINED-DETECTOR-FINETUNE-FEASIBILITY.json)
reuses the licensed model with 23,257 head parameters and nine patchable tensors.
The all-panel extracted-head parity check now passes: maximum difference
0.00000694394 is below the unchanged 0.00001 tolerance across all 37 inputs.
The two earlier failures remain recorded. Layer diagnosis identified CPU
convolution rounding; MKLDNN is disabled and only the first convolution
accumulates in float64 before returning to float32. Parameters, downstream
operations and frozen statistics are unchanged. A no-op patch remains
byte-identical to the original model. The head and training engine pass
19 focused checks. [V39 P1](../ml/ocr/pretrained_db_head_v39/P1_RESULT.json)
completed 1,680 train-only optimizer steps in
599.46 seconds on the 28 captured training panels. Epoch 60 was selected by
full-training loss, which fell from 0.59487 after epoch 1 to 0.17590. The four
running-statistic tensors remained byte-identical. Trained export parity passes
all 37 inputs at maximum difference 0.00000646710. The actual application
completes all 37 panels in 12.84 seconds, with no failed regions. Full-denominator
scoring takes 102.80 seconds: train has 210 correct regions, 54 false regions
and 499 misses; dev has 39 correct regions, three false regions and 144 misses.
Dev precision 92.86% and recall 21.31% both fail the unchanged 95% bars.
Compared with the original detector, false detections fall but recall worsens.
The candidate is closed as failed dev with no sealed budget consumed.
Recognition text and roles remain ungraded. The scorer passes 12 focused checks
and independent review. An inherited warning incorrectly mentions a structural
mask; the retained report's input hashes and composition identify original BGR
without masking. A separate correction records the metadata-only repair, whose
targeted test and independent review pass. The original run remains immutable.
The [actual application evaluator](GOAL-22-ORIGINAL-DB-EVALUATOR-OUTCOME.json)
passes 10 model-free checks and binds the exact loaded tool/App/OCR/Inference
assemblies before model loading. This failed candidate remains unapproved.

The [perfect-target round trip](GOAL-22-DB-TARGET-ROUNDTRIP-DIAGNOSIS.json)
identifies a training-target defect independently of model predictions. The
unchanged actual DB postprocessor recovers only 103/709 train and 1/183 dev
truths from ideal current shrink masks. It rejects 557 contours for short side
length and 90 for insufficient points. All 37 panels run in 2.35 seconds;
the target export takes 106.82 seconds, with no model inference or training.
The model-free runner passes seven checks and a four-fixture C# round-trip
regression confirms thin-target rejection and insufficient reconstructed area.
The [deterministic inverse-unclip target](GOAL-22-INVERSE-DB-TARGET-OUTCOME.json)
with inclusive raster endpoints now recovers 677/709 train and 181/183 dev
truths through the same decoder. It retains all 892 targets, with 25 contacts,
35 minimum-extent clamps and zero boundary translations. Nine focused tests
and independent review pass. Six training contours still fail box confidence;
contacts and all unmatched truths remain visible. This supports the V40
target-construction training experiment; it is not trained-model accuracy.

The [marker target preflight](GOAL-22-MARKER-TARGET-PREFLIGHT.json) retains
all 2,504 training truths and 44,891 selected rows, including 22 truths without
an eligible proposal. It finds no opposing labels on byte-identical patches.
The [exact suppressor-anchor replay](GOAL-22-MARKER-SUPPRESSOR-ANCHOR-DIAGNOSIS.json)
then reproduces the V25 counts in 134.18 seconds and caches every output.
All 25 component and two family false-positive suppression events originate
at training-negative anchors. The proposed unique-nearest-positive target
does not directly address those errors and is not selected. Negative sampling
has now been [measured against its full eligible inventory](GOAL-22-MARKER-NEGATIVE-COVERAGE.json).
At distances greater than three and at most 12 pixels from a marker, selection
is 14.38% for components and 5.61% for families. The family eight-to-12-pixel
band containing both false suppressors has 5.49% selection. This is sparse
coverage, not an exclusion or proof of causation. The model-free check takes
161.83 seconds and caches all proposal metadata; 18 focused tests pass.
No V26 training is authorized yet.

The [V25 marker outcome](../ml/markers/center/plot_domain_v25/P1_RESULT.json)
fails synthetic dev after 12,636 optimizer steps and 32.69 minutes.
At the unchanged 0.25 operating threshold, the component benchmark has
1,863 correct detections, 152 false detections and 141 misses, giving
92.46% precision and 92.96% recall. Full-source-family dev has 197 correct
detections, 59 false detections and nine misses: 76.95% precision and 95.63%
recall. Both benchmark gates fail. ONNX CPU parity passes at 0.00000191 across
candidate counts 1, 8 and 37. No sealed budget was consumed. The actual
plot-domain candidate adapter remains unapproved. V24 and V25 family results
use different renderings and proposal domains, so their raw metric differences
are not an isolated model comparison. The [fixed-threshold diagnosis](GOAL-22-V25-DEV-DIAGNOSIS.json)
reproduces both outcomes exactly in 219.16 seconds. Of 59 family false points,
49 lie within three pixels of legend geometry. Seven family misses are below
the confidence threshold and two are suppressed by NMS; all retain a nearby
proposal. Of 152 component false points, 116 are within 16 pixels of a true
marker, while 111 of 141 misses are below threshold. These proximity measures
are associations, not proof of cause. The resolved-legend exclusion repair
now keeps a recognized local legend glyph in the marker audit while excluding
it from final point rows. Its workflow regression produces three auditable
markers, one rejected legend glyph and two CSV rows. All 246 App tests and
37 Legends tests pass. This does not establish how many V25 false positives
the repair removes. Exact fixed-output NMS replay takes 206.75 seconds and
attributes 27 component misses and two family misses to suppression. False
points are the actual suppressor for 25 component misses and both family
misses. The family suppressors lie 6.96 and 7.38 pixels from truth, while the
suppressed candidates lie 0.93 and 1.80 pixels away. No involved raw radius
activates decoder clipping. Confidence/localization supervision remains under
investigation; no new marker candidate is selected.

The [real workflow ledger](GOAL-22-REAL-WORKFLOW-LEDGER-OUTCOME.json)
passes 27 accounting and shared-policy checks in 2.35 seconds. A live process
lease prevents concurrent or restarted runs from reusing a consumed attempt.
Pre-read failures remain void; post-read failures remain consumed. Immediate
disclosure retirement survives later failures, while aggregate-only results
preserve reuse under the shared policy. No actual ledger or private input was
used. The later execution-boundary outcome above extends this ledger with
content pinning and resolves the transport review findings. Authentic stage
source-result adapters and passing model evidence remain required.

The [real inventory and read boundary](GOAL-22-REAL-INVENTORY-READ-BOUNDARY-OUTCOME.json)
now verifies the frozen 120/51 study split using file metadata only. Its ten
checks confirm that excluded held-out project contents are never opened.
The evaluator reader adds a callback immediately before its first payload read,
after allocations and cancellation checks; all 27 reader checks pass.
Root verification runs both suites in 0.39 seconds. The final compilation has
zero warnings or errors. Durable once-per-candidate accounting is implemented
in the ledger above; real-sealed prerequisite support remains unfinished.

The [visible-content OCR diagnostic](GOAL-22-VISIBLE-CONTENT-OCR-SCORE-OUTCOME.json)
scores unchanged saved outputs against all 892 synthetic text references.
Dev finds 49 of 183 regions, with 86 false detections and 134 misses:
36.30% precision and 26.78% recall. Both 95% bars fail. Train retains all
709 references and also fails. Scoring takes 50.27 seconds without new model
inference. The stochastic source and all nine sources with recorded defects
remain included. Recognition, roles, calibration and CSV accuracy are not
graded. Production-compatible tiled inputs and a separately justified detector
candidate remain necessary; no private or sealed input was used.

The [synthetic source repair](GOAL-22-VISIBLE-CONTENT-GENERATOR-OUTCOME.json)
preserves physical ink under declared erosion and corrects halftone sampling.
All 23 source PNGs match a fresh, fully pinned regeneration byte for byte in
45.48 seconds. The unchanged automatic import, panel, axis and OCR pipeline
completes all 37 detected panels in 45.07 seconds. All 706 marker and 892 text
references remain accounted for, including nine sources with recorded defects.
One stochastic source has unavailable element attribution, not zero errors.
Thicker strokes can still fill marker interiors and text counters; no universal
readability or accuracy claim follows. The optional full audit takes 296.32
seconds; its earlier incomplete attempt is retained. All 31 generator checks
have positive evidence after correcting four schema-invalid test fixtures.
Marker proposal coverage and model acceptance remain separate unfinished work.
No private/sealed input, optimizer or packaged application build was used.

The [Engauge workflow harness](GOAL-22-ENGAUGE-WORKFLOW-OUTCOME.json)
now preserves every reference project and point while running a shared image
once. Only image bytes and their identity reach automatic reading; calibration
anchors and digitized points stay in the evaluator. All 49 input, grouping and
executor checks pass. The actual workflow adapter also passes with fictitious
stage adapters, three exported artifacts and unchanged source bytes. Complete
PNG validation rejects corrupted and incomplete image data before inference.
Final verification takes 4.41 seconds including compilation, with no warnings
or errors. The earlier compiler and corrupt-image fixture failures are retained.
This is synthetic harness verification. Private/sealed command admission and
actual model acceptance remain unfinished; phase/shared-baseline truth is not
inferred from Engauge files. No private or sealed input was read.

The [CI shutdown test repair](GOAL-22-CI-DISPOSAL-REPAIR.json) verifies that
disposal and caller cancellation finish while an operation remains blocked.
The local inference suite passes 84 checks with one existing opt-in skip.
The pushed repair also passes [CI run 34377310192](https://github.com/SKang393/Graph-auto-reader/actions/runs/34377310192).
Runtime shutdown behavior and the product build number are unchanged.

The [DB postprocess diagnostic](GOAL-22-DB-POSTPROCESS-OUTCOME.json) reproduces
all 52 saved detector outputs exactly while recording immutable threshold masks
and contour rejection stages. All 230 OCR checks pass, with 16 existing opt-in
skips; the prior 228-check App run already included this observer. The replay
takes 11.50 seconds and the final model-free diagnosis takes 20.14 seconds over all
329 train/dev text truths. Unmasked 960 finds 237 truths with 109 false regions;
1920 finds 190 with 127 false regions. The combined comparison loses 64 matches
and gains 17, distinct from the preceding dev-only loss of 51 and gain of seven.
Missing detector support, poor accepted geometry and localized rejection all
contribute. Unlocated short contours remain unresolved. This evidence supports
diagnosis, not a threshold choice, accuracy acceptance or production activation.
The first saved-input export failed before inference; its repaired historical
mask reconstruction matches both original gray and BGR hashes. Independent
review also tightened the exact-request-count check; all 18 scientific and
provenance sections remain identical in the new model-free run. No packaged
application build is produced.

The [axis contact safeguard](GOAL-22-AXIS-CONTACT-OUTCOME.json) rejects
unsupported intersections of disconnected finite lines, including internal
gaps in fitted families. All 41 axis and 228 App checks pass. An actual native
replay returns geometry on all 32 saved panels in 1.62 seconds. The complete
comparison preserves all 500 train and 206 dev truth points: train polygon
containment decreases from 363 to 327 and proposal support from 384 to 347;
dev remains 206 of 206. This is a structural correctness repair with a measured
coverage regression on the unchanged inputs. It does not establish model or
workflow acceptance. Generator diagnosis separately finds erased axis strokes
and combined plots; no panelization or generator change is included in this
checkpoint. Production remains inactive and no packaged build is produced.

The [larger-input OCR experiment](GOAL-22-HIGH-RESOLUTION-OUTCOME.json)
changes only the detector maximum side from 960 to 1920. Dev finds 100 of 183
text regions, with 45 false detections and 83 misses: 68.97% precision and
54.64% recall. Both 95% bars fail, and recall is worse than the preceding
advisory candidate. All 329 truths and 317 current detector contours remain
accounted for. Actual train/dev runs take 10.72/15.33 seconds; the final
model-free score takes 18.61 seconds. All five previous modes preserve exact
scientific output across 65 panels and 520 referenced files per side. App
passes 228 checks and the scorer passes 12, including partial-panel and
zero-panel failures. A scorer failure-path repair reproduces the initial
all-success result from the saved model outputs. Independent runtime, scorer
and parity reviews are clear. The option remains unapproved; no packaged
application build or production activation occurred.

The [advisory structure experiment](GOAL-22-ADVISORY-STRUCTURE-OUTCOME.json)
keeps every accepted initial detector contour visible instead of requiring a
second structural match. Dev finds 144 of 183 text regions, with 64 false
detections and 39 misses: 69.23% precision and 78.69% recall. Both 95% bars
still fail. All 329 train/dev truths and all 346 detector identities are
accounted for, with no recognition execution failures. Recognition and role
accuracy remain unscored. Actual train/dev runs take 6.81/9.55 seconds; the
completed model-free score takes 20.11 seconds. All four previous required
modes preserve scientific output across 52 panels and 416 referenced files
per side. App passes 221 checks, OCR passes 225 with 16 existing opt-in skips,
and 17 scorer checks pass. Verifier ancestry, ordering and writer defects were
repaired using the saved model outputs; their failed attempts remain recorded.
No production stage is activated and no packaged application build is produced.

The [initial-contour OCR experiment](GOAL-22-INITIAL-CONTOUR-OUTCOME.json)
keeps the original-input detector and consensus selection fixed, then uses the
tighter initial detector polygon for each selected recognition crop. Dev finds
82 of 183 text regions, with 33 false detections and 101 misses: 71.30%
precision and 44.81% recall. Both 95% bars still fail. All 329 train/dev truths
and all 205 selected region identities are retained. Recognition changes are
descriptive and do not establish recognition or role accuracy. Actual train/dev
runs take 5.16/10.06 seconds. Independent review required stronger evaluator,
truth-source, font and package authentication; the repaired model-free rescore
takes 20.40 seconds and reproduces every metric from the preserved diagnostic.
Default and previous original-input outputs remain identical across 26 panels
and 208 referenced files per side. App tests pass 219 checks, OCR passes 221
with 16 existing opt-in skips, and 27 scorer safeguards pass. Production remains
inactive and no packaged application build was produced.

The [original-pixel OCR experiment](GOAL-22-ORIGINAL-MODEL-INPUT-OUTCOME.json)
changes only the learned detector's input while keeping structural components
on the axis-masked image. All 146 train and 183 dev text truths are scored.
Dev final-region precision/recall change from 36.30%/26.78% to 43.48%/27.32%:
50 true detections, 65 false detections and 133 misses. Both 95% bars fail.
Across train and dev, raw expanded detections find 106 of 329 truths; the final
consensus retains 79. Recognition and roles remain unscored. The actual train
and dev runs take 5.05 and 7.23 seconds, and scoring takes 19.16 seconds.
The [preceding diagnosis](GOAL-22-DB-INITIAL-ERROR-DIAGNOSIS.json) found that
nearby structure masking changes detector response even when tick glyphs
themselves are not masked. This does not establish a production improvement.
Default scientific output remains identical across all 13 panels and 104
referenced image/mask files per side. Independent review verifies both complete
35-file executable snapshots and all allowed provenance/timing differences.
That checkpoint's App suite passed 217 checks, OCR passed 213 with 16 existing opt-in
skips, and all five scorer safeguards pass. No production path is activated.

The [frozen whole-workflow diagnostic](GOAL-22-FROZEN-WORKFLOW-OUTCOME.json)
now runs the actual axis, OCR, marker and classifier composition over all seven
fixed synthetic sources and 13 prepared panels. Every source fails automatic
calibration because numeric tick evidence is missing or conflicting. No manual
anchors or points were supplied, no Review corrections were made, and no CSV
was written. The calibration failures remain visible in the complete report.
The actual run takes 34.29 seconds. Its executable is bound to 26 managed and
seven native files, with 212 source files preserved in a separate snapshot.
The [offline CSV evaluation](GOAL-22-WHOLE-WORKFLOW-CSV-OUTCOME.json) now binds
that exact saved report to all 306 synthetic truth points, 17 series and 16
export relations. All seven calibration failures remain in the denominator:
306 source points and 324 required export rows are missing, with zero coverage.
Precision is undefined because no CSV was produced. Shared-baseline copies
account for the additional rows without duplicating point objects. Twenty-five
CSV scenarios, ten source-binding scenarios and eight truth-builder tests pass.
The model-free score takes 95 milliseconds after a 22.71-second independent
truth regeneration. It does not establish model or real-data acceptance.
A startup file-lock failure occurred before model initialization; the writer
now closes before checksum verification. Independent review also found and
repaired a source-subset gap: both declared manifests and every ordered source
identity must match the frozen protocol before runtime initialization.
The repaired framework-dependent tool builds without warnings or errors and
passes its model-free filesystem, source-set, binding and workflow self-tests.
These compiler and diagnostic outputs are not packaged application builds.
The stratified marker sampler outcome below remains limited to synthetic
train/dev. Real and sealed acceptance remain unopened in this execution segment.

The [runtime-input repair](GOAL-22-RUNTIME-INPUT-REPAIR.json) fixes raster/PDF
axis-length units, fitted-axis bounds, synthetic label overlap, official uint8
OCR resizing, and empty-OCR execution provenance. Its independently checked
input binding maps all 306 synthetic markers exactly once across 13 panels.
Family training inputs now use actual upstream runtime planes; annotations
supply labels only. No private or sealed input was read.

V24 retry11 completed 12,672 optimizer steps over 44,946 examples in 31.38
minutes, plus a 131.82-second input preflight. Broader training coverage
**fails both synthetic dev accuracy gates** at the unchanged operating point.
Independent-layout precision/recall improve from 88.89%/91.02% to
91.23%/93.91%. On the same current family inputs, retry10 scores
68.07%/94.17% and retry11 scores 27.89%/97.09%: false detections increase
from 91 to 517 while misses decrease from 12 to six. Proposal recall is 100%
in both sets. ONNX CPU parity passes at maximum absolute error
`9.5367431640625e-7`. The ledger records the
[failed outcome](../ml/markers/center/mask_preserving_v24/P1_RETRY11_RESULT.json),
closes execution, and leaves all later gate and production permissions false.
The original retry10 report and its seal archive remain historical evidence.
No private or sealed input was read and no production model was approved.

V24 retry12 completed the unchanged 36 epochs and 12,672 optimizer steps in
29.62 minutes, plus a 132.57-second runtime-input check. Its isolated change
replaces family generic-background prefix selection with uniform sampling,
using a separate fixed random stream. The
[model-free preflight](GOAL-22-V24-UNIFORM-SAMPLER-PREFLIGHT.json) verifies the
same 828 positive, 1,492 hard-negative and 6,788 other-negative examples,
unchanged positive/hard identities and optimizer RNG, and unchanged real-range
sampling. The 185.39-second model-free preflight makes no accuracy claim.
At the unchanged 0.25 operating threshold, family false detections fall from
517 to 119 while misses increase from six to eight. Family precision/recall
are 62.46%/96.12%; seven prohibited hits give a 2.21% rate, above the 2% bar.
Independent-layout precision/recall are 92.43%/92.61%. Both dev gates fail.
ONNX CPU parity passes with maximum absolute error `9.5367431640625e-7`.
The ledger binds the [failed outcome](../ml/markers/center/mask_preserving_v24/P1_RETRY12_RESULT.json),
closes execution, and clears current passed candidates. Independent review
clears the sampler, authorization and exact outcome closure.
A repository-identity startup error occurred before candidate
opening or any optimizer step and is recorded as
[void](../ml/markers/center/mask_preserving_v24/diagnostics/V24_RETRY12_STARTUP_VOID.json).
The process-local repository configuration was corrected and the same candidate
restarted under the same authorization. Private, sealed, production and release
permissions remain false. Retry11's exact opening, result and source snapshot
are retained in its historical attempt archive.

V24 retry13 completes the same 36 epochs, 12,672 optimizer steps and 44,946
examples in 27.73 minutes, plus a 130.59-second input check. The isolated
change stratifies the same 6,788 family background examples by mask and ink
density. The [model-free preflight](GOAL-22-V24-STRATIFIED-SAMPLER-PREFLIGHT.json)
binds all 64 bin capacities and quotas, unchanged positive/hard examples,
and unchanged real-range sampling and optimizer RNG. The actual report confirms
those exact quotas and selected-example identity. Family precision/recall are
54.20%/97.09%: 200 true detections, 169 false detections and six misses.
Compared with retry12, this adds 50 false detections and removes two misses.
Independent-layout precision/recall are 91.60%/93.01%, with 1864 true detections,
171 false detections and 140 misses. Both unchanged dev accuracy gates fail.
The family prohibited-hit rate is 1.36%, within its 2% bar; that does not
override the failed accuracy gates. ONNX CPU parity passes for dynamic counts
1, 8 and 37, with maximum absolute error `2.384185791015625e-6`.
The ledger binds the [failed outcome](../ml/markers/center/mask_preserving_v24/P1_RETRY13_RESULT.json),
closes execution and preserves retry12's exact seal archive. Independent review
verifies closure, all metric denominators and exact sampler quotas. All 46
training-contract and sampler checks pass. No private/sealed read, production
approval or packaged application build occurs.

Three [unused synthetic marker acceptance reserves](GOAL-22-SEALED-RESERVES.json)
are now generated and registered. Each contains the fixed 21 images, 31 panels,
838 markers and all 27 marker styles, with all five family axes held out.
Registration checks exact protocol, generator source snapshots, font provenance,
PNG/annotation correspondence and semantic coverage. Two infeasible header
layouts were recorded as generation voids before the next fixed seed was tried.
No model evaluated these archives. The registry keeps the minimum of two unused
compatible reserves when a future candidate reads one. Candidate-budget and
first-read integration remain required before sealed evaluation. These reserves
cover marker acceptance; they do not replace real-corpus or whole-workflow gates.

The preregistered matched-component OCR experiment completes the same four
train and nine dev panels. Dev detection precision/recall improve from
32.00%/21.86% to 53.60%/36.61%, still below the shared 95% bars. Recognition
and role accuracy are not scored by this diagnostic. Default production
geometry stays unchanged. See the [OCR outcome](../ml/ocr/official_bakeoff/component_geometry_dev_result.json).
Those experiment numbers precede the subsequent crop repair.

The subsequent [DB geometry observation](GOAL-22-DB-GEOMETRY-DIAGNOSTIC.json)
records the initial and expanded boxes of the same accepted detector contours.
Across all 329 train/dev text truths, unmasked precision/recall improve from
30.64%/32.22% with expanded boxes to 68.50%/72.04% with initial boxes. Both
still fail 95%. On dev alone, initial unmasked boxes score 69.23%/78.69%.
The masked initial boxes score 55.87%/65.03% on dev, so the remaining mask
and localization failures require diagnosis. Recognition and roles are unscored.
The observer preserves default scientific output exactly across 13 panels and
104 referenced image/mask files on each side of the combined train/dev
comparison. All 35 execution files are verified;
eight scientific mutations are rejected by the comparator. The OCR suite passes
206 checks with 16 opt-in skips, and 23 scorer safeguards pass. The actual
train/dev runs take 5.78/8.43 seconds; scoring takes 19.16 seconds. This adds
diagnostic observation only and does not activate a production geometry change.

The [margin and metadata repair](GOAL-22-MARGIN-AND-OCR-METADATA-REPAIR.json)
now preserves all 146 train and 183 dev text truths, compared with 17 and seven
omissions before repair. Raster margins expand after overlap selection leaves
one figure, so a nested legend proposal cannot suppress the outer labels.
Default PDF crops and separate retained figures keep their existing behavior.
The diagnostic metadata now selects the existing bounded probability tolerance;
the interrupted OCR output exceeded one by exactly one float ULP. Material
outliers and nonfinite values still fail closed. All 32 panels across the fixed
train/dev inputs and four additional train seeds complete actual runtime seed
composition. Default OCR geometry scores only 36.30% precision and 26.78% recall
on dev, so accuracy remains failed. Recognition and role acceptance are unscored.

The original single-thread deterministic training setup reproduces retry10's
1,903 family samples and selected-index checksum exactly. An unconstrained
diagnostic invocation caused the temporary mismatch; the historical outcome is
unchanged. Additional train seeds retain the existing family assignments, while
development source PNGs remain fixed. The
[completed coverage preflight](GOAL-22-V24-COVERAGE-PREFLIGHT.json) binds
20 train sources across 23 panels and three dev sources across nine panels to
actual runtime inputs. All 500 train and 206 dev markers remain represented;
annotations supply labels, not runtime masks. The family selected-index,
tensor, source-bundle, configuration, and executable identities are recorded.

Validation includes 203 App, 200 OCR, and 152 integration checks passing
(16 OCR and seven integration opt-in skips), 38 focused candidate integration
checks, 11 consensus geometry checks, 11 post-run contract checks, and all 14
tool input guards. Before training, all 94 V24 checks were verified. Independent
review cleared input binding, empty-OCR provenance, geometry isolation, protocol
binding, and the failed-run closure. These are source and synthetic checks,
not normal UI, private acceptance, or distribution acceptance.

The crop batch passes 207 App checks, 59 PDF checks with one opt-in skip, two
probability safeguards, and one metadata helper check. Independent crop review
is clear. The diagnostic compiler reports zero warnings or errors. Source
checkpoint `8cfe81420577c8671acb7c64d7cc414d929a8623` also passed CI run
`34319777804`, including the crop batch and packaging validation.

Retry11's postclosure contract suite passes all 12 checks. The preflight,
configuration dispatch, runtime binding, and closure received independent
review; the outcome is an accuracy failure, not a consumed sealed attempt.

An internal candidate workflow now follows the actual Import, Prepare,
Auto Detect, untouched Review, and CSV export path while leaving normal
Production approval checks active. Source fixtures verify both minimal and
audit CSV values, original-source crop coordinates, eight ordered provenance
envelopes, and rejection of invalid calibration or stale evidence. Detection
commits export evidence only after final validation and cancellation; canceled
runs preserve the prior evidence. All 211 App checks pass in Release, including
the seven focused candidate/direct-adapter checks. Independent review found and
verified the cancellation repair. The private acceptance tool now checks the
frozen 171-project, 120-dev/51-sealed assignment checksum before parsing any
project or creating a model runtime. Its synthetic self-test passes without
private access or inference. These establish wiring and guards, not an actual
model-driven real-graph export. The subsequent frozen candidate factory and
synthetic execution are recorded above; the real/sealed workflow evaluator
and scientific CSV acceptance remain incomplete.

Next work is marker false-detection attribution, OCR error attribution, and
the remaining stage acceptance and real/sealed workflow gates. The
[fresh native runtime](GOAL-22-NATIVE-RUNTIME-REBINDING.json) now has current
provenance binding and exact synthetic parity across all 32 panels, including
256 persisted image/mask files per runtime. Full raw panel comparison retains
scientific structure identities and validates every permitted timing, run,
native, and build-provenance difference. Eight adversarial mutations are
rejected. Its public-axis benchmark is
byte-identical, and 152 integration checks pass with seven opt-in skips. The
current checksum differs from the historical runtime, so the old clean-machine
record remains historical and the current clean-machine gate is blocked.
The final DLL and linker map match across both fresh builds; two intermediate
static archives differ. No production runtime was installed. No packaged build, tag, public
release, or portable deletion occurred. The retained portable is still 0.4.33
from source `3c131f8f`; it does not contain these repairs. Overall Goal 22 is
**FAIL** until all four outcomes and final distribution checks are verified.

## Intermediate source checkpoints, September 8-9

The following chronological records retain their original state and numbers.
Their references to holds or running work describe those earlier checkpoints;
the current snapshot above takes precedence.

The maintainer has resolved REV-004 and REV-006: implement and test the pre-OCR
structural provider; count packaged application builds/rebuilds, with routine
test/CI compiles excluded and an installer/portable pair sharing one version.
Compiler validation and the held runtime-input work may now resume.

The finished model-integrated product target is **2.0.0**. Final production
activation and the complete GitHub installer/portable release are authorized
conditionally on all mandatory gates passing. Intermediate public publication
is withheld. This does not make any candidate approved or any gate passed.
The existing filename and historical 1.0 headings remain for link continuity.
See [recorded decisions](REVIEW_QUEUE.md) and
[current version policy](VERSIONING-AND-RELEASES.md).

The version-policy follow-up passed 62 focused checks, five ledger-generator
scenarios, and ten portable-packaging scenarios. Promotion appends exactly one
ledger row numbered 20000 for 2.0.0, then 20001 for 2.0.1; no intermediate build
records are invented. Central version 0.4.33 and the existing 433 ledger rows
remain unchanged. These are script/fixture checks, not a produced package or a
passed production-release gate.

Source checkpoint `619cde5811ec49e9901db2567062b654eabcea70` contains the
completed policy, production-interface, preprocessing, independent-family,
and workflow-export harness changes. These are source and synthetic evidence,
not approval of automatic stages or proof of the real workflow.

The current marker evidence is the frozen retry9
[family-dev baseline](GOAL-22-PHASE-4R-V24-FAMILY-DEV-BASELINE.json): precision
`0.4723618090452261`, recall `0.912621359223301`, and prohibited-hit rate
`0.02512562814070352` across three held-out synthetic family scenes. It fails
the shared bars. The earlier independent-layout failure remains valid evidence;
the repeated-layout pass does not establish generalization.

V24 retry10 is held before training by the
[input review](GOAL-22-V24-RETRY10-INPUT-REVIEW.json). Its family OCR/artifact
channels currently come from renderer annotations; those oracle-mask inputs
are diagnostic only and cannot establish production compatibility. The ledger
disables execution until the actual upstream input path is bound and tested.
The explicit sampler now rejects dev inputs and binds selected indices. All 35
V24 checks were verified: 34 passed in the full run, and the remaining test
passed after restoring its checksum-matched ignored historical fixture.
The shared C# candidate and seed boundaries now compile and pass their tests.
Candidate execution shares the production parsers and algorithms, validates
image/model/manifest identities, and requires explicitly unapproved adapters.
The local synthetic tool consumes PNGs without renderer answers and marks its
seed outputs as incomplete and ineligible for training. See the
[runtime seed check](GOAL-22-SYNTHETIC-RUNTIME-SEED-CHECK.json) and
[tool documentation](../tools/GraphReader.SyntheticRuntimeEvidence/README.md).

The actual four-image train smoke exposed an empty refined line-family crash.
Discarding that unsupported family repaired the crash without inserting guessed
geometry; 37 Axis tests pass. Three images now complete the real axis/OCR seed
path, compared with one before the repair. The remaining image has zero text
regions and zero recognition crops. Its raw detector produces one proposal and
the component detector produces 69 candidates; the normal consensus returns
none. No recognition envelope or substitute seed is fabricated.

At the preceding runtime-boundary checkpoint, App tests passed 146/146;
OCR tests passed 193 with 16 skipped;
the V24 suite including raster export passes 38/38. Nine input-boundary checks
reject sealed/truth/mask inputs, changed bytes, unsafe paths, and false native
scope. These verify code and diagnostic isolation, not model accuracy. The
diagnostic uses the explicitly recorded development OpenCV binary, not the
separate source-reviewed production-runtime payload.

The next synthetic diagnostic batch passes 167/167 App tests, 17 evaluator
safeguard tests, and all nine input-boundary checks. The residual raster
prototype supplies arrows, brackets, legend structures, and line crossings
without a fabricated model identity. Its executable/configuration identity
binds both App and OCR assemblies. The emitted artifact plane is explicitly
the union of residual output and geometry seed, with all source envelopes.
Cancellation-aware writes preserve completed files and remove only incomplete
writer-owned files. No production adapter is enabled by this diagnostic route.

Three held-out family images complete this extended path. Final recognized OCR
region precision is 0.208333 and recall is 0.136612 against tight glyph boxes
at fixed IoU 0.5. This is not raw-detector recall: 34 of 73 tick truths overlap
padded final boxes below the IoU bar, while 39 have no overlapping final region.
The pre-OCR proxy retains 0.896589 of dark text-box ink eligible after geometry
exclusion. Visual diagnosis attributes 879 suppressed pixels to contained
glyph-like components and 2,244 to components crossing text and graph structure.
These are descriptive synthetic findings, not acceptance or evidence to enable
suppression. See the [structure diagnostic](GOAL-22-SYNTHETIC-STRUCTURE-DIAGNOSTIC.json).

A reusable raster panelizer validates source checksum, PNG chunk integrity,
decoded dimensions, actual rounded crop bounds, non-overlap, and reversible
source mapping. Normal image import and the diagnostic now use this shared path.
Full original bytes remain immutable and shared by the panel crops. Save/reopen
and repeated Auto Detect preserve the exact saved crops and panel identities;
enhancement receives the selected crop. Persisted scientific geometry and export
pixels map back to the full source without changing graph values. Failed reopen
leaves the current workspace intact. Legacy full-image projects remain supported.

This integration passes 191 App tests, 197 OCR tests (16 existing opt-in tests
skipped), 41 evaluator safeguards, and nine input-boundary checks. Independent
review found no remaining material issues in the crop restoration, enhancement,
workspace state, provenance, and retained Auto Detect paths. These are source
and synthetic checks, not native UI or distribution acceptance.

All nine panels from the same three synthetic dev images completed the actual
runtime diagnostic. Final recognized-region precision is 0.3125 and recall is
0.218579, with the full-source denominator retained. Seven truth regions are
outside all emitted crops and count as misses. Compact glyph grouping preserves
580 additional text-box ink pixels in the earlier whole-image diagnostic;
standalone dense zero glyphs remain ambiguous. The current panel-based structural
proxy recall is 0.913926, conditional on available crops and geometry exclusions.
Neither OCR accuracy nor representative text preservation has passed. See the
[raster integration evidence](GOAL-22-RASTER-PANEL-INTEGRATION.json).

The subsequent clean-checkout CI run exposed an ignored-file dependency in three
App checks and two integration regressions. Tests now locate the tracked solution,
validate complete crop/preparation transform chains, and use in-bounds export
fixtures. Saving projected data preserves retained phase IDs and point history.
The full integration suite passes 152 checks with seven opt-in checks skipped;
all 191 App checks were verified across the full run and a corrected two-phase
roundtrip fixture. See the [save and CI repair](GOAL-22-SAVE-AND-CI-REPAIR.json).
Remote CI for follow-up checkpoint `c87281b481e06f3be05027bcfd143412f1f796ad`
passed every job, including clean-checkout build/tests, localization, packaging,
version, and retention audits. See [CI run 34307805078](https://github.com/SKang393/Graph-auto-reader/actions/runs/34307805078).

The September 9 runtime-input repair completes all four train images/panels and
all three dev images/nine panels. The shared panelizer converts its axis-length
threshold from PDF points to raster pixels; short legend frames no longer form
false graph crops in the reproduced case. Fitted axis rays stay within decoded
pixel bounds. Synthetic phase and condition labels use measured non-overlapping
placements. Official OCR resizing now uses the pinned uint8 OpenCV interpolation
path and includes that algorithm in its cache identity.

A valid empty OCR result retains detector execution only, while both configured
model identities remain required. The recognizer is not credited with a run it
did not perform. The independent loader validates the pipeline's separate subrun
UUID and the shared project, panel, and source identities.

The resulting experimental input binding maps all 306 synthetic markers exactly
once across 13 panels. It records 41 non-marker omissions and uses actual runtime
OCR/structure planes rather than renderer annotation masks for family inputs.
Boundary validation passes 56 Python checks, 203 App checks, 200 OCR checks
(16 existing opt-in skips), 152 integration checks (seven opt-in skips), and all
nine tool input guards. The synthetic workflow self-test also passes using its
isolated deterministic adapters. These checks do not approve models or establish
real exported-value accuracy. The exact new [input preflight](GOAL-22-V24-RUNTIME-INPUT-PREFLIGHT.json)
binds 37,741 combined examples, including 1,903 family examples and 173 family
positives. The existing retry10 is authorized for synthetic train/dev only;
its optimizer run is in progress, with no private or sealed evaluation enabled.

The approved pre-OCR provider design has an implementation with six focused
synthetic fixtures. Independent review identified joined marker/connector
classification and cancellation defects, both repaired. Basic fixture text
recall is 1.0; scaled/rotated label recall is 0.990025. Scores remain uncalibrated;
representative preservation evidence is still required. The provider remains
disconnected from production. The residual prototype now runs in the local
diagnostic. Actual panel alignment is now integrated; representative mask
evidence and controlled runtime-input binding remain required before V24 retry10
may train.

V24 retry10 is unfinished. Its change supplements the existing synthetic train
set with four family-disjoint scenes and requires both independent-layout and
family dev to pass. The combined sampling preflight verified 35,838 existing
examples plus 1,936 family examples, including 176 family positives and 348
hard-weighted examples. The 30-test V24 suite passed in 36.45 seconds. These
checks establish input and code contracts only, not trained-model accuracy.
The interrupted pre-sealed reservation was recorded as void using the shared
budget mechanism after confirming that no training process remained. No sealed
budget was consumed, and no private input was read by these checks.

Packages A through E have implementation evidence in the source history:
immutable pre-run policy bindings, fail-closed candidate registration, Pillow
luminance/alpha and proposal preprocessing regression cases, independent
synthetic-family auditing, and actual workflow-backed synthetic CSV export.
Runtime models and complete artifact-mask provenance still require acceptance.
The synthetic workflow uses deterministic stage adapters; it cannot prove
available Production stages or real exported-value accuracy.

On September 8, the portable inventory had 433 ledger entries, exactly one
retained entry and directory, version `0.4.33`, and a matching executable
SHA-256. That retained binary was built from `3c131f8f`, so it does not contain
the subsequent source repairs. The next packaged build is `0.4.34` under the
existing portable sequence. REV-006 now defines compiler-only checks as outside
the packaged-build ledger; the integrated build remains an implementation and
validation task.

Remaining work is automatic-stage repair, frozen real/sealed confirmation,
manifest/store/package promotion, satisfaction of the conditional production
authorization, normal
Production workflow and Chandler evidence, and the integrated build.
[REV-004 and REV-006](REVIEW_QUEUE.md) are resolved. Independent synthetic and
runtime work may proceed. No new revision, private candidate comparison, approval
switch, stable promotion, tag, or public release has occurred in this update.

## Historical checkpoints

The sections below retain their original checkpoint-specific results. Their
PASS labels apply only to their stated scope and source/build identities;
they are not a current Goal 22 completion claim.

## Decision

**FAIL.** Current build 433 is version `0.4.33`, an internal development
checkpoint that is not release-eligible. It must not be tagged or published.
Required production models, final NCNN production
approval, private automatic supplied-example evidence, release artifacts, and
complete installer and portable clean-Windows workflow evidence are absent.
The exact OpenCV and PDFium component clean-machine gates now pass, but they do
not satisfy the remaining production workflow or distribution gates.

`PASS` below means direct repository, command, or runtime evidence satisfies the
named gate. Component-level tests do not satisfy an end-to-end or clean-machine
gate. `FAIL` means the gate failed, is blocked, or has not been directly proven.

## Goal 22 Phase 6 build integrity

**PASS.** Commit `1a1f4aa87329ec0040bff68d03d0855281d5078f` is confirmed on
`origin/main` with the tracked 432-entry `docs/BUILD_LEDGER.json`. Build numbers
1 through 432 map continuously from `0.0.1` through `0.4.32`; all records carry
commit, build-time, and executable SHA-256 evidence, zero are incomplete, one is
retained, and the 22 historical eligible builds are marked
`missed-historical`. No tag or release artifact was fabricated.

After the remote ledger and retained executable were verified, the guarded
retention path deleted 431 stale build directories. Exactly one directory now
remains and matches `latest.json`; its executable checksum still matches. The
20 exact uncited `live-preview-*.png` files were removed separately. `Data`, both
launchers, and all 99 evidence, 16 feedback, and 15 manual-evidence files were
preserved. The empty staging directory was also preserved.

The corrected cadence began with checkpoint `0.4.33`, the required successor to
the historical ledger. The build script rejects dirty and unrecorded preview modes
or a central version that is not the ledger successor. Checkpoint validation
accepts only the ledger maximum, its immediate successor, or an authorized stable
promotion, so the historical correction needs no permanent commit exception. The
incremental ledger test rejects reused stamped versions, and retention tests prove
a failed push leaves both fixture directories in place. Focused results: version policy `53`,
ledger generator `3`, portable packaging `10/10`, portable size `4/4`, and a
real-inventory retention simulation planning exactly 431 deletions before any
destructive action. The ledger generator also preserves round-trip JSON dates
under both Windows PowerShell and PowerShell 7.

The September 4 clean Windows CI failure was traced to checkout newline
conversion of checksum-bound source and notice files, absent local OCR
fixtures, and an unusable hosted DirectML device. The repository now defaults
tracked text to LF while retaining explicit historical CRLF exceptions. Exact
local OCR fixture tests report inconclusive when their ignored payloads are
absent. DirectML parity reports inconclusive on actual CPU fallback, with a
separate mandatory CPU contract test. Existing hashes and license notices are
unchanged. The full local .NET test run passes 1,034 tests with 25 expected
skips. Clean Windows CI run
[33932601744](https://github.com/SKang393/Graph-auto-reader/actions/runs/33932601744)
passes build, application tests, version/retention checks, localization,
release-artifact validation, and packaging audits at commit `88d1fc1`.
No release artifacts were emitted.

The first post-correction portable was actually produced on September 5 from
clean commit `3c131f8f2562ae9526de87205cfed85ace1e64a9`, whose complete Windows
CI run `33933045710` passed. `Build-DevPortable.ps1 -FastTestsOnly` passed its
App, Domain, and integration checks, self-contained publish, portable smoke,
and fail-closed Production-mode smoke. The executable reports file version
`0.4.33.0`, product version `0.4.33+3c131f8f2562ae9526de87205cfed85ace1e64a9`,
and SHA-256 `4f45bdf73adb005a92fadaee56dc6f698e876e707a2961b676cf449e3216600e`.

Outcome commit `c5faa44f138d2b250ab238255f262dafd7ddd9ca` was confirmed on
`origin/main` with 433 complete ledger entries before retention removed the
previous directory. Exactly one portable remains:
`artifacts/dev-portable/builds/0.4.33-20260905T205607798Z-3c131f8f`.
This is ManualPreview with the existing local-development OpenCV runtime, not
a production-approved or redistributable release. No tag was created. The next
produced build must use `0.4.34`.

## Goal 22 Phase 4R sparse-fragment input coverage

The optional `sparse_fragments` generator variant adds project-owned, thin,
anti-aliased line fragments to synthetic train and dev scenes. Its train-only
sampler reserves 1,738 eligible generic proposals without changing the 32,580
negative-example budget. The default generator remains unchanged. No model
was loaded, no optimizer step ran, and no private or sealed input was read.

The aggregate [input audit](../ml/markers/center/real_range_generator_v1/SPARSE_FRAGMENT_AUDIT.json)
passes all five preservation checks: truth coordinates, masks, and center
pixels are unchanged; sparse proposals are retained; negative, hard-negative,
and positive counts are preserved. The generator and sampler suite passes
13 tests in 110.79 seconds. This verifies an input-coverage extension, not a
model gate or production approval.

The earlier retry9 morphology report uses different proposal-label radii
(synthetic 3 pixels, real 5 pixels). Its rate ratio is descriptive, not a
controlled causal result. The assertion that radius differences cannot explain
the gap has not been demonstrated and has been removed from that report;
its updated ledger binding passes all eight training-contract tests. Also,
the initial thin-stroke variant's sparse center-ink p95 was below the reported
real above-threshold median. The extended deterministic schedule now varies
stroke width from 1 through 5 pixels, orientation, and shade. Its center-ink
p95 is `0.45901` on train and `0.45918` on dev, spanning that `0.42729`
reference while preserving all five input invariants. The audit explicitly
records the populations and their different label radii. The full
distribution gap remains unresolved; this range check must not be presented as
proof that another training run will fix real acceptance. Retry9 remains
unconsumed and failed on real-dev, with production and real-sealed access closed.

The subsequent [aligned-radius audit](GOAL-22-PHASE-4R-V24-RETRY9-LABEL-RADIUS-AUDIT.json)
holds the retry9 model, threshold, and synthetic input bytes fixed. Its 3-pixel
control exactly reproduces the historical counts. Moving the diagnostic label
radius to 5 pixels reclassifies 6,006 proposals as positive, including 1,016
above-threshold proposals. Synthetic above-threshold negatives fall from 1,512
to 496, or from `0.00649521` to `0.00218713` of negative proposals. The
recorded real rate at the same 5-pixel radius is `0.03569974`. Thus the radius
mismatch matters, but matching it does not remove the rate gap. This isolates
one diagnostic confound, not the cause of the model's real-image failure.
The run took 16.30 seconds; five focused tests pass. No private image, sealed
split, optimizer, sampler authorization, or production switch was used.

The extended generator and radius-audit suite passes 18 tests in 115.83 seconds.
The [frozen-model confirmation](GOAL-22-PHASE-4R-V24-SPARSE-DEV-CONFIRMATION.json)
then runs the unchanged retry9 ONNX on the extended synthetic dev stream at
threshold `0.25`: 1,992 true positives, 68 false positives, 12 misses, zero
duplicates, and zero prohibited-structure hits across 167 scenes. Precision
`0.96699029` and recall `0.99401198` clear the shared Tier 1 bars in 19.98
seconds, without an optimizer step or private read. Since the existing model
already passes this corrected input subset, that subset does not justify
another training run. The next investigation is Python/C# preprocessing and
full-image proposal parity, not selection against private answers. Real
acceptance and production approval remain failed/closed.

The source parity audit found and reproduced a V24 NMS mismatch: Python's
minimum separation is 5 pixels, while C# used 6.5. Two of five shared synthetic
boundary cases failed in C# before the repair (5.0 and 5.5 pixels, radius 2.5).
The candidate adapter now uses 5 pixels for V24 only; older candidates retain
6.5 pixels. All 124 App tests and all eight Python V24 tests pass, including
distance and radius-scaled suppression boundaries. This source fix does not
alter model weights, approval, or the retained build 433 binary. Full-image
grayscale/alpha and crop/grid parity still need direct cross-language evidence.

A separate [geometry-overlap audit](GOAL-22-PHASE-4R-MARKER-GEOMETRY-OVERLAP.json)
finds that all 167 dev scenes repeat the corresponding training centers,
diameters, and hard-negative geometry in both generator variants, despite
different tensor bytes. The current dev pass therefore does not establish
generalization to unseen graph layouts. Independently seeded synthetic dev
geometry is required before the next training or private-confirmation decision.

The optional `independent_layout` generator path now derives session-ordered
positions and values from split-specific layout seeds (`scene.seed + 20000`),
leaving historical layouts and all diameter bins unchanged. Train and dev
center layouts are disjoint, and repeated generation reproduces exact tensor
bytes; all 14 generator/sampler tests pass in 119.80 seconds. The
[frozen-model independent-layout run](GOAL-22-PHASE-4R-V24-INDEPENDENT-LAYOUT-DEV.json)
has zero center-geometry overlap with historical training. Across 167 scenes,
proposal recall remains 1.0, but the unchanged retry9 model returns 1,802 true
positives, 264 false positives, and 202 misses: precision `0.87221684`, recall
`0.89920160`. It fails both Tier 1 bars in 21.67 seconds. No optimizer, private,
or sealed input was used.

This disproves generalization from the repeated-layout dev pass, not the
entire real-image failure mechanism. The next training repair must use
independent synthetic train/dev geometry under the existing unconsumed
candidate, preserve historical records, and re-clear dev before any sealed
or private confirmation. Another private candidate comparison is not authorized.

## Goal 22 Phase 7 deterministic reasoners

**PASS.** The existing Legends and Phases implementations satisfy their frozen
fixture contracts independently of model availability. Fresh Release runs pass
all `35/35` Legends tests and all `38/38` Phases tests.

Legends covers inside and outside legends, no-legend symbol fallback, two-entry
geometry and embedding matching, arrow shafts and arrowheads as artifacts,
generalization hints for open-circle probes, right-edge participant metadata,
ambiguous text rejection, cross-panel propagation, and preservation of
user-confirmed names. Its 24-case held-out inside/outside mapping fixture clears
the fixed `0.90` accuracy gate.

Phases covers solid, dashed, and dotted divider aggregation; axes, borders, and
annotation exclusion; cross-panel propagation; AB, ABAB, multiple-baseline,
multiple-probe, maintenance, generalization, unknown-later-phase, shared
baseline, applicable-probe, original-x assignment, and manual
move/add/delete/relabel behavior. Its 20-case held-out fixture reports mean
divider error `0.102886` pixels, maximum error `0.218182` pixels, and maximum
session error `0.012121`; every required gate passes. No reasoner source,
contract, model, or dependency change was required.

## Goal 22 Phase 5 synthetic end-to-end wiring

**PASS for the synthetic portion; real confirmation remains pending.** A real
temporary synthetic PNG now runs through `ImageImportService`, production
Import and Prepare stages, `ProductionWorkflowDetectionStage` with the complete
automatic adapter, production Legend and Phase reasoners, Review correction,
and `ProductionWorkflowExportStage` with the real `ExportService`.

The test preserves and rechecks the immutable source SHA-256, keeps every
detection in original-pixel coordinates, retains graph coordinates through a
review correction, and verifies the exact eight-stage evidence chain: axis,
two OCR payloads, three marker payloads, legends, and phases. It then reads
real minimal and audit CSV files, verifies headers, nonempty rows, and artifact
SHA-256 values, and deletes only its temporary output. No network, private
data, model training, threshold change, or production-approval switch is used.

Fresh Release verification passes the focused production-chain pair `2/2`, the
complete App suite `101/101`, and the related orchestrator plus production-stage
integration suite `27/27`. Axis, OCR, mask, center, and classifier inference are
deterministic test adapters; this proves wiring, not model accuracy. The real
Phase 5 scenario remains pending the study-level `real-dev`/`real-sealed`
acceptance harness and the still-false production model approvals.

## Goal 22 revised Phase 1 performance and provider policy

**PASS.** An authorization-safe V30 profile rendered two train and two visible
dev scenes in memory, read no sealed archive, acquired no candidate, used no
private data, opened no model revision, and wrote no checkpoint or profile
file. Frozen input checksums are verified before any session or optimizer work.

Measured preparation took `24,354.794 ms` (`84.9909%`), frozen ONNX session
setup plus detector and recognizer inference took `4,233.291 ms` (`14.7729%`),
and one disposable train-only optimizer step took `67.670 ms` (`0.2361%`).
Preparation was `5.7532x` the frozen portion, so Goal 22's cache condition is
not met and no cache was implemented. The tracked aggregate record is
`docs/GOAL-22-PHASE-1-FROZEN-INFERENCE-PROFILE.json`.

The shared policy now permits train and dev runs to explicitly select any
installed, provenance-reviewed execution provider with mandatory CPU fallback.
Sealed runs remain exactly `CPUExecutionProvider` with `ORT_DISABLE_ALL`.
The current Python environment exposes Azure and CPU ONNX Runtime providers
only, with CPU-only PyTorch; no DirectML availability claim or dependency was
added. Focused verification passes all `5/5` profiler tests and `15/15`
evidence/provider-policy tests.

## Goal 22 Phase 2 promotion disposition

The eight revisions previously recorded as `public_gate_passed_unapproved` are
disqualified from production promotion at this phase. This is not an
authorization hold. `AGENTS.md` Section 8.1 prohibits production approval from
synthetic evidence alone, and `ProductionModelStore` accepts a payload only when
its checksum-bound benchmark evidence has `status=pass`,
`production_approval=true`, and `release_eligible=true`. No revision below has
an automatic private real-graph acceptance result. Adding any of them to the
production index now would falsify those required fields.

| Revision | Passing evidence and selected payload | Specific disqualification from Phase 2 production promotion |
| --- | --- | --- |
| `graph-text-component-fusion-v8` | Public report SHA-256 `6f13a622ee444442089104f5897e203beabb1b941981e75a42629d0d01acfdde`; P2 ONNX SHA-256 `e0254920b26784a87369aa25cc4ec387c6544db30bda4f9542b7ce9a8712e431` | Synthetic-only with no private automatic acceptance, model manifest, model-specific notice, production-store entry, or package-discovery evidence. It is also superseded for detector selection by V9 and V10. |
| `graph-text-component-recall-v9` | Public report SHA-256 `232eea4b01917bcbc786e289752b023f0d1ec9106df0349d2606dce06e76eeb9`; P3 ONNX SHA-256 `2d35ce2f55cee8317dfe1faf0281a6e87693cca485dbdaf39e4039ced5b97d9c` | Synthetic-only with no private automatic acceptance, manifest, store, or package evidence. Its detector payload is superseded by the spaced V10 candidate. |
| `graph-text-spaced-component-recall-v10` | Public report SHA-256 `1c7526cb2147a7a68cdc6eb3d6bc3a073324b3b08f3c81c8b359832f038de7b1`; P2/P3 ONNX SHA-256 `474b8468dbd91416f4e4978dafc46cb2317775d59d821c0470e0cd3e0f6203db` | Synthetic-only with no private automatic acceptance. Its protocol also lacks the explicit model license and notice fields required to create a production manifest, and no store or package entry exists. |
| `graph-text-composite-proposal-role-v11` | Public report SHA-256 `9259765bcb188804ba78a427699ad77910b41c17605515ed799d5cb9feb52840`; P2 ONNX SHA-256 `af13b387140d70946b23ff7349fed82649fde95eb6f6cabe90179b2914a16631` | Synthetic-only with no private automatic acceptance, recognition-plus-marker composition pass, manifest, reviewed artifact notice, store entry, or package evidence. The candidate adapter is not reachable from ordinary Auto Detect. |
| `graph-numeric-component-ensemble-v5` | Public report SHA-256 `a50869bb75f13a39d4601b72e47f49b4b8e684bf4fbb992bd4342e11573e3a20`; P1 ONNX SHA-256 `9db95c41ce396e8b2dff3b525556615528a00ca87f4cc531274374b961417c84` | Synthetic-only with no private automatic acceptance, passing paired detector and independent marker-stage evidence, production manifest, redistribution record, store entry, or package evidence. |
| `official-ppocrv5-image-spacing-v2` | Public report SHA-256 `83afdcfd4fd3dd8b4fa8c0881b5f12d652c0d8c5f9b889af380b4fdf7b83091c`; official ONNX SHA-256 `7839f12b644f574eaf677e92a11bd3e337f4b2f910160666073888783fece743` | Recognition-only synthetic evidence with no private automatic acceptance, passing paired detector and independent marker-stage evidence, schema manifest, store entry, package discovery, or clean-machine composition evidence. The reviewed Apache-2.0 source and notice records are not yet bound by a model manifest. |
| `graph-ambiguity-line-context-v2` | Public report SHA-256 `a54856d932b695936b78fc92e6d3035f670791ec2cb7be3c15bc5b8e9415b05f`; P2 ONNX SHA-256 `6486a2d1e10c69ca07c2c0e8fb3cd4e59bc21ad480a16a01eb99b03e21be2646` | Superseded by the V3 source-group model because this line-box input does not match the production source-group adapter. It also has no private automatic acceptance, manifest, store, or package evidence. |
| `graph-ambiguity-source-group-v3` | Public report SHA-256 `a69b11a164a87f9df959eda863f2263110c2eec3e1d3102f610f7ae32c4e99bd`; P2 ONNX SHA-256 `b8e2773ca3966469081875fc36b3981ef4eb458356d8dfdae2be2722602f0096` | Synthetic-only with no private automatic acceptance, manifest, reviewed artifact notice, store entry, or package evidence. The repository also lacks a tracked `P2_RESULT.json`, so its promotion evidence is incomplete even though the candidate report and gate seals exist. |

The historical gate reports remain immutable evidence and are not rewritten.
Phase 4 may remove the real-data blocker for a selected, still-viable payload;
manifest, store, and package promotion must then occur together before that
payload can report production availability.

## Goal 22 revised Phase 3 acceptance-bar rescore

The authoritative Tier 1 policy is now `ml/policy/acceptance-bars.json`:
detection precision and recall, recognition exact, OCR role accuracy, marker
center precision and recall, and marker shape/fill accuracy target `0.95`; CER
is at most `0.05`; prohibited structures are at most 2 percent of detected
regions. Only the selected operating threshold is graded. Tier 2 separately
requires monotonic and evenly spaced calibration evidence, pixel-traceable
values, and zero silently exported unvalidated calibrations. Fresh structural
verification passes all `36/36` Axis tests and `18/18` Export tests, while the
Phase 5 synthetic chain proves real export files retain pixel provenance.

The aggregate-only rescore performed zero training, inference, sealed reads,
or case-level reads. All five requested OCR records clear every applicable Tier
1 bar, and the previously omitted public-passing V8 production composition has
higher recorded aggregate values on its separate public corpus while already
binding an exact C# factory. V8 is selected with
precision and recall `1.0`, recognition exact `0.99375`, CER
`0.0013979496738117428`, role accuracy `1.0`, and prohibited rate `0`. Its
complete identity binds the V10 component detector, official recognizer and
YAML, V5 numeric recognizer, V3 ambiguity recognizer, protocol, and
`OcrV8ProductionCompositionFactory`. V30 remains in the candidate history.

Marker production-repair-v2 P3 does not clear Tier 1: one false center makes
precision `18/19 = 0.9473684210526315`. Normalized-training-v4 P1 recorded
perfect public precision and recall but its payload is absent. The best
payload-available candidate is runtime-consistency-v2 P2, selected with
precision `0.9820359281437125`, recall `0.9761904761904762`, and zero
prohibited hits.

The full automatic pipeline is not Tier 1 complete. The already approved
marker classifier passes shape accuracy at `0.9907407407407407` but its public
fill accuracy is `0.9444444444444444`. Retroactively enforcing `0.95` would
reject an approved artifact, which AGENTS.md Section 7.4 prohibits. `REV-002`
is resolved: the approved payload retains `0.90`, while `0.95` remains the
future-candidate target.

V8 and runtime-consistency-v2 P2 remain the historical public-passing
selections, but corrected evidence now disqualifies them from current
promotion. The checksum-bound marker proposal adapter exists and passes its
focused tests, so adapter compatibility is no longer a blocker. Corrected
real-range synthetic OCR and real-dev marker scoring both fail Tier 1, while
`real-sealed` remains unopened. No production approval, manifest, model-store
entry, or package promotion was fabricated. The full status is
`ml/policy/goal22-phase3-rescore-result.json`; the older
`ocr-product-bar-v1.json` is retained as superseded historical evidence.

## Goal 22 Phase 4 private real-graph acceptance

**FAIL and generator-repair trigger.** One maintainer-supplied private graph was evaluated
once through the exact V30 P1 OCR candidate and the approved marker classifier
on CPU. The source remained byte-identical at SHA-256
`65bd6e1021bd9a3f2d2564a640ff90060a34e784782f2d847b4b26f9c6c342e1`.
The runner emitted aggregate metrics only, with no case identifiers,
predictions, truth rows, pixels, or private paths. The local aggregate report
SHA-256 is `7196db96503172d8af385dbc5afd20315cc72b67f4cb70310e0a19c83f77174e`.

- V30 OCR failed all four product gates: scene-exact `0/1`, 3 true positives
  from 38 expected regions, 2 false and prohibited regions, 35 misses, 3
  duplicates, recognition exact `0.07894736842105263`, CER
  `1.1222222222222222`, and role accuracy `0.05263157894736842`.
- The marker classifier failed its private gate: shape accuracy `14/24 =
  0.5833333333333334`, fill accuracy `22/24 = 0.9166666666666666`, and both
  open probes were classified incorrectly (`0/2`).

This contradicts the passing synthetic evidence and redirects work to Phase 4R.
The one-image run is now historical diagnostic evidence, not the acceptance
corpus. The authoritative local corpus contains 40 studies, 171 complete `.dig`
projects, and 3,055 digitized points. Its study-level split is frozen and the
120-project `real-dev` diagnostic is complete; the 51-project `real-sealed`
split remains unopened because current candidates fail earlier gates. Private
acceptance therefore remains false. Training, candidate selection, and
threshold tuning on these private data remain forbidden. The historical
one-image aggregate-only record remains
`docs/GOAL-22-PHASE-4-PRIVATE-ACCEPTANCE.json`.

The revised Phase 4R generator repair now passes the corpus-wide synthetic gate.
Its deterministic 10-case matrix spans `361x240`, wide `6352x600`, and tall
`600x4484` inputs; six cases use the observed `1338x492` median. Encoded output
includes RGB8, nonopaque RGBA8 with alpha `224..255`, and JPEG round trips at
qualities 55, 70, and 85. Production resize scale spans `0.1511..2.6593`,
source text spans `11..17` pixels, and post-resize text spans
`1.6625..42.5485` pixels using the true pre-padding scale.
Marker diameter spans `6..25` pixels. All 12 distribution gates and all 39
synthetic tests pass in `163.82 s`.
No optimizer step or candidate selection used private data. Corrected synthetic
OCR and real-dev marker confirmation fail, and `real-sealed` remains unopened,
so production approval stays false.
The aggregate record is `docs/GOAL-22-PHASE-4R-GENERATOR-GAP.json`.

The revised Phase 4 corpus foundation now parses all 171 embedded PNG `.dig`
projects without writing pixels or truth rows. It validates exactly 513 axis
anchors and 3,055 digitized points, freezes a study-isolated 120-project
`real-dev` and 51-project `real-sealed` assignment at SHA-256
`decdac87c0c6d8ee8350b4e26bee2256c551ce20c518732f62fb6d990ea5850a`,
and reports no case or study identifiers. The filesystem has 40 study
directories; 37 contain `.dig` projects. Axis, marker-center, and export metric
helpers are implemented and now drive checksum-bound C# CPU diagnostics. No
private acceptance or production approval is claimed. The aggregate record is
`docs/GOAL-22-PHASE-4-CORPUS-FOUNDATION.json`.

The real-dev distribution probe reads only the 120-project `real-dev` split and
records zero `real-sealed` reads. Embedded images span `361..3200` pixels wide
with median `1167`, and the production pre-padding resize scale spans
`0.3..2.6593` with median `0.8226`. Angular-occupancy measurement covers 1,968
of 2,004 digitized centres (`98.20%`): effective marker diameter is `7` pixels
at p10, `12` at the median, and `24` at p90. The added crop-scale measurement
records p05 `6`, p95 `27`, the full `1..48` range, and ratios against the exact
33-pixel proposal-patch side. Only 912 truths lie inside the calibration-anchor
rectangle, while 1,110 lie inside its 21-pixel expanded proposal search; all
2,004 lie inside the immutable source image. No model inference,
optimizer step, candidate selection, sealed read, name, path, truth row, or
pixel was emitted. The aggregate record is
`docs/GOAL-22-PHASE-4R-REAL-DEV-DISTRIBUTION.json`.

The actual V20/V21/V23 synthetic marker-input audit covers 21 training scenes,
168 training markers, 12 frozen-dev scenes, and 96 dev markers without loading
a model or reading private data. Every tensor remains `[3,168,224]`; the V20
resize degradation is roundtrip-restored before proposal extraction. Training
diameters span `6..24` pixels, exactly the real-dev p05-to-p90 range, but omit
the `24..27` p90-to-p95 tail and the full `1..48` envelope. This bounded scale
gap cannot by itself explain V23 recall `0.15568862275449102`. Stage-level
coverage confirms a separate harness defect: the diagnostic treated
calibration-anchor extrema as plot boundaries even though the anchors do not
annotate plot extents. The aggregate comparison is
`docs/GOAL-22-PHASE-4R-MARKER-INPUT-GAP.json`.

Metadata-only contract inspection confirms that selected OCR V8 has an exact C#
composition factory binding all four model payloads. The selected marker
runtime-consistency P2 consumes `[N,3,33,33]` proposal patches and returns
`[N,4]`; the checksum-bound candidate adapter now implements that contract and
remains permanently fail-closed for ordinary production detection. All payload
checksums are valid, but current corrected evidence fails and production
approval remains false. The audit is
`docs/GOAL-22-PHASE-4-CANDIDATE-CONTRACT-AUDIT.json`.

The exact V8 composition on the corrected 10-scene real-range synthetic set
retains 119 of 226 text regions. Precision passes at `0.952`, but recall is
`0.5265486725663717`, recognition exact is `0.3672566371681416`, CER is
`0.0825`, and role accuracy is `0.5378151260504201`. The official pretrained DB
detector also fails at precision `0.22119815668202766` and recall
`0.21238938053097345`. On `real-dev`, only 28 of 120 projects produce a valid
automatic calibration. The calibrated subset maps 481 of 486 points within
+/- 5 y units, but automatic calibration coverage is only
`0.23333333333333334`. Every invalid calibration is rejected instead of being
accepted silently. The aggregate record is
`docs/GOAL-22-PHASE-4-CORRECTED-OCR-DIAGNOSTIC.json`.

The exact runtime-consistency P2 marker candidate executes successfully on all
120 `real-dev` projects through the production-like V8 text mask and
anchor-derived axis mask. Maximum-cardinality matching at 5 pixels records 391
true positives, 3,332 false positives, and 1,613 false negatives: precision
`0.1050228310502283` and recall `0.19510978043912175`. Both Tier 1 gates fail,
and no `real-sealed` file is read. The aggregate record is
`docs/GOAL-22-PHASE-4-REAL-DEV-MARKER-DIAGNOSTIC.json`.

A checksum-bound no-training OCR diagnostic reuses the selected V10 P2 model
on overlapping source-scale tiles. Fixed `512/128`, `640/128`, and `960/192`
tile-size/overlap strategies all match the same 123 of 226 synthetic truths,
so recall remains `0.5442477876106194`. The best precision is
`0.9461538461538461`, still below Tier 1. Exact manifest verification,
cross-tile IoU deduplication, and canonical maximum-cardinality scoring show
that tiling changes false positives but does not repair the responsible recall
subsystem. The aggregate record is
`docs/GOAL-22-PHASE-4R-OCR-SOURCE-TILING-DIAGNOSTIC.json`.

OCR V32 then follows the required sourcing order by fine-tuning the existing
checksum-bound V10 P2 proposal classifier on five-axis family-disjoint
corrected synthetic train/dev scenes. The baseline dev attempt reaches
precision `0.855072463768116` and recall `0.686046511627907`; a fixed positive
class weight of 4 reaches precision `0.8333333333333334` and recall
`0.6976744186046512`. Dynamic CPU ONNX parity passes at
`3.814697265625e-06`, but both dev attempts remain far below Tier 1. V32 is
retired without a sealed read or candidate consumption. Its aggregate outcome
is `ml/ocr/real_range_classifier_finetune_v32/P1_RESULT.json`.

OCR V33 then exercises a new project-owned multiscale detail-context-geometry
classifier after the required official and fine-tune routes failed. With class
weights `[1,2]`, dev precision is `0.972972972972973` and recall is
`0.4186046511627907`; with `[1,8]`, precision is `0.8947368421052632` and
recall is `0.5930232558139535`. Dynamic CPU ONNX parity passes at
`4.76837158203125e-07`, but neither dev attempt clears Tier 1. V33 is retired
without a sealed read or candidate consumption. Its closed aggregate outcome
is `ml/ocr/real_range_classifier_v33/P1_RESULT.json`.

A source-bound raw-proposal diagnostic then proves the upstream ceiling: the
V32 generator maximum-matches only 61 of 86 dev truths, recall
`0.7093023255813954`, before any classifier. V34 tests the smallest
deterministic repair, percentile contrast plus one-pixel expansion and
regrouping. It recovers no additional truth and raises false proposals from 367
to 16,770. V34 is a failed non-candidate diagnostic with zero training, sealed,
public, or private-data use. The records are
`docs/GOAL-22-PHASE-4R-OCR-RAW-PROPOSAL-DIAGNOSTIC.json` and
`ml/ocr/real_range_proposal_v34/DEV_DIAGNOSTIC.json`.

OCR V35 then trains a project-owned source-scale tiled segmentation detector
after every earlier sourcing route failed. It selects epoch 12 from train loss,
but dev precision is `0.20512820512820512` and recall is
`0.46511627906976744`. Dynamic CPU ONNX parity is
`1.0013580322265625e-05`, narrowly above its `1e-5` limit. V35 is retired
without a sealed read or candidate consumption. Its closed aggregate outcome
is `ml/ocr/real_range_detector_v35/P1_RESULT.json`.

A synthetic-dev-only V35 stage diagnostic then holds the trained ONNX fixed.
All 204 tiles cover their source pixels, and overlap predictions have mean
standard deviation `0.004184835954672203`. Sweeping threshold, morphology,
and component area improves proposal recall only to `0.686046511627907` with
precision `0.30569948186528495`, still far below Tier 1. The responsible stage
is therefore pixel segmentation, not tiling or connected-component settings.
No sealed, public, private, or real-development data is read. The aggregate
record is `ml/ocr/real_range_detector_v35/diagnostics/DIAGNOSTIC.json`.

OCR V36 then retains V35's model, tiling, fixed family-disjoint splits,
optimizer, epochs, and gates while replacing full-box supervision with centered
`0.40`-axis-ratio shrink cores and deterministic paired source-box expansion.
The authorized P1 run completes 156 optimizer steps, but fixed-dev precision
falls to `0.15254237288135594` and recall to `0.313953488372093`, with 27 true
positives, 150 false positives, and 59 false negatives. Dynamic CPU ONNX parity
passes at `9.5367431640625e-06`. V36 is retired without candidate consumption
or a sealed, public, private, article, or real-data read. Its closed aggregate
outcome is `ml/ocr/shrink_region_detector_v36/P1_RESULT.json`.

A fixed-dev V36 stage diagnostic then separates core pixels, connected core
components, paired expansion, and tiling. Best core-pixel recall is only
`0.904147212304312` at precision `0.144945403311025`; predicted-core proposal
recall reaches only `0.20930232558139536`. Expanding ground-truth cores recovers
all 86 source boxes with minimum IoU `0.7456648887988618`, and all source pixels
are tile-covered. Core-pixel segmentation is therefore the responsible stage,
while deterministic expansion and tiling are excluded. No training or
protected-data read is performed. The aggregate record is
`ml/ocr/shrink_region_detector_v36/diagnostics/DIAGNOSTIC.json`.

OCR V37 then restores V35's full-box target and appends one fresh composite
blur, contrast, noise, and JPEG variant for each of the five retained V32 train
scenes. The fixed V32 dev split remains byte-identical. The authorized P1 run
completes 312 optimizer steps over 408 tiles and improves dev precision from
V35's `0.20512820512820512` to `0.23696682464454977` and recall from
`0.46511627906976744` to `0.5813953488372093`. Both remain far below Tier 1,
and CPU ONNX parity `1.1444091796875e-05` exceeds the fixed `1e-5` limit. V37
is retired without candidate consumption or a sealed, public, private, article,
or real-data read. Its closed aggregate outcome is
`ml/ocr/degradation_coverage_detector_v37/P1_RESULT.json`.

A fixed-dev V37 diagnostic then holds the trained ONNX constant. Pixel recall
can reach `0.954382221687978` only at precision `0.2992147127129862`, while the
best connected-component recall is `0.6511627906976745`. Threshold, morphology,
and component-area sweeps cannot clear both bars. All source pixels are covered
by the 204 dev tiles, so full-box pixel segmentation remains the responsible
stage rather than tiling or deterministic postprocessing. No training or
protected-data read is performed. The aggregate record is
`ml/ocr/degradation_coverage_detector_v37/diagnostics/DIAGNOSTIC.json`.

OCR V38 then retains V37's scenes, model, full-box targets, tiling,
postprocessing, dev split, and gates while adding a fixed equal-weight batch
soft-Dice term to the unchanged positive-weighted BCE objective. The authorized
P1 run completes 312 optimizer steps, but dev precision falls to
`0.22169811320754718` and recall to `0.5465116279069767`. CPU ONNX parity
`1.33514404296875e-05` also exceeds the fixed `1e-5` limit. V38 is retired
without candidate consumption or a sealed, public, private, article, or
real-data read. Its closed aggregate outcome is
`ml/ocr/dice_loss_detector_v38/P1_RESULT.json`.

A fixed-dev V38 diagnostic then confirms pixel recall peaks at
`0.9482667948852794` with precision `0.30872562016622557`, while the best
connected-component recall is `0.6162790697674418`. Threshold, morphology, and
component-area sweeps still cannot clear both Tier 1 bars, and all source pixels
remain tile-covered. Full-box pixel segmentation remains the responsible stage;
the Dice objective did not move the bottleneck. No training or protected-data
read is performed. The aggregate record is
`ml/ocr/dice_loss_detector_v38/diagnostics/DIAGNOSTIC.json`.

A V38 fixed-threshold attribution diagnostic then assigns 97,057
false-positive pixels through deterministic structural proxies. Marker or
connecting-line ink accounts for 40,722 pixels, phase dividers for 22,698,
axes or tick lines for 10,251, text-box margins for 9,215, other dark ink for
1,323, and empty background for 12,848. The `600x4484` source dimension has
14 of 16 missed boxes, and medium boxes have 13 of 21 misses. This directs any
next OCR work toward explicit structural-ink suppression rather than another
threshold or generic loss change. The aggregate record is
`ml/ocr/dice_loss_detector_v38/diagnostics/fp_attribution/ATTRIBUTION.json`.

OCR V39 design inspection then finds no distinct safe pre-OCR suppression
input. Axis, tick, phase-divider, and ambiguous-grid geometry is already masked
before detector inference. The dominant marker/connecting-line category is
available only after OCR and marker detection, while text margins require OCR
output or synthetic truth. No candidate is created, no inference is run, and
all protected-read counts remain zero. The blocked design is
`ml/ocr/structural_suppression_v39/BLOCKED_DESIGN_REPORT.json`.

Marker V14 evaluates deterministic compactness, isotropy, radial-support,
line-evidence, and mask-clearance scores over a synthetic `6..25` pixel marker
envelope. Its proposal stream covers every dev truth, but feature distributions
overlap: no fixed threshold clears both bars. Across the preregistered
thresholds, best dev precision is `0.74` and best dev recall is
`0.7604166666666666`. The revision stops without training and reads no private,
public, or sealed data. Its source-, evaluator-, manifest-, and policy-bound
aggregate is
`ml/markers/center/geometry_classifier_v14/DEV_DIAGNOSTIC.json`.

Marker V15 then follows the required sourcing order by fine-tuning the existing
checksum-bound runtime-consistency P2 checkpoint on the disjoint V13 synthetic
train stream. Its authorized P1 run completes 342 optimizer steps and passes
dynamic CPU ONNX parity at `8.940696716308594e-08`, but every dev threshold
fails. The best F1 operating point has precision `0.3959731543624161` and recall
`0.6145833333333334`. P1 is retired without a sealed read or candidate
consumption, and no public, private, production, or release approval is opened.
The closed outcome is
`ml/markers/center/geometry_finetune_v15/P1_RESULT.json`.

Marker V16 then exercises a project-owned scale-separated multi-scale CNN after
the compatible fine-tune failed. Positive-loss weight 4 reaches precision
`1.0` and recall `0.65625`; weight 16 reaches precision `1.0` and recall
`0.6770833333333334`. Dynamic CPU ONNX parity passes at
`4.76837158203125e-07`, but both dev attempts remain below Tier 1. V16 is
retired without a sealed read or candidate consumption. Its closed aggregate
outcome is `ml/markers/center/scale_classifier_v16/P1_RESULT.json`.

Marker V17 then retains V16 and repeats large-radius positive train proposals
after stage-level diagnosis showed confidence caused 29 of 31 misses. It passes
dynamic CPU ONNX parity at `1.0058283805847168e-07` and retains precision
`1.0`, but recall improves only to `0.6979166666666666`. V17 is retired without
a sealed read or candidate consumption. Its closed aggregate outcome is
`ml/markers/center/metric_aligned_v17/P1_RESULT.json`.

Marker V18 then retains V17 and mines train positives below confidence `0.25`.
The authorized warmup finds zero qualifying train positives, so the final run
uses the unchanged 3,212 examples and reproduces precision `1.0` and recall
`0.6979166666666666`. One report-timer failure is recorded as a void run before
the repaired runner completes. V18 is retired without candidate consumption or
a sealed read. Its closed aggregate outcome is
`ml/markers/center/hard_positive_v18/P1_RESULT.json`.

Marker V19 then retains the fixed V13 dev split and V16/V17 model contract but
expands only the disjoint synthetic train families across the measured resize,
luminance, alpha-composite, JPEG, text-scale, and marker-scale envelope. The
authorized P1 run completes 864 optimizer steps and improves recall to
`0.7916666666666666` at precision `1.0`, with zero false positives, duplicates,
or prohibited hits. Dynamic CPU ONNX parity passes at
`2.384185791015625e-07`, but 20 of 96 dev truths remain missed. V19 is retired
without candidate consumption or a sealed, public, private, or real-data read.
Its closed aggregate outcome is
`ml/markers/center/train_family_v19/P1_RESULT.json`.

A fixed-dev V19 diagnostic then proves every one of the 96 truths has both raw
and geometry-filtered proposals within 3 pixels. Nineteen of the 20 misses have
classifier confidence below `0.25`; one high-confidence proposal is rejected
by the marker-geometry consensus guard. At threshold `0.10`, recall rises only
to `0.8125` while precision falls to `0.975`; at threshold `0`, recall is
`0.9375` with 423 false positives. The remaining defect is classifier
separation, not proposal availability. No training or protected-data read is
performed. The aggregate record is
`ml/markers/center/train_family_v19/diagnostics/V19_DIAGNOSTIC.json`.

Marker V20 then retains V19's twelve broad train scenes byte-for-byte and adds
nine fresh train-only scenes covering open squares, 11-to-12-pixel marker
radii, and intersection-heavy geometry. The authorized P1 run completes 1,476
optimizer steps and improves fixed-dev recall to `0.8645833333333334` at
precision `1.0`, with zero false positives, duplicates, or prohibited hits.
Dynamic CPU ONNX parity passes at `2.384185791015625e-07`, but 13 of 96 truths
remain missed. V20 is retired without candidate consumption or a sealed,
public, private, article, or real-data read. Its closed aggregate outcome is
`ml/markers/center/tail_coverage_v20/P1_RESULT.json`.

A fixed-dev V20 diagnostic then confirms all 96 truths still have raw and
geometry-filtered proposals within both 3 and 5 pixels. Ten misses are below
confidence `0.25`, two are rejected by marker-geometry consensus, and one is
rejected by the artifact mask; NMS contributes none. Threshold `0.10` reaches
88 of 96 truths at precision `1.0`, still below Tier 1. Nine of the 13 fixed
threshold misses are open squares and nine have 11-to-12-pixel truth radii. No
training or protected-data read is performed. The aggregate record is
`ml/markers/center/tail_coverage_v20/diagnostics/V20_DIAGNOSTIC.json`.

Marker V21 then retains V20's 21 train scenes, architecture, proposals,
regression terms, threshold, runtime guards, and gates while replacing only the
weighted BCE classification term with fixed focal loss at alpha `0.25` and
gamma `2.0`. The authorized P1 run completes 1,476 optimizer steps and improves
fixed-dev recall to `0.9166666666666666` at precision `1.0`, with zero false
positives, duplicates, or prohibited hits. Dynamic CPU ONNX parity passes at
`4.76837158203125e-07`, but 8 of 96 truths remain missed. V21 is retired without
candidate consumption or a sealed, public, private, article, or real-data read.
Its closed aggregate outcome is
`ml/markers/center/focal_confidence_v21/P1_RESULT.json`.

A fixed-dev V21 diagnostic then confirms all 96 truths retain raw and
geometry-filtered proposals. Five of the eight misses are marker-geometry
vetoes, two are artifact-mask vetoes, and only one remains below confidence
`0.25`; NMS contributes none. Lowering the threshold to `0.05` reaches 91 of
96 truths at precision `1.0`, still one truth below the Tier 1 recall count.
The dominant remaining defect is deterministic runtime postprocessing, not the
model or proposal stream. No training or protected-data read is performed. The
aggregate record is
`ml/markers/center/focal_confidence_v21/diagnostics/V21_DIAGNOSTIC.json`.

A zero-optimizer V22 feasibility check then retains V21's threshold,
proposals, decoding, ordinary vetoes, and NMS while testing only a generic
high-confidence veto bypass. Floor `0.90` bypasses one candidate and reaches
89 of 96 truths at precision `1.0`; floors `0.95` through `0.999` bypass none
and retain 88 truths. Every floor has zero prohibited hits, but none clears the
`0.95` recall bar, so no V22 candidate is created. The aggregate record is
`ml/markers/center/veto_override_v22/V22_FEASIBILITY_DIAGNOSTIC.json`.

Marker V23 then retains the V21 ONNX, threshold, artifact veto, proposals,
offset decoding, radius clipping, and NMS while evaluating the existing ring
support rule at fixed integer radii 3 through 12. The authorized zero-optimizer
P1 run passes synthetic dev with 93 of 96 truths, precision `1.0`, recall
`0.96875`, F1 `0.9841269841269841`, zero false positives, zero duplicates, and
zero prohibited hits. The candidate remains unconsumed and unapproved pending a
production-runtime postprocessor and held-out `real-sealed` confirmation. Its
tracked outcome is
`ml/markers/center/multiradius_geometry_v23/P1_RESULT.json`.

The candidate-only C# proposal adapter now binds the exact V21 ONNX SHA-256 and
implements V23 multi-radius consensus while preserving the 0.25 threshold,
proposal tensor, offsets, radius clipping, masks, CPU provider, candidate bound,
and NMS. Legacy P2 behavior remains unchanged and ordinary production detection
still fails closed. All 12 focused App tests pass in Release. No real corpus is
read; V23 remains pending production-composition parity and `real-sealed`.

The aggregate real-acceptance tool now has an explicit
`--run-real-dev-marker-v23` mode requiring the corpus root and exact ignored
model path. It preserves the historical P2 command and emits V23 mode and model
identity only for the new command. The Release build and no-corpus self-test
pass with zero warnings, inference runs, and sealed reads.

The initial C# V23 run executed all 120 `real-dev` projects, but its 312 true
positives, 421 false positives, 1,692 false negatives, precision
`0.4256480218281037`, and recall `0.15568862275449102` are superseded because
the harness incorrectly treated calibration-anchor extrema as plot bounds. It
remains historical aggregate evidence with zero `real-sealed` reads at
`docs/GOAL-22-PHASE-4-V23-REAL-DEV-MARKER.json`.

The V23 candidate adapter and dev harness now expose aggregate counters for the
proposal grid, ink and mask rejections, emitted/inferred proposals, threshold,
decoded masking, geometry consensus, plot bounds, NMS, and final candidates.
The diagnostic also matches pre-NMS candidates in memory and emits only their
aggregate true-positive, false-positive, false-negative, precision, and recall
values. Candidate lists and case-level results are never serialized. Existing
candidate outputs are unchanged. All 14 focused App tests, the Release tool
build, and the no-corpus self-test pass; no corpus read occurs in this
instrumentation step.

The explicit aggregate-only diagnostic then executes all 120 `real-dev`
projects with zero failures and zero `real-sealed` reads. The anchor-bounded
raw grid covers only 1,108 of 2,004 truths (`0.5528942115768463` recall),
matching the independently measured 1,110 truths inside the expanded anchor
rectangle within two grid-quantization cases. The ink filter loses no
additional truths. OCR masking leaves 1,042, artifact masking leaves 885, and
the fixed classifier threshold leaves 356. Before NMS the diagnostic finds
316 true positives, 1,140 false positives, and 1,688 false negatives, for
precision `0.21703296703296704` and recall `0.15768463073852296`. Final recall
is `0.15568862275449102`, so NMS costs only four true positives and
`0.001996007984031936` recall. NMS and ink support are not primary failure
sources. The first correction is a truth-independent full-image search for the
standalone marker score, while preserving all model and postprocessing values;
generator or candidate changes before that rerun would react to invalid harness
evidence. The tracked aggregate record is
`docs/GOAL-22-PHASE-4-V23-REAL-DEV-STAGES.json`.

The corrected independent marker run then searches the full immutable source
image while retaining the exact V23 model, masks, `0.25` threshold, geometry,
and NMS. All 2,004 truths have raw-grid and ink-supported proposals. OCR masking
retains 1,929 (`0.9625748502994012` recall), and artifact masking retains 1,772
(`0.8842315369261478`). The fixed classifier retains 913 truths plus 7,653
false candidates before the remaining postprocessing. Final output has 897 true
positives, 3,565 false positives, and 1,107 false negatives: precision
`0.20103092783505155` and recall `0.4476047904191617`. The candidate therefore
still fails both Tier 1 gates. The responsible repair is the synthetic
three-channel marker/non-marker patch and hard-mask representation, not proposal
coverage, ink support, search bounds, or NMS. The run writes aggregates only,
records zero `real-sealed` reads, and is tracked in
`docs/GOAL-22-PHASE-4-V23-REAL-DEV-FULL-IMAGE-STAGES.json`.

Matched aggregate truth-patch measurement then compares all 2,004 real-dev
centres with the 168 V20 synthetic training centres using the exact zero-padded
`[3,33,33]` input. The luminance ranges overlap, but mask topology does not:
real-dev has 75 OCR-centre hard-mask hits (`3.74%`) and 332 artifact-centre hits
(`16.57%`), while synthetic train has zero and two (`1.19%`). Synthetic artifact
masks are often present elsewhere in a patch, but rarely cross the marker
centre; real axis masks do. The real ink-centre p10 is `0.36376468896865843`
versus synthetic `0.035294`, exposing a second lower-tail mismatch. The next
Phase 4R artifact is therefore a synthetic-only generator spanning the measured
diameter, centre-mask topology, and ink-statistic ranges before any new marker
candidate is opened.

The corrected `marker-center-real-range-generator-v1` now passes all seven
distribution gates with disjoint 167-scene train and dev families and 2,004
markers per split. Labelled diameters match the real-dev `1`, `6`, `12`, `24`,
`27`, and `48` pixel minimum/p05/median/p90/p95/maximum values; isolated pixel
measurement confirms the exact renderer covers `1..49` pixels. Synthetic ink
mean is p05 `0.08771648910232868`, median `0.18735565235314283`, and p95
`0.7026954293251038`, spanning the real distribution. Each split independently
contains 75 OCR-centre and 332 artifact-centre hard-mask hits measured from its
own tensors. Axis-like artifact crossings reach patch-mean p90
`0.12947657704353333` and maximum `0.2764003673094582`, spanning the real
`0.12121212121212122` and `0.2396694214876033` values. Four focused tests pass;
no model, optimizer, private data, or candidate is used. The aggregate artifact
is `ml/markers/center/real_range_generator_v1/AUDIT.json`.

A zero-optimizer V24 feasibility run then keeps every ink-supported proposal,
retains OCR and artifact masks as classifier channels, removes both hard
centre-mask vetoes, and preserves the frozen V21 ONNX, `0.25` threshold, V23
multiradius geometry, radius clipping, and NMS. Proposal recall is `1.0` across
all 2,004 corrected synthetic dev truths, but the frozen model yields 1,001 true
positives, 442 false positives, and 1,003 false negatives: precision
`0.6936936936936937`, recall `0.499500998003992`, and F1
`0.5807948941108211`. It has zero duplicates but 167 prohibited-structure
hits. Mutually exclusive recall is `0.5914179104477612` unmasked, `0.359375`
OCR-only, `0.08411214953271028` artifact-only, and `0.0` when both masks cross
the centre. The CPU-only run consumed no candidate, performed zero optimizer
steps, and read no private or sealed data. Exactly one synthetic V24 training
candidate is now startable. The feasibility record is
`ml/markers/center/mask_preserving_v24/FEASIBILITY.json`.

V24 P1 then fine-tunes the exact Apache-2.0 V21 checkpoint on the corrected
synthetic train split with mask-preserving proposals. Its first attempt is void
before any sealed read because alternating generator tensors were `float64`;
the failure record is retained, the generator is repaired to deterministic
`float32`, and the same authorization reruns without consuming the candidate.
The valid run completes all 10,080 fixed optimizer steps. At the preregistered
`0.25` dev threshold it retains 1,982 of 2,004 truths with 22 false positives
and 22 misses: proposal recall `1.0`, precision `0.9890219560878244`, recall
`0.9890219560878244`, F1 `0.9890219560878244`, zero duplicates, and zero
prohibited hits. The exact ONNX SHA-256 is
`35a0e5563228cfa384a3c4ce4d9c68afaeb57db8dd859f77fcbf5c3d2980bd9e`;
CPU parity passes at `2.384185791015625e-06`. P1 remains unconsumed and
unapproved with zero real-dev, real-sealed, or public-gate reads. The next step
is checksum-bound candidate-only C# runtime integration and aggregate real-dev
confirmation. The tracked outcome is
`ml/markers/center/mask_preserving_v24/P1_RESULT.json`.

The candidate-only C# adapter now binds those exact V24 P1 bytes, emits every
ink-supported proposal while retaining OCR and artifact masks as model input
channels, and removes only the two hard centre-mask vetoes. It preserves the
fixed `0.25` threshold, CPU-only cache-bypassed inference, offset and radius
decoding, radii `2.5..8`, multiradius geometry `3..12`, candidate limit, full
image diagnostic bounds, and NMS. V23 behavior remains unchanged, and ordinary
production detection still fails closed because V24 `IsApproved` is false.
All 16 focused App tests pass, the Release real-acceptance tool builds with zero
warnings or errors, and its no-corpus self-test passes. Aggregate-only real-dev
is now authorized; real-sealed remains locked.

The exact V24 C# runtime then executes all 120 `real-dev` projects with zero
failures and zero `real-sealed` reads. Raw-grid, ink-supported, and
mask-preserved proposal recall are all `1.0`, but the fixed classifier emits
19,037 above-threshold candidates: 1,170 match truths and 17,867 do not. Final
output has 1,143 true positives, 8,176 false positives, and 861 misses, giving
precision `0.12265264513359803` and recall `0.5703592814371258`. Relative to
V23, recall improves by `0.1227544910179641`, but precision falls by
`0.07837828270145352`; both Tier 1 gates still fail. This passing-synthetic,
failing-real contradiction identifies another generator defect: corrected
positive and mask topology are insufficient because synthetic non-marker
proposal patches do not span the real negative stream. P1 remains unconsumed,
production approval stays false, and real-sealed stays locked. The aggregate
record is `docs/GOAL-22-PHASE-4-V24-REAL-DEV-FULL-IMAGE-STAGES.json`.

A separate synthetic audit now measures the exact mask-preserving proposal
stream without changing generator scene bytes or the P1 source binding. Each
split contains 179,445 proposals: 3,258 positives under the fixed three-pixel
training rule and 176,187 negatives. Synthetic dev negative ink mean has p05
`0.02883739396929741`, median `0.10055533796548843`, and p95
`0.42676535844802765`; centre-5x5 ink median is
`0.0030915404204279184`. A matching candidate-only C# diagnostic now enumerates
the real V24 full-image proposal stream, emits only positive/negative aggregate
feature quantiles, performs zero marker-model inference, and preserves source
bytes. Seventeen focused App tests, the Release tool build, and its no-corpus
self-test pass. The synthetic record is
`ml/markers/center/real_range_generator_v1/NEGATIVE_PROPOSAL_AUDIT.json`;
aggregate real-dev measurement is authorized while real-sealed remains locked.

That no-marker-inference real-dev probe completes all 120 projects in
`20.8536359` seconds with zero failures, source mutations, or sealed reads. It
labels 3,576 of 1,358,010 ink-supported proposals positive at the fixed
three-pixel training distance and 1,354,434 negative. Negative ink-mean central
coverage is similar, but two decisive gaps remain. Real ink-maximum p05 is
`0.1921568512916565` versus synthetic `0.8337485790252686`, so synthetic train
omits faint structure. Real OCR-mask mean p95 is `0.7272727272727273` versus
synthetic `0.022956840693950653`, so it also omits OCR-heavy negative patches.
P1 sampled 32,580 negatives without explicit coverage of either missing family.
This is a generator and train-only sampling defect, not authority to tune the
private threshold or model. The aggregate gap record is
`docs/GOAL-22-PHASE-4R-V24-NEGATIVE-PATCH-GAP.json`; P1 remains unconsumed and
real-sealed remains locked.

The synthetic generator now adds two negative-only families outside every truth
centre window. Faint full-width strokes give negative ink-maximum minimum and
p05 `0.11372548341751099`, covering the real `0.11372548341751099` minimum and
`0.1921568512916565` p05. OCR-heavy lower-band regions raise negative OCR-mask
mean p95 to `0.8484848737716675`, covering real `0.7272727272727273`. Both
train and dev retain 2,004 truths, exact 75 OCR and 332 artifact centre hits,
all seven positive-distribution gates, disjoint seeds, and `float32` tensors.
Each split now contains 224,208 negatives and the explicit hard-negative kinds
`faint_line` and `ocr_heavy`. Five focused tests pass. No model or private data
is used; deterministic stratified train-only sampling remains required before
the unconsumed P1 retry.

The first stratified P1 retry retains all 6,012 legacy hard negatives and
allocates the remaining 26,568 slots across faint, OCR-heavy, artifact, and
generic strata. It fails the fixed `0.25` dev precision bar at
`0.8823529411764706` while recall is `0.9880239520958084`. Aggregate diagnosis
attributes all 264 final false positives to generic negatives; faint-low,
faint-p05, and legacy hard strata have zero raw above-threshold proposals, and
OCR-heavy has two. No threshold change is selected. A second same-P1 retry
therefore preserves real-quantile tail counts and restores 21,681 of the fixed
32,580 negative slots to the generic stratum. Its deterministic selection hash
is `d5b7c442827dab27085b2d76bb4f7a29d53e113b039b6c97cb6b2bfb1c0ec35d`.
At fixed `0.25`, retry 2 passes corrected synthetic dev with 1,974 true
positives, 29 false positives, and 30 misses: proposal recall `1.0`, precision
`0.9855217174238642`, recall `0.9850299401197605`, F1
`0.9852757674070376`, zero duplicates, and zero prohibited hits. Exact retry2
ONNX SHA-256 is
`98c605eea8a579d28ad0e5d3b355458ab1e1883c3947c4a3a442557d639f5b79`;
CPU parity passes at `9.5367431640625e-07`. P1 remains unconsumed with zero
private or sealed reads. Candidate-only C# runtime rebinding and real-dev are
next. The adapter is now rebound to those exact retry2 bytes while preserving
the mask channels, fixed threshold, multiradius geometry, candidate limits,
CPU-only execution, and production fail-closed boundary. Seventeen focused App
tests pass, the Release real-acceptance tool builds with zero warnings or
errors, and its no-corpus self-test passes. Aggregate-only real-dev is
authorized; real-sealed remains locked.

The exact retry2 C# runtime then completes all 120 real-dev projects with zero
failures and zero real-sealed reads. Proposal recall remains `1.0`, but 22,510
model outputs exceed the fixed threshold: 1,236 match truths and 21,274 do not.
Final output has 1,202 true positives, 8,786 false positives, and 802 misses,
giving precision `0.12034441329595515` and recall `0.5998003992015968`.
Relative to retry1, recall improves by `0.029441117764471003`, while precision
falls by `0.0023082318376428795`; both gates still fail. Scalar negative-range
coverage and generic rebalancing therefore do not explain the private failure.
The remaining generator defect is spatial morphology and topology of real
above-threshold false proposals. P1 remains unconsumed, production approval is
false, and real-sealed remains locked. The aggregate record is
`docs/GOAL-22-PHASE-4-V24-RETRY2-REAL-DEV-STAGES.json`.

The later V24 diagnostic loop remains on the same unconsumed P1 candidate and
uses only synthetic train/dev data for optimizer work. Retry 9 adds deterministic
connector-band negative coverage and passes corrected synthetic dev at the fixed
`0.25` operating point with 1,992 true positives, 69 false positives, 12 misses,
precision `0.9665211062590975`, recall `0.9940119760479041`, zero duplicates,
zero prohibited hits, and CPU parity `9.5367431640625e-07`. Its aggregate-only
real-dev confirmation completes all 120 projects with zero failures and zero
real-sealed reads, but fails both Tier 1 marker gates: 1,455 true positives,
17,902 false positives, 549 misses, precision `0.07516660639561916`, and recall
`0.7260479041916168` in `449148.3793999999` ms. The report emits no case-level
output, truth rows, or pixels and is not used for threshold or candidate
selection. Retry 9 remains unconsumed and unapproved. The next permitted action
is a synthetic generator repair before any new retry or sealed evaluation.
The aggregate morphology comparison now confirms the remaining generator gap:
real negatives cross the fixed threshold at `0.03569974209311796`, versus
`0.006495208065742503` synthetic, a `5.496320014967361` ratio. Real
above-threshold negatives have median center fill `0.42729412317276005`
versus `0.9716200649738311` synthetic and median covariance eigenvalue ratio
`3.708286974966204` versus `1.6684595643857898`, showing that sparse,
anti-aliased, elongated, off-center fragments remain underrepresented. The
comparison explicitly records the frozen 3-pixel synthetic label radius and
the 5-pixel real diagnostic radius. It changes no threshold, architecture,
loss, candidate, or private-data selection rule. The tracked records are
`docs/GOAL-22-PHASE-4-V24-RETRY9-REAL-DEV-STAGES.json` and
`docs/GOAL-22-PHASE-4R-V24-RETRY9-MORPHOLOGY-GAP.json`.

## Evidence snapshot

| Evidence | Result | Scope |
| --- | --- | --- |
| OCR component-proposal detector V6 | FAIL, public gate consumed | P1 ran exactly once from committed source and passed all 48 validation scenes with 144/144 text regions, zero false positives, misses, duplicates, or prohibited-structure hits at threshold `0.65`; CPU ONNX parity passed at `2.86102294921875e-06`. Its single checksum-bound CPU public evaluation then retained 192/192 truth regions with zero misses and zero duplicates, but seven false regions left only 57/64 scenes exact and produced seven prohibited-structure hits. Public report SHA-256 is `560e067c92317d390658121d4a6b6d71a5ac25ec49c338456e5a18c3c415c5d6`; exact ignored ONNX SHA-256 is `9aa4206ab91f4123d6c7bb38b4090d8e9e3257a9eb823bf8e355373e8352ee97`; direct input and output tensor-stream SHA-256 values are `869f3f8f3a1127caf803cf7e66badd6c1546c7a7f8e12a09abd8fdeebd7f41c6` and `78de90f374cc7489d44ba467f1b59454b81802db10ef677e266ae596c45495e1`. P1 cannot rerun. P2/P3 are retired because the public split is exposed and the only gate is consumed. No manifest, model-store entry, package payload, production approval, or release eligibility exists. |
| OCR morphology proposal and role V13 | FAIL, public gate consumed | P1 trained on fresh synthetic morphology families, retained all 1,280 validation truths and every role, but left one prohibited false proposal and exceeded the fixed `1e-5` parity gate at `1.52587890625e-05`. P2 reused the exact P1 checkpoint with zero optimizer steps and only a fixed output scale. It passed all 160 validation scenes with zero false, missed, duplicate, or prohibited regions, perfect role accuracy, and CPU parity `7.62939453125e-06`. The only authorized 224-scene public run then passed 223 scenes with 1,792 true positives, one prohibited false region, zero misses, zero duplicates, and perfect role accuracy. Report SHA-256 is `fe0a0d63d0c00c35c463e45c11a87f97aa81bb625d0744c8c2307537b6d6d2ff`. No case-level detail was emitted. P1, P2, and the public gate are consumed; P3 is retired. No manifest, model store, package, private validation, production approval, or release eligibility exists. |
| OCR structural graph proposal and role V14 | FAIL, exhausted before public gate | P3 completed all 808 fixed optimizer steps and passed CPU ONNX parity at `9.5367431640625e-06`. It eliminated every false proposal, duplicate, and prohibited structure hit, but retained 1,150/1,152 truths and only 89/144 scenes exactly. Overall role accuracy was `0.9539130434782609`; YTick passed at `0.8680555555555556`, but PhaseHeading `0.7638888888888888` failed the fixed `0.85` per-role gate. Candidate report SHA-256 is `89b66d006166a8a2efb770c029e0a3f9dc76a5ed0425a2ee16a9f0900da262a4`; rejected ONNX SHA-256 is `6fc8ec245262eb99aaa2c6fd315be97353dea569fc336aeb248b2a808f9014d6`. P1 through P3 are consumed, the public archive remains unopened with zero evaluations, and no manifest, store, package, private validation, approval, or release eligibility exists. |
| OCR layout-conditioned proposal and role V15 | FAIL closed, exhausted after public gate | P3 passed all 160 validation scenes. Its only public run then passed 217/224 scenes exactly with 1,791/1,792 truths, one false region, one miss, one prohibited hit, zero duplicates, role accuracy `0.9972082635399219`, and minimum role accuracy `0.9820627802690582`. No case-level failure details were emitted. Public report SHA-256 is `8bd7170db115f6fccbfc9b998bd5f6fce0d8ae001469b692fa07e8392068553d`. P1 through P3 and the public gate are consumed. No manifest, store, package, private validation, approval, or release eligibility exists. |
| OCR margin-robust layout proposal and role V16 | FAIL closed, exhausted before public gate | V16 used fresh 640-train, 192-validation, and 256 truth-hidden public families and only aggregate V15 trigger metrics. P3 completed 480 optimizer steps with 10,240 training-only hard negatives, passed CPU ONNX parity at `3.814697265625e-06`, preserved all 1,536 validation truths and every role at thresholds `0.56` through `0.68`, and produced zero duplicates. Four false prohibited regions left 188/192 scenes exact and no required three-threshold zero-error window. P3 report SHA-256 is `c1121b120e89accbf4153aece1a6f8af819df9eafd74bde01f1e81e167bfacb1`; rejected ONNX SHA-256 is `4939d9e23c1264436ccabe2e292d2152b9f3de09f410bebe92f53a938559fde2`. P1 through P3 are consumed. The public archive SHA-256 `663b9a0c1600ca65c04c2acf85a021a057777a44f7e930ce82fc6beb4b7a97c1` remains unopened with zero evaluations. No manifest, store, package, private validation, approval, or release eligibility exists. |
| OCR structural-veto proposal and role V17 | FAIL closed, three-candidate budget exhausted | V17 bound exact V16 P3 checkpoint SHA-256 `6ed1948787b1c396445ac16f8fa3f067cc2a2460e907ddd6ab9a828ab8c9136b` and fresh synthetic splits of 720 training, 216 validation, and 288 truth-hidden public scenes. Selection manifest SHA-256 is `d68b49383a1de32b0ab68a08fe2531d5365e8dd2a6905eb97fdd93053de0e939`; unopened public archive SHA-256 is `59f25355b8f2b97ba5da229583fed8e702351e625025b26443c6c61983c44714`. P1, P2, and P3 were each executed once and consumed. Final P3 report SHA-256 `9449ddcd2ce21de493fb46811f7949494a655a64d59a3df0cd97e2a2772be851`, checkpoint SHA-256 `f5b82318ef3d06ba4bef98175f64c95bff4378fb4c6061f75bfee2a5f3b4f1ae`, and ONNX SHA-256 `ca32487f1df2c3fea1b8c2f51daf7578ed9756e9140d1b0eaf2a16b283591262` are direct ignored evidence. At threshold `0.64`, P3 passed 214/216 validation scenes with all 1,728 truths, one prohibited false region, zero misses or duplicates, role accuracy `0.9994212962962963`, minimum per-role accuracy `0.9953703703703703`, and CPU parity `5.245208740234375e-06`; it produced no passing three-threshold window. The public archive was never opened and has zero evaluations. No rerun, public gate, production approval, model store, package, private validation, or release eligibility is authorized. |
| OCR recognition-confirmed proposal and role V18 | FAIL closed, single-candidate budget exhausted | V18 bound exact V17 P3 detector ONNX SHA-256 `ca32487f1df2c3fea1b8c2f51daf7578ed9756e9140d1b0eaf2a16b283591262` at threshold `0.64` and exact official PP-OCRv5 English mobile recognizer ONNX SHA-256 `7839f12b644f574eaf677e92a11bd3e337f4b2f910160666073888783fece743` at fixed mean collapsed-CTC confidence `0.60`. The single zero-optimizer P1 executed exact 192-scene stored validation bytes through both CPU payloads. It passed recognition exact `0.96875`, CER `0.004607852548718441`, role accuracy `0.998046875`, and minimum role accuracy `0.9895833333333334`, but failed exact region selection at 189/192 scenes, 1,533/1,536 truths, one prohibited false region, three misses, and zero duplicates. Result SHA-256 is `8b77be5cf32db4d7519035476dd155f1726fd545bfca0014d82d8a2922e38dba`. P1 and the one-candidate budget are consumed. The 256-scene public archive SHA-256 `30c7cd5151b3e8959e688593d6eaf67a86cfa379e7ed035e7801250b6b2f210c` remains unopened at zero evaluations. No rerun, tuning, manifest, model store, packaging, private validation, approval, or release eligibility is authorized. |
| OCR proposal confirmation calibrator V19 | FAIL closed, exhausted before public | V19 used only aggregate consumed V18, P1, and P2 metrics and froze fresh stored splits of 192 training, 128 visible validation, and 192 truth-hidden public scenes. P3 executed once from its committed source binding and retained all fixed P2 data, payloads, seed, negative-class weight, thresholds, optimizer, and 180 steps while changing only the first projection to the preregistered deterministic quadratic lift. At threshold `0.65` it passed 128/128 scenes with 1,024/1,024 truths, zero false regions, misses, duplicates, or prohibited hits, recognition exact `0.97265625`, CER `0.004033419763756842`, role accuracy `1.0`, and CPU ONNX parity `7.152557373046875e-07`. Thresholds `0.35` through `0.55` still retained one prohibited false region, and `0.75` missed three truths, so the mandatory three-consecutive-threshold zero-error window remained empty. P3 report SHA-256 is `fbbd30ce...6232`; rejected ONNX SHA-256 is `98c8dd2e...2a42`; checkpoint SHA-256 is `3338e380...125a`. P1 through P3 are consumed and cannot rerun. Public archive `85f3f9e5...b847` remains unopened at zero evaluations. No manifest, store, package, private validation, approval, or release eligibility exists. |
| OCR large-margin proposal calibrator V20 | FAIL closed, three-candidate budget exhausted | V20 froze fresh 576-train, 128-validation, and 192 truth-hidden public scene families. P1 completed 288 optimizer steps and passed CPU ONNX parity at `4.76837158203125e-7`, but the unchanged detector floor exposed only 823/1,024 validation truths. P2 performed zero optimizer steps over all 5,384 frozen validation proposals with the exact P1 model; its best threshold retained 1,020 truths but left four misses, four false prohibited regions, and only 68/128 exact scenes. P3 then consumed the final authorized candidate using a preregistered complete-stream quadratic multitask calibrator with 4,682 parameters and 1,480 optimizer steps. P3 passed CPU ONNX parity at `2.384185791015625e-6`, recognition exact `0.990234375`, CER `0.0014405071`, role accuracy `0.9970703125`, and minimum role accuracy `0.984375`. Its best threshold `0.65` passed 124/128 scenes with 1,021 true positives, three misses, two false prohibited regions, and zero duplicates; no required three-threshold zero-error window existed. P1 through P3 are consumed. P3 report SHA-256 is `be94ae54...ebdeb6`; rejected ONNX SHA-256 is `431c7677...c9b5f`; checkpoint SHA-256 is `edccbb02...a488e`. The public archive SHA-256 `e15bfbec...565` remains unopened with zero evaluations. No rerun, public gate, manifest, model store, package, private validation, approval, or release eligibility is authorized. |
| V8 C# production-composition alignment | PASS for visible validation only; production remains blocked | Exact source commit `58e2576b17df682d5ce55a1eda1d55ca19365e45` executed the checksum-bound V10 spaced detector, official English recognizer, numeric V5 recognizer, and ambiguity classifier through the production C# CPU session factory on 128 stored visible synthetic scenes. It passed 128/128 scenes and 640/640 regions with zero false positives, misses, duplicates, prohibited hits, or role confusions. Recognition exact was `0.9953125`, CER `0.000873871249635887`, role accuracy `1.0`, numeric exact `1.0`, word exact `0.9917355371900827`, and ambiguity exact `1.0`. Report SHA-256 is `24e02fc68be0c4338b8d5b10061418308a7a4b2f6b765fd713be096da788b3e0`; every model call records direct input/output tensor hashes and CPU provider identity. This is not a fresh truth-hidden public gate, does not execute the marker stage, and sets `production_approval=false` and `release_eligible=false`. |
| Selected-confidence OCR P2 | FAIL closed, single eight-role public run consumed | The five-role selection passed 128/128 fresh scenes and 640/640 truths, but the separately frozen eight-role public run at source commit `4632ec40aabacfbecc87bf80ec5496cc5d80fa66` passed only 87/160 exact scenes with 1,206/1,280 truths, one false and prohibited region, 74 misses, and zero duplicates. Recognition exact was `0.85625`, CER `0.02385599653003687`, role accuracy `0.6921875`, numeric exact `1.0`, word exact `0.8781838316722038`, and ambiguity exact `1.0`; PhaseHeading and Participant role accuracy were both `0.0`. Direct CPU runtime evidence passed with input and output hashes for every call across all four payloads. Public report SHA-256 is `9a8d715f86675c66c9f02a5a44289533751f25f7d007d93743b088a2c039efbf`; tracked result SHA-256 is `11d448e69b5aa972139d518d8e920e3eeeec78ca7d66d2b0a9b0f7fe35c7be78`. No case-level output is tracked or authorized for tuning. The public run is consumed and cannot rerun or be repaired against these fixtures. Marker evidence, artifact-mask approval, manifest, model store, packaging, private validation, production approval, and release eligibility remain absent. |
| Cross-model consensus OCR P3 | FAIL closed, final candidate consumed before public | The single authorized selection run executed all five checksum-bound payloads on CPU from source commit `872d03409a6f1395620a557a1daebc56921319e7`. It passed 102/192 scenes exactly with 1,445/1,536 truths, five false and prohibited regions, 91 misses, and zero duplicates. Recognition exact was `0.85546875`, CER `0.02369968340117594`, role accuracy `0.494140625`, numeric exact `1.0`, word exact `0.8790397045244691`, and ambiguity exact `1.0`; YTick and XTick roles passed, while AxisTitle, PhaseHeading, LegendText, Participant, Annotation, and Other failed the fixed per-role threshold. Direct CPU runtime evidence passed with per-call input and output tensor hashes for all five payloads. Report SHA-256 is `010e135e5f321b9350002cd12bbbd1aebb5c830a3f72aea5612f9849fafc1a2c`; tracked result SHA-256 is `f7935a64de07f1187a4ca854c01a90b4fcd004c533f57c6f464d98c5e59105e2`. P3 and the selected-confidence defect-class budget are exhausted. The separately frozen public archive remains unopened with zero evaluations and cannot be authorized. No rerun, repair, case-level tuning, marker evidence, artifact-mask approval, manifest, model store, package, private validation, production approval, or release eligibility exists. |
| Relational scene OCR V21 P1/P2/P3 | FAIL closed, three-candidate budget exhausted before public gate | P1 consumed 1,536 optimizer steps and passed 107/128 exact scenes with 1,004/1,024 truths, one prohibited false region, 20 misses, role accuracy `0.9775390625`, and parity `0.000011444091796875`. P2 continued the exact P1 checkpoint for 384 steps and improved to 109/128 exact scenes with 1,006/1,024 truths, but retained one prohibited false region, 18 misses, and parity `0.000010251998901367188`. Final P3 reused the exact P2 checkpoint with zero optimizer steps, scaled logits by `0.5`, and used the fixed geometric mean of proposal and role confidence. P3 passed parity at `0.00000512599945068359` but only 116/128 scenes at threshold `0.45`, retaining 1,014/1,024 truths with two prohibited false regions, ten misses, zero duplicates, role accuracy `0.98828125`, and every role above `0.94`. P3 report, rejected ONNX, inherited checkpoint, and tracked result SHA-256 values are `6eed9f77...8a8b`, `3435b88c...e6cb`, `d4692783...4035`, and `8aa11cc5...1bac`. | P1 through P3 cannot rerun. The sealed-public archive `b4ae7547...dab8` remains unopened with zero evaluations. No public authorization, marker composition, artifact-mask approval, manifest, model-store promotion, package, private validation, production approval, or release eligibility exists. |
| Scene-evidence attention OCR V22 P1/P2/P3 | FAIL closed, three-candidate budget exhausted before public gate | P1 and P2 each trained from scratch on the same frozen 256-scene synthetic training split for exactly 1,280 optimizer steps. Final P3 continued the exact P2 checkpoint for three lower-rate proposal-only epochs, then froze proposal behavior while repairing only the role head for two epochs, completing the fixed total of 1,280 steps. P3 passed CPU ONNX parity at `7.450580596923828e-06`. At thresholds `0.55`, `0.65`, and `0.75`, it removed every false proposal, duplicate, and prohibited-structure hit, but retained one missed truth and passed only 107/128 scenes exactly with 1,023/1,024 truths. Recognition exact was `0.978515625`, CER was `0.0038148946757339524`, overall role accuracy was `0.9765625`, and Annotation accuracy was `0.9296875`. P3 report, rejected ONNX, checkpoint, and tracked result SHA-256 values are `490bd32d57d55a56b412f30133e58a7f93633f6aadfd1d4f3dd5d91bba004198`, `7bd88232bdb4b78efa4caec530153efd38363f8da3208dcbf355ad9a312f4988`, `fa1a54c89720618b2b340fd8634f202ef61ddfcaf056b7af9af9f53b6e398562`, and `cd8903db6ec7c9e46e2e49938b6552bf09521e53aa277d01f514f460f566afe9`. P1 through P3 are consumed and cannot rerun. The 192-scene truth-hidden public archive remains unopened with zero evaluations. No public gate, marker composition, manifest, model-store entry, package payload, private validation, production approval, or release eligibility exists. |
| Role-anchor set OCR V23 | BLOCKED, three-candidate budget exhausted at selection | V23 uses a distinct project-owned role-anchor architecture and fresh checksum-bound 256-train, 128-selection, and 192 truth-hidden public splits. P1 retained all 1,024 truths but left three false and prohibited regions. P2 reduced those to two but regressed to 23 misses and role accuracy `0.87890625`. Final P3 loaded the exact P1 checkpoint and trained only four proposal-head tensors for 512 steps. Frozen parameter hashes and direct comparison prove every P1 teacher role logit remained unchanged; CPU parity passed at `9.5367431640625e-06`. At thresholds `0.35` and `0.45`, P3 retained all 1,024 truths with zero misses and duplicates, recognition exact `0.96875`, CER `0.005302402651201326`, role accuracy `0.99609375`, and lowest role accuracy `0.9765625`, but the same three false and prohibited regions remained. No zero-error three-threshold window existed. P1, P2, and P3 cannot rerun. The public archive remains unopened, and marker, manifest, model-store, package, private, approval, and release execution remain unauthorized. |
| Crop-evidence role-anchor OCR V24 | FAIL closed, three-candidate budget exhausted before public gate | Final P3 executed once from its committed aggregate-only preregistration. It reused the exact P2 weights with zero optimizer steps, required parent probability at least `0.35` and P2-minus-parent residual margin at least `-0.25`, preserved every P2 role output, and passed CPU parity at `9.5367431640625e-06`. At all fixed thresholds it retained 1,013/1,024 truths with zero false regions, duplicates, or prohibited hits, recognition exact `0.9609375`, CER `0.013242840589306406`, role accuracy `0.984375`, and lowest role accuracy `0.921875` for Other. Eleven misses left 114/128 exact scenes and no three-threshold zero-error window. Report, rejected ONNX, and tracked result SHA-256 values are `36f44860...2556`, `f97f8c7d...cacc`, and `42a0da28...8766`. P1 through P3 cannot rerun. The 192-scene public archive remains unopened with zero evaluations. No public run, marker run, manifest, store entry, package payload, private validation, approval, or release evidence exists. |
| Evidence-rescue OCR V25 | FAIL closed, three-candidate budget exhausted before public | P1 and P2 failed the fixed zero-error selection gate. Final P3 used only their aggregate metrics, froze exact P1 proposal and role behavior, and trained only its preregistered two-scale spatial residual for five epochs and exactly 1,280 steps. A first command was rejected before any seal, output, optimizer step, or selection evaluation because the ledger retained P2 in the current preregistration list; direct record SHA-256 is `7409803c...9b4c`. The corrected consumed run used config `a75fa413...016b` and runner `5f025e00...0d46`. At every fixed threshold it retained 1,017/1,024 truths with one false prohibited region, seven misses, zero duplicates, and 112/128 exact scenes. Recognition exact was `0.96484375`, CER `0.010632995514205018`, role accuracy `0.9833984375`, exact parent-role preservation passed, and CPU ONNX parity passed at `9.5367431640625e-06`, but no threshold or required window passed. P3 report, rejected ONNX, checkpoint, and tracked-result hashes are `cca90751...72e6b`, `a0f2fdcf...bdd59`, `1f077e2d...3ef32`, and `010034ad...9bca3`. P1 through P3 are consumed and cannot rerun. Public archive `bab5eaa6...c40` remains unauthorized, unopened, and at zero evaluations. No marker-composition, manifest, store, package, private-validation, approval, or release evidence exists. |
| Scene-topology proposal OCR V26 | FAIL closed, exhausted before public | P1 retained all 1,024 truths but left five prohibited false regions and failed CPU ONNX parity. P2 reduced the residual error to one prohibited false region while preserving all truths, but no required three-threshold zero-error window existed and parity still exceeded `1e-5`. P3 then executed exactly once from its committed aggregate-only preregistration. It loaded the exact P2 checkpoint, froze every parameter except `proposal_head.5`, trained exactly 1,152 steps, and preserved the frozen parameter stream and parent role output exactly. At thresholds `0.65` and `0.75`, P3 retained all 1,024 truths with zero misses or duplicates but still left one prohibited false region, so only 122/128 scenes were exact and no passing threshold window existed. Recognition exact was `0.97265625`, CER `0.004640371229698376`, and role accuracy `0.9951171875`. CPU ONNX Runtime with `ORT_ENABLE_BASIC` still measured `1.1444091796875e-05`, above the unchanged strict parity gate. P3 aggregate result, ignored report, checkpoint, and ONNX hashes are `1b5f253c...7696f`, `185e2d68...7464`, `fcf88061...b6ae`, and `9f4aa807...fe17`. P1, P2, and P3 are consumed, so V26 cannot rerun or tune. The `84e00f59...1981` public archive remains unauthorized, unopened, and at zero evaluations. No manifest, store entry, package, marker composition, private validation, approval, or release evidence exists. |
| Marker-center mask-consensus V8 | FAIL closed, three-candidate budget exhausted before public | P1 and P2 each passed only 122/128 validation scenes, with six false positives and 23 misses. Final P3 acquired its committed one-time authorization and failed before training in the exact P2 per-channel parity-reproduction preflight. Center parity reproduced as `0.000003874301910400391` instead of `0.0000033080577850341797`; radius and artifact matched exactly, and zero optimizer steps ran. Candidate report, tracked result, opened seal, and result seal SHA-256 values are `a40cbdee...9f9b`, `fd2dfa1a...04f8`, `ca153280...47b4`, and `e676945f...c91d`. P1 through P3 are consumed. The public archive remains unopened with zero evaluations. No manifest, store, package, private Chandler validation, production approval, or release is authorized. |
| Marker-center mask-consensus V9 | FAIL closed for production, three-candidate budget exhausted before public | V9 isolates the V8 runner-reproducibility defect without weakening the independent `1e-5` model parity gate. Its predecessor diagnostic allows only `1e-6` per-channel aggregate reproduction drift. Fresh 512/128/160 train, validation, and truth-hidden public families remain fixed. Final P3 consumed its committed one-time authorization and completed 768 optimizer steps. Predecessor reproduction passed exactly and CPU ONNX parity passed at `5.841255187988281e-6`, but fixed selection failed at 121/128 exact scenes with 12 false positives, 18 misses, zero duplicates or prohibited hits, artifact precision `0.7897035539741141`, artifact recall `0.980064776805665`, and no passing threshold window. Report, rejected ONNX, and tracked result SHA-256 values are `9f36255b...42ad`, `a4a2445a...d25e`, and `542c6093...6f79`. P1 through P3 are consumed. The public archive remains unopened with zero evaluations; no manifest, store, package, private Chandler validation, approval, or release is authorized. |
| Central version | `0.4.33` | `Directory.Build.props`; prepared successor for build 433, while the historical portable ledger ends at corrected build 432 (`0.4.32`) |
| Packaging regression | PASS, 62/62 | Canonical model paths, complete indexed-tree verification, reparse/traversal rejection, recursive duplicate-JSON rejection, canonical UTC release metadata, rejected research-manifest exclusion, explicit required production-task enforcement, mandatory direct-evidence gates, exact-binary rejection, and required reviewed OpenCV and PDFium common-publish inputs |
| Session 17 localization regression | PASS, 9/9 | Audit-tool behavior; repository audit has 258 keys with zero missing, extra, duplicate, or unresolved references |
| Current release audit | FAIL, 16 substantive clean-tree blockers, 7 manifests, and 1 eligible model file | Direct clean-tree audit at commit `589e8892718feaa011849132313c1eb6e71f534e` reports `ReleaseReady=False` and `ArtifactsEmitted=False`. Of 13 named mandatory evidence gates, exact OpenCV and PDFium clean-machine loading pass and the other 11 remain blocked. The offline default also requires exactly one checksum-verified production-approved payload for OCR detection, OCR recognition, marker center, and marker classification. Only the classifier satisfies its task, leaving three model blockers. Rejected and preregistered research candidates remain audited without becoming mandatory release payloads. A dirty integration tree adds only the separate dirty-tree blocker. |
| Real-ESRGAN local integration | PASS for checksum-bound developer WPF evaluation; production FAIL | The exact authorized-vcomp anime x2 runtime now resolves only as `AvailableForLocalEvaluationOnly` after verifying the manifest, notice, executable, OpenMP dependency, and both model payloads. A direct public case passed exact 2x dimensions and immutable-source checks. The real Chandler WPF manual workflow then produced a 1726 by 790 comparison derivative in 732.9203 ms with output SHA-256 `954090b7...9d1`; Chandler remained SHA-256 `65bd6e10...42e1`. Evidence report SHA-256 is `b317fb4a...9d52`. The UI exposes this only as Experimental developer evaluation. Runtime redistribution, scientific quality, peak memory, clean-machine, bundling, production, and release approvals remain false. The required offline CPU workflow continues with enhancement disabled. |
| Marker production gates | FAIL closed overall; classifier production-approved and adapter-composed | Repair-v2 marker-center `P1`, `P2`, and `P3` are consumed. P3 passed selection and CPU ONNX parity, then its once-only public `public-stair` scene produced one false-positive center. That dense detector budget is exhausted. A distinct candidate-level defect class froze 33 train/validation scenes and a truth-hidden 16-scene public split with disjoint renderer and degradation families. Candidate-level P1 failed 0/9 exact scenes and ONNX export. P2 ONNX SHA-256 `a6a7249d3993e08b83ec4e4bb7b0d7f8a09a14a233e166920610f14f97f80be8` passed CPU parity at `1.1920928955078125e-6` but only 8/9 validation scenes at threshold `0.7`. Final P3 reused those exact bytes with zero optimizer steps, passed 9/9 validation scenes at threshold `0.2`, and then opened the once-only public gate. It passed only 11/16 scenes exactly with 122 true positives, one false positive, two false negatives, zero duplicates, and two prohibited divider hits. Gate report SHA-256 is `02490587ef0bffccd5ca636fcd9e2d69f8ee55f9943fd22100065499755f321f`. All three candidate slots and the public-gate budget are exhausted. The subsequent line-aware defect class also consumed P1 through P3. Its final P3 reused exact P2 ONNX SHA-256 `25d79f6f207f35180d7482e147a1fb1fc099dcd15ab098f109cfb3242c7ff144`, passed all 9 selection scenes and 63/63 markers with zero false positives, misses, duplicates, or prohibited hits at threshold `0.07`, and passed CPU parity at `8.270144462585449e-07`. Its single authorized public-gate attempt failed closed before opening a seal because the frozen configuration omitted the canonical gate's required `expected_*` bindings. No archive load, metric, output, opened seal, or result seal exists. Attempt record SHA-256 is `fff7b6dc33c76bcc8a5257aac0fa629697947030ec2d048609b0ebf9c5394ce5`; the attempt is consumed and cannot be repaired or rerun. The radial-feature defect class replaced learned convolutional features with 36 fixed radial/topology projections plus a small MLP, froze new disjoint renderer/degradation families, and bound a fresh 16-scene public archive through all four canonical `expected_*` seal fields before optimizer execution. Its P1 ONNX SHA-256 `924c555e2f27955c644143125d7abd3b05859ea9928ab9d1e741e0544fa19e8b` passed CPU parity at `1.9073486328125e-06` and retained 61/63 validation markers with zero false positives, duplicates, or prohibited hits, but only 7/9 scenes were exact because two high-confidence proposals had center offsets rejected by the frozen geometry-consensus filter. Report SHA-256 is `ed2a93fcf04851411b8d8d8b49deaaf3c82afa948b582a43d4a7f88ac06d5a10`; P1 is consumed and the public archive stayed closed. P2 increased only the positive center-offset loss weight from `1.5` to `3.0`, produced ONNX SHA-256 `64f4c23795ab9e953a2e8aeba1887f8c99785f02385f453c9a46e6885578ff58` with CPU parity error `9.5367431640625e-07`, but worsened selection to 5/9 exact scenes and 59/63 markers at threshold `0.15`. It still had zero false positives, duplicates, or prohibited hits; report SHA-256 is `6d9398092fee307aa4f59e2a423d386e5e0be0923e3a36e2efa2d75a6201029b`, P2 is consumed, and the public archive stayed closed. Final P3 reused the exact best P1 model bytes with zero optimizer steps and changed only quantization-sensitive failed geometry rejection by searching the deterministic one-pixel neighborhood before unchanged nonmaximum suppression. It passed all 9 selection scenes and 63/63 markers at threshold `0.3` with zero false positives, misses, duplicates, or prohibited hits; CPU parity remained `1.9073486328125e-06`. Report SHA-256 is `67b5ea3b28973f0bd24ae0f755713af1c70b6fe6a9b2437268be5975b9f14af3`. P3 is selected and consumed. Its single authorized public evaluation is now consumed and failed: 11/16 scenes exact, 109/116 markers retained, seven false negatives, zero false positives, zero duplicates, and zero prohibited-structure hits at threshold `0.3`. The tracked evaluator output SHA-256 is `e343bb72cf098c9a7598d91a0264288eadd7401967f7197f5d5fe6249be58996`. All three candidates and the public-gate budget are exhausted, so this revision cannot rerun or approve a production marker-center payload. Classifier runtime-repair P1 retained the exact weights with zero optimizer steps. Its once-only 140-case public-v3 and disjoint confirmation-v3 gates passed every fixed classification and `1e-5` parity gate. Exact payload SHA `26f9304f1689053a0b94aa896a1e239f6ade1e5c1920736a3535c1b32f803b8a` passed the C# probability decoder on CPU and DirectML with maximum provider difference `3.5762786865234375e-07`. The checksum-bound payload, Apache-2.0 notice, benchmark evidence, and canonical package index pass `ProductionModelStore` resolution. All marker-center payloads remain absent from the production store, so no complete marker stage or application release is approved. |
| Marker-center runtime consistency V2 | FAIL closed, P2 public gate consumed | Direct source review found that radial-feature P3 selection imported `radial-local-consensus-refinement-v1`, while its consumed public evaluator imported the older line-aware postprocessor. Zero-training P1 passed CPU ONNX parity but failed 4/12 visible scenes and was consumed without opening public data. One bounded visible-selection sweep, diagnosis SHA-256 `9163904d698e38ca360bc9b7714636e7cbda54c6ce1202f0d07a991fee86a3d1`, preregistered P2 with exact ONNX SHA-256 `924c555e2f27955c644143125d7abd3b05859ea9928ab9d1e741e0544fa19e8b`, threshold `0.25`, and minimum duplicate separation `6.5`. P2 passed 12/12 selection scenes and 96/96 markers with zero false positives, misses, duplicates, or prohibited hits, plus CPU parity at `2.384185791015625e-06`. Its single truth-hidden public evaluation then failed: 13/20 scenes exact, 164 true positives, three false positives, four false negatives, zero duplicates, and zero prohibited-structure hits. Public report SHA-256 is `9013f187982c6f8e492d6cfbbbd28214f116f21e268ca35ad07526ca014ba5dd`. The archive is exposed, P2 cannot rerun, and P3 is retired under this revision rather than tuned against public results. Production approval, model-store discovery, packaging, and release eligibility remain false. |
| Marker-center background invariance V3 | FAIL closed, P1 consumed before public gate | The failed runtime-consistency result was used only as trigger evidence and no exposed fixture bytes were reused. The only zero-training candidate retained exact ONNX SHA-256 `924c555e2f27955c644143125d7abd3b05859ea9928ab9d1e741e0544fa19e8b`, threshold `0.25`, minimum separation `6.5`, masks, proposal eligibility, geometry refinement, duplicate suppression, and every gate. Its sole change subtracted the deterministic lower-middle median from the ink channel of each 33 by 33 proposal patch. CPU parity passed at `1.430511474609375e-06`, but selection passed only 8/16 scenes with 125 true positives, five false positives, three false negatives, three duplicates, and zero prohibited hits. Candidate report SHA-256 is `8f0116e137300b211ec0adab80907106a776b3ea1e5f2c17018316826372dbc9`; result SHA-256 is `c37aad98d7cc404662e0478acb19b1cf50a3d450603f3d8631cab206cadb0fcc`; final protocol SHA-256 is `286838ee982386e317874c0aa8c3fe3ec28274c27ab14e4dae76a441178ca806`. The truth-hidden public gate did not execute and remains at zero evaluations. P1 cannot rerun or be tuned, manifested, stored, packaged, or approved. |
| Marker-center normalized training V4 | FAIL closed, synthetic and exact C# candidate-runtime gates passed but production blocked | P1 ran once from committed source and trained new radial-topology MLP weights on the exact median-normalized patch contract. It passed 16/16 visible selection scenes with 128/128 markers and CPU ONNX parity at `1.46031379699707e-06`. The single truth-hidden public gate then passed 20/20 scenes with 168/168 markers and zero false positives, misses, duplicates, or prohibited hits. ONNX SHA-256 is `017fca04fa3817596ce3088d73f51003dd3658bc56ec3130e25c92252e6bf739`; candidate report SHA-256 is `7bdcb834a0b3f51dc748c32d6fb713461b0d213719e2b1768997332e705648e8`; public report SHA-256 is `f8c22631103825eb6fb2e09b2e209304951a10d62adefb5a10b2d823667051e1`; tracked result SHA-256 is `7f203ae5a7182c76a7d5210334695a0742634d97f2d0e9e5bd80a296c00d9ef3`; final protocol SHA-256 is `0a149ad42e923990c45b7a876852d30225c8b075cd6d149176cb3da72a332f34`. The isolated C# preprocessor reproduces the frozen PyTorch proposal coordinates and exact float32 tensor SHA-256 `93a4523015551a2be1340874f870073964f1dc2bc5338f25e40313865d663711`; source SHA-256 is `716cfab8ca31a47dab059160a4888113b3fab137ed33440596aed8c3f9c9eb38`. A separate fail-closed CPU candidate service now binds that tensor to the exact `[N,4]` probability, offset, and radius output plus the frozen threshold, mask rejection, deterministic one-pixel geometry refinement, and radius-aware suppression. Service source SHA-256 is `689c945e72e8ed6a1df0e5bc383469f372486a28aed00a69cdd8828ef6563b4b`; test source SHA-256 is `e2cc1f3cda0ec7706580876ffa370cfe2039eddce5320450322f3da219176b8f`. All seven focused tests passed. The exact ignored ONNX executed through the shared bounded C# CPU runtime in `332.350` ms with cache key `66c0f3ba114a2f302296bf0fd69c38bb25300dfe11a9a1b74c14db6775e5bea4`; the deliberately simple probe accepted zero markers, so this proves exact payload execution but not accuracy. The full marker suite passes 94 tests with two expected opt-in skips when the exact payload path is absent. The service remains deliberately separate from ordinary Auto Detect. P1 cannot rerun and P2/P3 are retired. Production approval remains false because approved checksum-bound text and artifact masks, manifest and production-model-store discovery, packaging, private Chandler zero-error validation, and clean-machine CPU evidence are absent. |
| Marker-center dense production contract V5 | FAIL closed, P1/P2/P3 exhausted before public gate | P1 failed with 7/24 exact scenes, 21 misses, six prohibited hits, and parity `0.000031888484954833984`. P2 and P3 each retained 200/216 markers and only 8/24 exact scenes, with 16 misses and seven prohibited hits. P3 consumed 1,152 fresh steps and passed CPU ONNX parity at `0.000009506940841674805`, but checkpoint-to-fused-graph semantic error `0.000011861324310302734` exceeded `0.00001`. Its report, ONNX, and result SHA-256 values are `a446d3c7918c88c7100006c71fb398e6ad6a81d4daddb411b47e7077412e0952`, `48d1c9c5f34842faebc8c06bfd2708264bae533b5bec097b079b0e8ab733782a`, and `fd7a8c761e8fd6d0bfe185038f0738ef0caaafc67732b6b901f466aa41ce6ba9`. Aggregate-only post-exhaustion diagnosis found seven truth centers inside the prohibited radius, 18 overlaps between truth-matching and prohibited acceptance regions across 14/24 scenes, and eight prohibited hard points cleared from artifact-mask truth. Diagnosis SHA-256 is `e2639d78b6941cf992e0edb50b2587369d454556b6380cefc45b81ef088f070b`; no case identities or pixels were emitted. V5 cannot rerun. Its public archive remains locked, unopened, and at zero evaluations. A new defect class must freeze mutually disjoint targets before training; no manifest, store, package, private validation, production approval, or release eligibility exists. |
| Direct C# OCR-marker composition V1 | FAIL closed, one-time gate consumed before report | A new 40-scene synthetic archive SHA-256 `c114bcc6b93c0b71e6b33cf13a0cd11f86afc9d54d2e5da58f7afb438ce25660` bound direct fixture bytes, the four exact OCR payloads, normalized marker ONNX `017fca04...1739`, CPU execution, and per-call tensor hashes. Its only authorized run opened seal key `fbcd84e6aa8b74fdce2f7ce191e0c336b2b442b3b716afad9706d73d119a84a5` from commit `f2a9fc507d3869227c55f0f7e3b6b0df500ca77b`, then failed on the first marker call because the C# decoder rejected activation-boundary drift within the model's frozen `1e-5` parity allowance. Opened-seal SHA-256 is `379425b78fb3a7c1ccf6ce264e29aca5b81b60b744fa49b09eb057d37adb4b23`; result-seal SHA-256 is `56fc0ee34ad05119012df5886955a52e7a400d7c7cb5227af00d1c03dbc639b6`; no gate report exists. The runtime now clamps only finite drift inside that existing tolerance and continues to reject larger or non-finite output, with focused and exact-payload CPU tests passing. The exposed archive cannot rerun or tune. A fresh preregistration, disjoint split, source commit, and one-run seal are required. Production approval, manifests, model store, packaging, private Chandler, and release eligibility remain false. |
| Direct C# OCR-marker composition V2 | FAIL closed, one-time gate consumed with direct report | The fresh disjoint 40-scene archive SHA-256 `f7873bc6fca1bd42c216b94d89d267c843ec2133375bd41f13ab4b887538f301` was sealed and committed before inference. The only authorized run executed all five exact payloads from commit `89cda2e789f329a68f906ceafaa8d93008458f07` through CPU and recorded every input and output tensor hash. OCR retained 180/200 regions, produced 119 false regions and 20 misses, had zero duplicates, no exact scenes, recognition exact `0.845`, CER `0.01195219123505976`, and role accuracy `0.64`. Markers retained 310/328 truths with 19 false positives, 18 misses, zero duplicates, 14/40 exact scenes, and 18 text-origin marker creations. Report SHA-256 is `9875d0e9f82fb2cae2fecde8a1b38653286ee5a7eff92c5c851b85763525d670`; canonical seal key is `e7120ab3916e84c0f44addad26fdd790a99682180dd8403c5da0afbad91530e1`; opened/result seal SHA-256 values are `cef6995b9a6bbd591d5866db98a92fa94f2105ecc1ccbe8408bd1d56664292ef` and `8e79bf42de3c76ddafb0a5b059b6ef524ad417a6a5a06c74241d93304d3cdc63`. The result is consumed. No manifest, store, package, private Chandler run, production approval, or release eligibility exists. |
| Numeric OCR production gate | PASS for strengthened fail-closed adapter, exact BGR runtime boundaries, DB postprocessing, and frozen structure consensus; official and project model gates FAIL closed | The production gate now binds the frozen protocol, metrics evaluator, workflow, exact fixture ZIP bytes, split records, direct detector and recognizer tensor identities, core and final predictions, CPU runtime provenance, and independently composed marker-stage results. It decodes every embedded PNG, rehashes source BGR, applies frozen axis/tick/divider mask geometry, rehashes the exact masked detector BGR, and rederives one-to-one model/candidate consensus, truth matches, duplicates, exclusion hits, and aggregate metrics. The raster boundary retains immutable Gray8 graph-stage pixels plus a separately checksum-bound BGR24 OCR plane. Detector masking whitens all three BGR channels while recognition crops retain the unmasked original BGR pixels. Detector and recognizer tensor builders sample B, G, and R independently, include color mode in cache identity, and reject missing, RGB, or malformed production channel-order evidence before provider execution. PP-OCR resize uses aspect-preserving height scaling, half-pixel bilinear sampling, and neutral right padding. CTC decoding preserves official probabilities and counts Unicode scalars. The detector now has an explicitly selected `db_postprocess_v1` implementation using OpenCV contour extraction, default fast mini-box scoring, minimum-area rotated boxes, geometric `area * ratio / perimeter` unclip, original-coordinate quadrilaterals, deterministic raw-contour caps and IDs, and cancellation checks. Its `resize_long` path pads sources whose height-plus-width is below 64, scales the long side to 960, truncates before 128-stride ceiling alignment, and retains fractional original coordinates after transform inversion. Production manifest binding requires the reviewed 960-pixel, 128-stride, BGR, `score_mode=fast`, 0.30 threshold, 0.60 box threshold, 1.5 unclip, three-pixel minimum, and 1000-candidate values. The legacy once-only official evaluation froze 100 validation text, 100 sealed text, and 20 exclusion fixtures before inference. Split SHA-256 is `1fc3b2e72f89cbfb0d8854ec8701368e7ae764cbd5c6fef17b7e497d06ec9f09`; fixture ZIP SHA-256 is `69eeeff73f4cfd2dd6580ad9538f1a89527f8e5b320ce6a9cd7155d2bd22ea99`; report SHA-256 is `3fceb688d44a2f4c322e40bdfa3dd09ca74ec6d5742ab257ae102ccf76e1c44b`. That report remains `status=fail`, `production_approval=false`, and `release_eligible=false`: validation exact `0.0`, CER `1.0`, role `0.03`; sealed exact `0.0`, CER `1.0`, role `0.02`; detection exact `18/220`; 195 false text regions; false regions on 7/20 exclusions; no independent composed marker evidence. ONNX parity still passed at `2.205371856689453e-06`. The reviewed Apache-2.0 model license and ONNX change notice are tracked but not bound through a model store or package. No tracked OCR manifest exists. The distinct `graphreader-ocr-structure-consensus-public-gate-v1` evaluator SHA-256 `65de1c76288c2cd9646386afb941bf641a1a87c5abfec5d76b6cb0f7a818c992` and C# gate source SHA-256 `c55175ef6c32a5eaf76e99dea97351611daca953fa0cee354505b71804ea5093` were frozen with the new 500-case design and one-run budget before fixture generation or inference. The evaluator matches production vertical-glyph grouping, rejects every embedded resource above 8 MiB, and prohibits externally precomputed marker-result injection. The authoritative 200-text plus 50-exclusion cases per partition were frozen once at split SHA-256 `8685a3dfcb8212f612115c20d0f70437e0738fa1c4d86743cfd0e50bc5a41a8d` and fixture ZIP SHA-256 `6bd83d6b05918a6829a99e4cfecbb5af0e0629c458242398429856baae5e02b2`; post-freeze verification passed. The separate evaluation environment retained the locked conversion stack and added evaluation-only OpenCV wheel SHA-256 `afcf28bd1209dd58810d33defb622b325d3cbe49dcd7a43a902982c33e5fad05`. The single authorized CPU execution then failed closed on its first detector inference with `BLOCKED: Detector output is not a probability tensor.` before an output root or report was created. No new metrics, predictions, parity, marker result, manifest, store entry, or package evidence exists. The ignored failure record SHA-256 is `b14dd36632224254933fad9c826ab80298e25371a0e32040bcab66a4684fd4e0`. The one-run budget is consumed, the 500-case split is exposed, and this composition must not be rerun, repaired, or tuned. Earlier project candidate budgets remain exhausted. The separately preregistered component-geometric V4 used only procedural graph labels and the already reviewed Noto font families. P1 passed CPU parity but failed validation exact at `0.8515625`. P2 ran exactly once, passed parity at `1.049041748046875e-05`, and failed validation at exact `0.7734375`, CER `0.2087227414330218`, role `0.8828125`, and exclusion `0.9609375`; its report SHA-256 is `3d88cf89e4102834b6fd5a1cb46caf4c8713058a0a8a3665239071eb40a04859`. Both candidates are consumed, the sealed 384-sample public archive remains unopened, and final P3 is preregistered to preserve normalized shape plus four explicit source-geometry scalars. OCR remains unavailable and no release artifact is authorized. |
| PP-OCR dynamic recognition width | PASS for adapter contract only; model approval remains FAIL closed | The opt-in production path now mirrors pinned PaddleOCR commit `33cbdd9deb2e00f61e7966db70669b249c005a37` `tools/infer/predict_rec.py` SHA-256 `98c1e935b1878df7293670ae716e3054f10006271d7d751d038b358ad5ab8b63`: source crops sort by aspect ratio, each batch uses at least 320 pixels and expands to `int(48 * max_wh_ratio)`, and each crop uses `ceil(48 * ratio)` content width with neutral right padding. The production manifest must explicitly declare `[N,3,48,W]`, `paddle_batch_max_wh_ratio_v1`, minimum 320, maximum 4096, and dynamic output time. Any crop above the reviewed memory bound, mixed-width tensor batch, fixed time-step claim, or changed bound fails before provider execution. The exact reviewed ONNX SHA-256 `7839f12b644f574eaf677e92a11bd3e337f4b2f910160666073888783fece743` executed successfully on CPU at widths 320 and 321 through the new C# recognizer path. Existing fixed-width models retain their prior behavior. This closes an adapter mismatch only. No OCR payload, manifest, store entry, private validation, approval, package, or release is authorized. |
| V8 plus official-English plus V5 numeric OCR composition | FAIL, validation consumed and public unopened | The exact checksum-bound V8 detector, official English recognizer, and V5 numeric specialist executed once on all 64 fresh frozen validation graph scenes through `CPUExecutionProvider`. V8 retained 207/320 truth regions, missed 113, produced zero false regions, duplicates, or prohibited hits, and made only 3/64 scenes detection-exact. Misses were 56 legend labels, 25 x ticks, 21 y ticks, and 11 annotations. Recognition itself was exact for all 207 retained regions: word exact `1.0`, numeric exact `1.0`, and CER `0.0`. Report SHA-256 is `a3d851c043993bdc5546c68dfa26be837c88495600f7cbd214c55ee107ef330f`; detector input and output tensor-stream SHA-256 values are `b49c0b8f9f4ae2764bd2a5ae888c6b25d91e6ee891d1bdd19d58b0476271bcaf` and `d01ea50acc4632c15e508f018d84295287c0af8a2c70aaf65523d45ec840c8a9`. The single validation run is consumed, the new public-composition archive remains unopened, and production approval, release eligibility, manifest, model-store, packaging, marker-stage, private, and clean-machine evidence remain absent. |
| Component-recall detector V9 | Public synthetic detection gate passed; production approval absent | Zero-optimizer P3 passed all 80 selection scenes with 400/400 truths, zero false regions, misses, duplicates, or prohibited hits, plus CPU ONNX parity `5.7220458984375e-06`. Exact ONNX SHA-256 is `2d35ce2f55cee8317dfe1faf0281a6e87693cca485dbdaf39e4039ced5b97d9c`; selection report SHA-256 is `ffb2950fc727c77a2376bce57f71c4977cba204d24236ccc4934f6077e36bdc2`. Its only public run passed all 112 scenes with 560/560 truths, the same zero-error safety counts, and 112 direct CPU calls; report SHA-256 is `232eea4b01917bcbc786e289752b023f0d1ec9106df0349d2606dce06e76eeb9`. All candidates and the public split are consumed. Paired recognition, independent marker-stage, private, manifest, store, packaging, and clean-machine evidence remain blocked. |
| OCR production composition V2 | Validation failed; public remains unopened | The exact V9 detector, official spacing-P2 recognizer, and numeric V5 payloads ran once on the fresh 80-scene validation split. The run retained 399/400 truths, with one missed `O o l I` annotation, zero false regions, duplicates, or prohibited hits, and 79/80 exact scenes. Recognition over detected regions passed fixed thresholds at exact `0.955`, CER `0.009415262636273538`, role `0.9975`, numeric `0.96875`, word `0.9517543859649122`, and ambiguity `0.9090909090909091`. Report SHA-256 is `7a20ae70e9c970f2d10dd80f03a41ab363424cf1a33d98327e835727b587bed1`. Exact detection is mandatory, validation is consumed, the 112-scene public archive remains unopened, and production approval remains false. |
| Spaced component-recall detector V10 | Public synthetic detection gate passed; production approval absent | P2 fine-tuned for 648 optimizer steps and produced ONNX SHA-256 `474b8468dbd91416f4e4978dafc46cb2317775d59d821c0470e0cd3e0f6203db`, then failed closed before sealing metrics because its report gate referenced a missing evaluator field. P3 reused exact P2 model bytes with zero optimizer steps and passed all 80 validation scenes with 400/400 truths, zero false regions, misses, duplicates, or prohibited hits at threshold `0.95`; CPU parity passed at `5.7220458984375e-06`. Candidate report SHA-256 is `3755d9d0308fa9420dd3e0cff5c620fed96407ffdc34e4b59e5c651bf50c7455`. Its only public run then passed all 112 scenes with 560/560 truths and the same zero-error safety counts through 112 direct CPU calls; public report SHA-256 is `1c7526cb2147a7a68cdc6eb3d6bc3a073324b3b08f3c81c8b359832f038de7b1`. All candidates and the public gate are consumed. Paired recognition, independent marker-stage, private, model-store, provider, packaging, and clean-machine evidence remain mandatory; production approval remains false. |
| Composite proposal-role V11 | Public synthetic proposal-role gate and C# payload adapter passed; production approval absent | P2 reused the exact P1 checkpoint with zero optimizer steps and output scale `0.5`. It passed 96/96 validation scenes, 768/768 regions and roles, every role at `1.0`, all zero-error safety counts, and CPU parity `4.76837158203125e-06`. Its only public run passed 128/128 scenes and 1,024/1,024 regions and roles through 128 direct CPU calls, with zero false regions, misses, duplicates, or prohibited hits. ONNX SHA-256 is `af13b387140d70946b23ff7349fed82649fde95eb6f6cabe90179b2914a16631`; public report SHA-256 is `9259765bcb188804ba78a427699ad77910b41c17605515ed799d5cb9feb52840`. The candidate-only C# adapter matches the frozen Python `[N,2,32,144]` tensor bytes, validates `[N,10]`, propagates the fixed eight-role hints, rejects invalid output, and executed the exact payload on CPU. It is absent from ordinary Auto Detect. P1, P2, and the public gate are consumed. A fresh checksum-bound C# recognition and marker composition gate, provenance, notices, model-store, packaging, private validation, and clean-machine evidence remain mandatory; production approval remains false. |
| Direct C# OCR-marker composition V3 | FAIL closed, run and fixtures consumed | The single authorized CPU run executed all five checksum-bound payloads with per-call tensor hashes. OCR retained 373/384 truths with 95 false regions and 11 misses; recognition exact was `0.8880208333333334`, CER `0.02302158273381295`, role accuracy `0.5260416666666666`, numeric `1.0`, word `0.8819188191881919`, and ambiguity `1.0`. Marker composition retained 379/392 truths with eight false markers, 13 misses, zero duplicates, eight text-derived marker creations, and exact counts in 30/48 scenes. Report SHA-256 is `a5fc84a4a02c10f39abb156cb33895bb7b6a21c32a98137586b31b9058d7e9a4`; opened seal is `a09a3aa3...f5d7`; fail result seal is `ce9c1d30...c4a1`. No rerun, repair, manifest, store, package, private validation, approval, or release is authorized. |
| Direct C# OCR-marker composition V4 | FAIL closed, one authorized run consumed | Exact authorization commit `e70eb014b0436f22abd811a243b85a13cbd72f9e` executed the V8 C# OCR composition and normalized marker V4 once on 64 fresh sealed synthetic scenes. OCR found 320/320 truths but produced 192 false regions, zero exact scenes, no misses or duplicates; recognition exact was `0.99375`, CER `0.0011695906432748538`, role and numeric accuracy `1.0`, word exact `0.988950276243094`, and ambiguity exact `1.0`. Marker composition retained 465/522 truths with two false markers, 57 misses, zero duplicates, and 14/64 exact scenes. Text-derived marker creation and every prohibited-structure hit were zero. All five payloads record direct CPU tensor hashes. Report SHA-256 is `ac6dfc56095f1d010330fef604bae8e7e0f880763b64ad0d02eb923ff03cc0d3`; opened seal is `32b0f796...05b1`; fail result seal is `b8a7c767...fb18`. Full eight-role coverage, an approved artifact-mask provider, approved manifests and store, packaging and clean-machine execution, and private Chandler validation remain blocked. V4 cannot rerun or be tuned. |
| OCR production composition V3 | FAIL closed, validation consumed and public unopened | The exact V10 detector, official spacing-P2 recognizer, and numeric V5 recognizer executed once through `CPUExecutionProvider` on the fresh checksum-bound 80-scene validation split. Detection retained 396/400 truth regions across 76/80 exact scenes with four misses, zero false regions, zero duplicates, and zero prohibited hits. Recognition passed overall exact `0.96`, CER `0.00797373358348968`, role `0.9825`, numeric `1.0`, and word `0.9563318777292577`, but failed ambiguity exact at `0.8181818181818182` and changed three non-space truths. Detector, official-recognizer, and numeric-recognizer call counts were 80, 396, and 340. Report SHA-256 is `905bb12948ce7bdcdba95f4940e9b1b5f97017da6586c808ff5c43e128049ea9`; opened seal SHA-256 is `ab4760f505d725d95b09ba92fe0ed39d485854e43b4a1a698cb16fdd1679b587`; result seal SHA-256 is `bb1ded39bc90fabf01947ef2a031693f6e158667823c776a2a877571d35da4f4`. The validation gate is consumed and cannot rerun. The 112-scene public archive remains unopened, no manifest or model-store entry exists, and production approval and release eligibility remain false. |
| Graph text-region detector V1 | FAIL, exhausted before public gate | Final P3 ran once from the committed preregistration. It passed CPU parity at `2.68220901489258e-06`, the strict probability output contract, and all 24 graph-structure exclusion fixtures, but only 63/96 fixtures were exact. Text exactness was 39/72; 29 text fixtures had no matched region and ten false regions remained. Report SHA-256 is `aec6ab3ccffd0b175e5275eea282f843c8eaa3eed67c75fea8cd56a98754be80`; ONNX SHA-256 is `7c9450a01b9ec3f37d609bf1894f5763ef9a6ebccbe2916ea0450b96cc81fb1f`. P1 through P3 are consumed, the sealed public archive remains unopened, and no manifest, model-store entry, package payload, production approval, or release eligibility was created. |
| Graph text balanced-recall detector V2 | FAIL, exhausted before public gate | P1 and P2 passed probability and parity but failed selection at 52/96 and 56/96 exact respectively. Final P3 ran exactly once from committed source, retained the exact P2 model and constants, and changed only the hard-negative scope to empty-target exclusions. It passed probability and CPU parity at `4.649162292480469e-06`, but regressed to 30/96 exact with 82 false regions, 20 exclusion false regions, and 13 missed text fixtures. Report SHA-256 is `42a3c317e1b5dbaa516cd4740f09c67c9d45af191908dea5581d7f39202a4bbb`; ONNX SHA-256 is `239f62602487100bb70060952c87c81f3982b3d1bfa2ac9a58fb1d9e453bcec3`. P1 through P3 are consumed, the sealed archive remains unopened, and no manifest, store entry, package, production approval, or release eligibility exists. |
| Graph text ignore-band detector V3 | FAIL, exhausted before public gate | The distinct defect class froze new train, 112-case selection, and truth-hidden 152-case public families before any optimizer step. P1 passed probability and CPU parity but only 8/112 fixtures were exact, with 210 false regions and 36 exclusion false regions. P2 changed only to whole-frame tile composition, passed probability and parity at `4.708766937255859e-06`, and improved to 27/112 exact with 115 false regions and 15 exclusion false regions. Final P3 added a one-sided 0.25 probability ceiling margin only inside the ignored boundary band. It passed probability and CPU parity at `1.5497207641601562e-06`, improved to 92/112 exact, and removed every exclusion false positive, but left two text false regions, 19 text misses, and one multi-region text fixture. Report SHA-256 is `a89fda186be1443bff819aff2026baaecfd22956ef8480946534b553b2e53fbe`; ONNX SHA-256 is `912e3e58363079e505bdc52b56b51d984bfee8c5b5f75fbd4674fffb2355e141`. One broad diagnosis found failures across all four held-out structure and degradation groups. P1 through P3 are consumed, protocol SHA-256 remains `257f791a8e125aa81ea6d6096644f27582aeea7a7153300075a5853dc3a4c421`, and unopened archive SHA-256 remains `1b23558516d72b2241501cd24ac0019ace01ba117e725aa9ad030c5e78e59a95`. Public evaluation, manifest, model-store discovery, packaging, production approval, and release eligibility remain false. |
| Graph text stride-4 detector V4 | FAIL, exhausted before public gate | The disjoint 1,920-tile training, 136-case selection, and truth-hidden 176-case public sets remained frozen at protocol SHA-256 `8e205a7f6cfc2252294948cfb576b6045421f338580dc0532165cc97371b0bd0`, selection SHA-256 `2b839e9775082aa04eac6a4d34fcf9532b2013f7d107e5f1c18501e375886aeb`, and unopened public archive SHA-256 `b6498f2b37bfce66878c2fcba1da1764ec07323022e6d173f215d1ed039edd1b`. P1 passed probability, CPU parity, and all exclusions but only 101/136 fixtures. P2 changed only transpose-convolution upsampling to bilinear resize-convolution, passed parity at `9.5367431640625e-07`, and improved to 108/136 exact, but retained 24 text misses, 15 false regions, three multi-region text fixtures, and one exclusion false region. P3 added only a full-resolution detail branch and regressed to 82/136 exact, 50 text misses, 39 false regions, eight multi-region text fixtures, and one exclusion false region. P3 report SHA-256 is `e17abd79d869d0912c3b1537e4c84850fc2b4e921fb4b52f0e3afdc0ce443bdb`; ONNX SHA-256 is `88156abc6fcf4272598f39d3ec2ac98731d02887dd5fbda5103f67afef76432b`. P1 through P3 are consumed, V4 cannot rerun, and no public evaluation, manifest, model-store entry, package payload, production approval, or release eligibility exists. |
| Graph text DB-objective detector V5 | FAIL, exhausted before public gate | Final P3 ran once from committed preregistration and changed only P2's squared ceiling margin to explicit negative binary cross-entropy in the same ignored boundary. It passed the probability contract and CPU ONNX parity at `9.5367431640625e-07`, retained all 40 exclusions without a false region, and improved exact fixtures to 113/136. It still missed 22 text fixtures, produced three text false regions, and split one text fixture into multiple regions, so the fixed selection gate failed. Report SHA-256 is `7474e9c8033291d43e2303fb142c351023239f3a8722f96e2f2aa29613bc81d3`; rejected ONNX SHA-256 is `6e64510f923a57588f5703e740723d7561e59da46a541f5309d2bedd5e7ca362`. P1 through P3 are consumed, the truth-hidden public archive remains unopened at SHA-256 `3d19f21f0aad9b4da8ca7b1fc7610589f22ac20ab31873bd769c7e1c84478853`, and this revision cannot rerun. Public evaluation, manifest, production model-store discovery, packaging, production approval, and release eligibility remain false. |
| Official PP-OCRv5 recognition-only V1 | P1 exhausted, failed selection | The exact official ONNX SHA-256 `7839f12b644f574eaf677e92a11bd3e337f4b2f910160666073888783fece743` executed once on all 192 frozen direct crop bytes. It produced 190/192 exact matches (`0.9895833333333334`), CER `0.008086253369272238`, role accuracy `0.9895833333333334`, numeric exact `1.0`, word exact `0.9583333333333334`, and CPU conversion parity `2.205371856689453e-06`. Both frozen `O o l I` cases were decoded as `OolI` and assigned the wrong role, giving the mandatory ambiguity family `0.0`; selection therefore failed closed. Selection report SHA-256 is `31b334269c5d29fd5e13546f9f4cea0011323b75ebc6c5f2862885c69b178b54`; tracked result SHA-256 is `69966537744b51287ff1163c8bebf1b90e91a3b4a8548f048198ff1c2d9d9f3f`. The 256-case truth-hidden public archive SHA-256 `e285407ed007f1140eb000d5fca0b9223a13dda6632b111525b9d2d2f8fbee85` remains unopened with zero evaluations. P1 cannot rerun or be tuned, and no detector composition, marker-stage evidence, manifest, model-store entry, package payload, production approval, or release eligibility exists. |
| Official recognition image-spacing repair V2 | Public synthetic recognition gate passed; production approval absent | P2 passed all 224 selection crops with every exact-family metric `1.0`, CER `0.0`, and zero non-space-truth changes; selection report SHA-256 is `1dfe29e25ea42acfabc327e64e1d872f8e38f9ef1f7f2de22ebd48a2734e09f7`. Its single public run then passed 287/288 exact (`0.9965277777777778`), CER `0.000946073793755913`, role `0.9965277777777778`, numeric and word exact `1.0`, ambiguity exact `0.9722222222222222`, and zero non-space-truth changes through 288 direct CPU calls. Public report SHA-256 is `83afdcfd4fd3dd8b4fa8c0881b5f12d652c0d8c5f9b889af380b4fdf7b83091c`. The gate is consumed. Detector composition, independent marker-stage, private, C# runtime, manifest, store, packaging, and clean-machine evidence remain blocked. |
| Current public scoreboard | PASS, 37/37 in 196.913 ms, peak managed memory 987216 bytes | Synthetic metric-contract smoke only, including manifest contracts; not detector accuracy or application end-to-end evidence |
| Component-geometric OCR V4 current result | FAIL, exhausted before public gate | P3 passed CPU parity at `4.76837158203125e-06`, validation exact `0.9609375`, CER `0.04361370716510903`, and role `0.9713541666666666`, but one divider false acceptance left exclusion accuracy `0.9921875` below the mandatory `1.0`. Report SHA-256 is `807db11ffe147f9eadf025bede7403469d064161bb4ffd26349e857eee7588ca`. P1 through P3 are consumed and the sealed public archive remains unopened. This row supersedes the earlier P3-preregistered snapshot embedded in the long numeric OCR adapter history above. |
| OCR structural-filter repair | FAIL, public gate consumed | Separate revision `graph-numeric-component-structural-filter-v1` consumed its single zero-training P1 selection and the only authorized public evaluation. It retained exact P3 ONNX SHA-256 `941b19a8f700484a039335d25ecc47f41de1976b3b2016653c6beb2a4eb51894`, confidence threshold `0.55`, and the fixed pre-classifier component-height rule `>= 0.75`. Validation exact `0.9609375`, CER `0.04361370716510903`, role `0.9739583333333334`, and exclusion `1.0` passed, but the 384-case public run failed exact match at `0.87890625` and CER at `0.15327695560253699`. Public role `0.9192708333333334` and exclusion `1.0` passed. The direct CPU run made 370 inference calls; report SHA-256 is `a8cd15bf2a3228209ff24d7db77b458decd9e060bb5c15e2acfc35ab111363b4`. The exposed split cannot be rerun or tuned against. Production approval, model-store discovery, packaging, and release eligibility remain false. |
| OCR component-ensemble V5 | Public gate, bounded C# payload execution, and fail-closed factory contract PASS; production approval absent | P1 ran once from the committed `graph-numeric-component-ensemble-v5` preregistration and is consumed. Validation passed exact `0.96484375`, CER `0.04443282801881861`, role `0.9732142857142857`, exclusion `1.0`, and CPU parity error `9.298324584960938e-06` at threshold `0.65`. Its separately frozen 512-positive plus 160-exclusion public gate then ran exactly once and passed exact `0.994140625`, CER `0.005759162303664921`, role `0.9970238095238095`, and exclusion `1.0` with 654 checksum-bound CPU calls. Public report SHA-256 is `a50869bb75f13a39d4601b72e47f49b4b8e684bf4fbb992bd4342e11573e3a20`; ONNX SHA-256 is `9db95c41ce396e8b2dff3b525556615528a00ca87f4cc531274374b961417c84`. A dedicated C# recognizer binds the per-glyph tensor, component geometry, structural rejection, numeric grammar, provider, scheduling, cancellation, and cache contracts; the exact ignored ONNX executed through it on CPU in `5.9915` ms for a deterministic two-component probe. A separate factory now recognizes only `component_ensemble_numeric_v1`, validates the exact BGR DB detector and V5 component manifest values, executes both exact payloads with CPU-only cache-bypassed probes before composition, and leaves the source-sealed official-pair adapter unchanged. Its crop contract now also requires white source padding and a 0.25 oriented-vertical content margin, derived from the fixed 0.75 structure-rejection threshold so tight detector boxes do not become falsely tall glyphs. Crop pixels, resize mode, relative padding, padding value, and cache identity are tested, while normal PP-OCR retains its prior neutral-padding defaults. Six focused parser tests pass, including detector, recognizer, threshold, and composition-padding drift rejection. The public archive is exposed and cannot rerun or tune. No newly preregistered detector composition and component-runtime source seal, independent marker-stage run, manifest, model-store entry, package payload, private validation, production approval, or release eligibility exists. |
| Marker-center mask-consensus V8 | FAIL closed; P1, P2, and P3 consumed before public gate | P2 ran once with zero optimizer steps and retained 1,193/1,216 markers across 122/128 exact scenes, with six false positives, 23 misses, artifact precision `0.7872885671271697`, recall `0.9829926331356857`, and failed parity `1.621246337890625e-05`. Final P3 opened its one-time seal, then failed the exact P2 parity-reproduction preflight before training. Only center-channel parity drifted, from expected `0.0000033080577850341797` to observed `0.000003874301910400391`; radius and artifact reproduced exactly. Zero optimizer steps ran. P3 report/result/opened/result-seal hashes are `a40cbdee...9f9b`, `fd2dfa1a...04f8`, `ca153280...47b4`, and `e676945f...c91d`. No case detail or pixels were inspected. The sealed public archive remains unopened with zero evaluations; no manifest, store, package, private Chandler run, approval, or release is authorized. A new attempt requires a separately preregistered defect class and fresh splits. |
| Marker-center mask-consensus V9 | FAIL closed for production; P1 through P3 consumed before public | V9 uses the exact V8 P2 validation archive only for the predecessor-runner diagnostic and retains the strict `1e-5` ONNX parity gate for every new model. Fresh V9 train, validation, and sealed-public archives are `1430642d...dced`, `ba8de943...0eba`, and `b3867736...145e`. P1 failed before training with the legacy parity alias error. P2 completed 768 steps but failed selection and parity. Final P3 completed its fixed 768 steps, passed predecessor reproduction and CPU ONNX parity at `5.841255187988281e-6`, but failed selection with 121/128 exact scenes, 12 false positives, 18 misses, artifact precision `0.7897035539741141`, artifact recall `0.980064776805665`, and no passing threshold window. The public archive remains locked, unopened, and unauthorized. Manifest, model store, packaging, private Chandler, approval, and release gates remain blocked. |
| Marker-center decoupled heads V10 | FAIL closed; structurally infeasible and exhausted | P1 through P3 are consumed. Final P3 passed CPU parity but failed selection at 119/128 exact scenes, two false positives, 29 misses, one marker-artifact hit, artifact precision `0.7822080285166708`, and artifact recall `0.9665081800297624`. Aggregate feasibility SHA-256 `c0b580c6...b62f` proves the permanent seed union can reach at most `0.796366958474284` precision against the fixed `0.90` gate. No public evaluation, manifest, store, package, private Chandler validation, approval, or release is authorized. |
| Marker-center seed refinement V11 | FAIL closed, three-candidate budget exhausted before public gate | V11 froze new 512/128/160 synthetic train, validation, and truth-hidden public archives with SHA-256 values `2039ca41...058`, `d3f2cdbc...868`, and `046c5269...ca9`. P1 and P2 each failed at 122/128 exact scenes with artifact precision below `0.783`. Final P3 completed its one authorized 1,792-step CPU run and passed parity at `3.2782554626464844e-06`, but its weight-6.0 false-seed-retention loss collapsed the refined artifact mask at the fixed threshold. It removed 289,107 seed-positive pixels, added none, and produced artifact precision and recall `0.0`. Center selection regressed to 112/128 exact scenes with 1,178 true positives, two false positives, 38 misses, zero duplicates, and zero prohibited or marker-artifact hits. P3 report/checkpoint/ONNX/tracked-result hashes are `688b1b18...64f2`, `2c134d68...84e2`, `070ea4c7...4706`, and `4276a6ba...69ac`. P1 through P3 are consumed and cannot rerun. The public archive remains locked and unopened at zero evaluations. No manifest, store, package, private validation, approval, or release is allowed. |
| Production workflow integration | PASS for fail-closed foundation, complete approved-component composition boundary, verified asset discovery, immutable raster input, cancellation-safe checkpointed batches, PDF workspace tabs, and production evidence persistence; real execution FAIL | Real import, prepare, approved-adapter detection, review projection, correction replay, and export seams are composed. The orchestrator now emits a checkpoint only after a panel has completed detection and correction replay. Production composition validates and projects that panel atomically, merges it into the current automatic review, retains it when a later panel is cancelled, and normalizes a successful rerun back to the complete ordered result. Direct two-panel executable composition cancelled on panel two, retained exactly panel one plus unchanged source hashes, then completed both panels on rerun. The concrete production detection adapter executes immutable-original raster decode, axis geometry, two-model OCR, evidence-bound masks, marker center, shape/fill classification, grouping, legend and participant reasoning, phase reasoning, three-anchor session-first calibration, editable candidate projection, and exact export evidence. It rejects unapproved components and enhanced entry, gives artifacts no point rows, uses stable source-bound marker and point IDs, maps results to original pixels, and retains eight ordered stage envelopes when all stages complete, including separate checksum-bound artifact-mask provenance. PDF import now encodes deterministic axis-aligned panel crops instead of rejecting every non-full-page proposal, retains immutable crop bytes and the source-document checksum, and stores an invertible panel-pixel to PDF-page transform. A direct crop regression proves checksum stability, rerun identity, dimensions, and mapping. Production startup asynchronously checksum-validates every indexed model resource and the reviewed OpenCV runtime before setting stage status, and composes the full adapter only when every concrete dependency exists. OCR composition now routes approved CTC manifests to the original source-sealed factory and approved component-ensemble manifests to a separate exact-contract factory. Both routes require one approved detection and one approved recognition payload, rehash model-store resources, require paired direct evidence, and execute both payloads through the bounded shared CPU runtime before advertising OCR. The component route additionally rejects any drift in its exact glyph tensor, six geometry features, thresholds, grammar, or reviewed DB detector settings. It remains null when either task is absent or any evidence or execution check fails. A concrete lazy artifact-mask adapter consumes the existing marker-center three-head tensor without changing the frozen model task schema. It rehashes and parses the selected direct `marker-center-artifact-mask-public-gate-v1` report, requires the committed evaluator bytes at frozen SHA-256 `58c3cd9ec6bf2255c4b5d2cc9d865e2d37f58b0688a8bceda78ff523398a9f5f`, validates the public-synthetic dataset schema and seed, validates an inference-locked sealed split, binds dataset, evaluator, split, manifest, and report identities, and requires exact downstream counts in every sealed fixture with zero false positives, false negatives, duplicates, or text, axis, tick, divider, bracket, arrow-shaft, arrowhead, legend, and line-intersection hits. The current rejected marker-center manifest has no such passing profile, so ordinary composition still leaves the mask provider and complete detector null. The mask composer builds OCR and axis seed masks once, takes exclusive ownership of those private planes, preserves them through inference, and reuses them instead of cloning at every stage boundary. Missing real OCR approval, the rejected marker-center payload, artifact-mask provider approval, scanned-PDF clean-machine approval, and approved packaged end-to-end evidence keep the workflow and release blocked. |
| Current full Release solution | PASS, 907 passed and 25 expected skips | All skips are expected hardware, local-payload, or opt-in probes. The clean portable checkpoint passed all 98 App tests, Markers passed 95 with two local-payload or provider skips, OCR passed 185 with 13 provider or local-payload skips, and integration passed 152 with seven opt-in skips. The exact ignored normalized marker ONNX separately passed its C# CPU runtime probe after aligning activated output boundaries with the frozen `1e-5` parity tolerance. The suite includes exact colored WPF decode to BGR24 order, BGR detector and recognizer tensor order, BGR crop and cache identity, model-specific relative crop padding and padding-value cache identity, three-channel detector masking, fail-closed grayscale, missing-order, and RGB production metadata, DB rotated-polygon geometry, strict probability thresholding, fast mini-box scoring, bounded round-join unclip, raw contour-order candidate caps, post-unclip minimum sizing, original-coordinate mapping, graph-structure consensus and duplicate suppression, normalized marker proposal tensor and postprocessing parity, CPU-only provider enforcement, candidate failure provenance, pre/post-provider cancellation, fail-closed manifest parameters, checksum-bound reviewed OpenCV runtime approval, compiled runtime-mode binding, keyboard graph-cursor behavior, cancellation-safe WPF imports, checkpointed production automatic batches, born-digital PDF import, deterministic PDF crop provenance, the fail-closed marker artifact-mask bridge, cache-bypassed executable payload preflight, cancellation cleanup, real-dispatcher UI-thread rejection, tampering, completed-stage provenance cases, and direct-evidence attack cases. This is still not a complete assistive-technology production workflow, production detector accuracy, final artifact parity, complete scanned-PDF production workflow evidence, or final distribution evidence. |
| Compiled distribution runtime selection | PASS for build binding and direct executable probe; complete release FAIL | Ordinary and development-portable builds compile `ManualPreview`; the common Windows publish explicitly compiles `Production`. A dedicated executable probe returned `0` from the Production publish and `2` from the development build, Production metadata could not be downgraded by a runtime environment override, an invalid `RecordedFake` build mode failed compilation, and 62/62 packaging regressions retained the direct-probe result in verified build metadata. No final release artifact was emitted. |
| WPF keyboard and UI Automation foundation | PASS for direct control behavior; complete production workflow FAIL | The focused App suite proves that a focused graph canvas moves its cursor by one original pixel with arrow keys, by ten pixels with Shift, centers with Home, and invokes the current edit action with Enter. The canvas exposes localized keyboard help, every one of the 51 shell automation IDs has an explicit localized or bound name, and the status region is a polite live region. This does not replace a keyboard-only and screen-reader run through the complete approved-model production workflow, so the mandatory release gate remains blocked. |
| OpenCvSharp native audit | PASS for reproducibility, public provenance, parity, adapter execution, and exact clean-machine load; complete production release FAIL | Two source builds produced identical DLL `87c12460...5791` and linker map `e7f9f768...0a77`; the reviewed 15-entry inventory, public distribution notice, and ignored scoped Microsoft attestation validate the exact source profile. Four committed procedural axis families produced byte-identical canonical output with the NuGet and source DLLs (`cafb1df2...5ac7`). In a fresh Windows 11 Enterprise Evaluation VM with no network adapters and no developer tools on `PATH`, exact commit `cfa874b...`, version `0.0.21`, executable `a20008a...`, and OpenCV binary `87c12460...` all rehashed correctly; native loading passed and `--portable-smoke` exited `0` in 121.128 seconds within the bounded 300-second software-emulation window. The canonical tracked report SHA-256 is `c2725f3f...a670`. This closes only the `opencv-clean-machine-load` gate; no release artifact was emitted. |
| PDFium renderer | PASS for reproducibility, dependency closure, reviewed approval, local workspace execution, and exact clean-machine rendering; complete production release FAIL | Two source builds at PDFium `2870fa9...` are byte-identical. All 240 target labels map once to 15 component dispositions, 16 exact notice sources validate, and PE imports are four Windows system APIs. In the same fresh offline VM, exact renderer `efd13a38...`, application executable `a20008a...`, and application DLL `f8e6611f...` rehashed correctly. The native renderer produced a 72 by 72 BGRA result from the controlled immutable PDF with contract pass and exit `0`, then the packaged `--production-runtime-smoke --require-packaged-pdfium` fallback exited `0`. The canonical tracked report SHA-256 is `1489a380...5768`. This closes only the `pdfium-clean-machine-load` gate; complete downstream PDF workflows and release artifacts remain blocked. |
| Local portable validation | PASS, 8/8 gates | The durable launcher verifies schema-v2 metadata and the exact executable SHA-256 before process creation, including a direct tampering rejection. Purpose-aware classification recorded 55 allowed portable Data events, 12 attributed Windows/WPF/.NET/GPU warnings, zero application-owned or unattributed failures, and fail-closed initially absent LocalAppData and RoamingAppData `GraphAutoReader` root tests; this is not clean-profile or VM evidence. |
| Distribution definitions | FAIL for release | The compiled Production runtime binding is directly verified, but installed and extracted production execution is unverified and neither final release artifact exists. |

The OCR detector runtime now has a separately selected
`probability_with_1e-5_clamp` activation for future preregistered work. It
clamps only finite numerical drift within `1e-5` of `[0,1]`, binds the fixed
tolerance into cache and request provenance, and rejects larger or non-finite
values. The original strict activation is unchanged. This closes only the
runtime boundary defect. It does not reopen the consumed 500-case structure
consensus attempt, approve either official OCR payload, authorize a public or
private evaluation, or change any release gate.

The distinct bounded-probability V2 composition is preregistered before any V2
fixture generation or official-model inference. Protocol SHA-256 is
`0ee2ec0e...e133`; workflow SHA-256 is `b2a52e92...18d7`. It binds both exposed
predecessor split and source inventories, requires zero fixture-byte and case-ID
overlap, and retains every prior model, preprocessing, postprocessing,
consensus, threshold, and provider choice. The only candidate change is the
fixed bounded activation. The single verified 500-case freeze is bound at split
SHA-256 `a7f407aa...f998`, archive SHA-256 `a1f978cf...961d`, and source
inventory SHA-256 `3cd3033a...d34`; both prior-overlap counts are zero. Its
post-freeze authorization binds the exact candidate, evaluator bundle, and gate
configuration. Its single official CPU execution is consumed and failed closed.
The bounded activation accepted one `1.1920928955078125e-7` boundary drift
across 500 calls, but validation exact/CER/role were
`0.205/0.7094339622641509/0.34`, sealed-test exact/CER/role were
`0.21/0.6842105263157895/0.355`, detection exact rate was `0.49`, and 10
exclusion fixtures produced false regions. Duplicate count was zero and ONNX
parity passed at `2.205371856689453e-6`; independent marker-stage evidence is
still absent. Report SHA-256 is `fbd0d960...4d10` and terminal result-seal
SHA-256 is `e9aff703...98ca`. Approval, private validation, and release evidence
remain unavailable, and the exposed split cannot be reused.

Component-geometric OCR V4 is exhausted and remains fail-closed. Final P3 used
only procedural graph labels, passed CPU ONNX parity at
`4.76837158203125e-06`, validation exact match at `0.9609375`, CER at
`0.04361370716510903`, and role accuracy at `0.9713541666666666`. It failed the
mandatory marker-exclusion gate because one of 128 divider exclusions was
classified as digit one, leaving exclusion accuracy at `0.9921875`. Report
SHA-256 is `807db11ffe147f9eadf025bede7403469d064161bb4ffd26349e857eee7588ca`.
P1 through P3 are consumed, the 384-sample public archive remains unopened, and
no OCR manifest, production-store entry, package payload, production approval,
or release approval exists.

The separate structural-filter V1 repair consumed its single selection with
zero optimizer steps. Its fixed pre-classifier rule rejected all 14 validation
dividers, raising exclusion accuracy to `1.0` while retaining exact match
`0.9609375`, CER `0.04361370716510903`, and role accuracy
`0.9739583333333334`. Its one authorized public evaluation then failed exact
match at `0.87890625` and CER at `0.15327695560253699`; role accuracy
`0.9192708333333334` and exclusion `1.0` cannot waive those failures. The
public archive is exposed, the candidate and gate budgets are exhausted, and
the revision cannot rerun, tune, manifest, store, package, or approve a model.

Component-ensemble V5 is separately preregistered from that exhausted revision.
P1 ran exactly once and passed its fixed validation-selection gates at threshold
`0.65`: exact match `0.96484375`, CER `0.04443282801881861`, role accuracy
`0.9732142857142857`, exclusion accuracy `1.0`, and CPU ONNX parity error
`9.298324584960938e-06`. The checkpoint and ONNX remain ignored and unapproved.
The new 672-case public archive was frozen before training and then evaluated
exactly once from the committed selection state. It passed exact match
`0.994140625`, CER `0.005759162303664921`, role accuracy
`0.9970238095238095`, and exclusion accuracy `1.0` with 654 direct CPU calls.
Report SHA-256 is
`a50869bb75f13a39d4601b72e47f49b4b8e684bf4fbb992bd4342e11573e3a20`.
The exposed public split cannot rerun, repair, or tune. The checkpoint and ONNX
remain ignored and unapproved. No model manifest, store entry, package payload,
marker-stage composition evidence, production approval, or release approval
exists.

Marker-center line-aware P1 is consumed and cannot rerun. Its ONNX preflight
failed before optimizer creation, produced no checkpoint, opened no sealed
public fixture, and ran zero public evaluations. The ignored failure report is
bound at SHA-256 `9e3bee532c852ba13c3bdde9390ab9dcbf1a1fcf091072f802dddefe20b6d56c`.
P2 passed CPU ONNX parity at `8.270144462585449e-07` with zero false
positives, duplicates, or prohibited hits, but missed four of 63 validation
markers and is consumed without opening the public archive. P3 reused exact P2
checkpoint SHA-256
`d80732ae317faa3a315fec0d2add194091fe5ce4295be3ff1d6faa65af7562fb`
and ONNX SHA-256
`25d79f6f207f35180d7482e147a1fb1fc099dcd15ab098f109cfb3242c7ff144`
with zero optimizer steps. At threshold `0.07`, it passed all nine selection
scenes and 63/63 markers with zero false positives, misses, duplicates, or
prohibited hits; CPU ONNX parity passed at `8.270144462585449e-07`. Its single
authorized public-gate attempt then failed canonical seal preflight because the
frozen configuration omitted the required `expected_*` bindings. No public
archive was loaded and no metric, gate output, opened seal, or result seal was
created. The tracked attempt record SHA-256 is
`fff7b6dc33c76bcc8a5257aac0fa629697947030ec2d048609b0ebf9c5394ce5`.
The three-candidate and single-attempt budgets are exhausted. This revision
must not be repaired, rerun, tuned, manifested, stored, packaged, or approved.

### Marker-center feasible dense V6 P1 selection

P1 consumed its single 1,152-step execution and passed all 48 visible
selection scenes with 432/432 markers, zero false positives, misses, duplicates,
or prohibited hits, artifact precision `0.9930517209644764`, artifact recall
`0.9974041579689269`, zero marker-artifact hits, and CPU ONNX parity
`0.00000616908073425293`. ONNX, candidate-report, tracked-result, and protocol
SHA-256 values are
`f2e4c209250101ba7c6c1aecec529602d565c3526af4b569a5dd645a2623268c`,
`342c1a18276603ef05e5e68a53a5c4e8f372183142cef9846293058a4d854e09`,
`cc3dfd494c77e05590d68c9d243ca174db9b148b57a9fa1f560c5d6c00291759`,
and `df181749d775d7f94c82e5e75ad0bdd6cfa646eaba530ced14727532dfeef48d`.
The checkpoint-to-fused-graph difference was
`0.000012755393981933594`; this diagnostic was explicitly not a selection gate,
while exact fused-graph-to-CPU-ONNX parity passed the fixed `0.00001` gate. The
truth-hidden public V1 command failed before seal creation, archive parsing, or
metric execution because its frozen configuration omitted the canonical task
identity. Attempt record SHA-256 is
`39d0f1f7ae6168c3eb5cd3f5e80d17ff480b69b541dd1dc467aac3589baacae5`.
V1 is consumed and cannot rerun. It left the public archive SHA-256
`1dac2ce1fd6404493e46749632cfa5f8818104d4817e7b1735565fb477abb741`
unopened at zero evaluations. A corrected V2 wrapper binds task,
revision, candidate hash schema, dataset, evaluator, and gate configuration;
its config SHA-256 is
`1e2cdd82bf4e69808e02a66f831c278ff62f965c953c6b8dc38a09ec55d22e89`.
V2 then opened the archive, consumed its only checksum-bound run, and failed:
0/64 exact fixtures, 190
false positives, 297 misses, zero duplicates, four prohibited hits, artifact
precision `0.6145023044357781`, and recall `0.6914750693739623`. Public report
and tracked result SHA-256 values are
`668a9bef5212f3b2a230ea531dc5369f756e4d3d8bb4ab6eb66f6e724ebf1bf3`
and `4b154417e0512c616f763e8702c98795bdf2064e1245aa0553baae2739cf7d46`.
V6 is exhausted. P2 and P3 are retired because the public split is exposed and
cannot support tuning. No manifest, model-store entry, package payload, private
validation, production approval, or release eligibility exists.

### Marker-center domain-randomized V7

V7 addresses only the aggregate V6 public collapse, without reading or reusing
V6 public fixture bytes or case-level failures. It freezes 384 training, 96
visible validation, and 96 truth-hidden public scenes across fresh split-disjoint
layout and degradation families. The train and validation domain covers marker
shapes, mask imperfections, illumination, blur, compression, stroke,
quantization, scanlines, and noise while preserving the V6 model capacity,
tensor contract, objective, postprocessing, thresholds, and gates. Frozen train,
validation, and public archive SHA-256 values are
`accdc7117821d0e27900988262ff09c5730b31016a02ec4c651c5ebdc4042fa6`,
`8f20701ce02dcf1510cd9773fc0cacc27b102c46bb087b026f81831a754c4e05`,
and `2097d81f6c5b9d05f6015b1528c7a96094259478bf7a40c3a214dbab3c027502`.
All splits have zero target conflicts and no missing artifact hard points. The
current V7 protocol SHA-256 is
`fe16d6fd69fe3c57caf45c6b7d1266406d1bca9e3ca9503100e877b00d8111bd`.
P1 completed all 2,304 optimizer steps and wrote exact checkpoint and ONNX
SHA-256 values `4170b542c048d0a1f1ca711694240d995d9e8b5dd1ec22484edc6759d9c79159`
and `78c378dc95d20c49bf2daebbf7b60cb2fe93185482745e7a80c4d26585a43b55`,
then failed during final report assembly because its explicitly supplied
relative output path could not be normalized against the absolute repository
root. Failure report and tracked result SHA-256 values are
`0ffecb074295603c7ea089724677b885372109744a16096e55b98fa7e626d0d4`
and `8f2fa3e88409b40efccc616861b1f6c38d65ed78a49d04caca5e90efbaa2257d`.
P1 is consumed. P2 reused the exact bytes with zero optimizer steps and failed
visible selection at 49/96 exact scenes, 21 false positives, 82 misses, three
prohibited hits, one marker-artifact hit, artifact precision
`0.8340070217582113`, and recall `0.9549122476437939`; CPU ONNX parity passed at
`0.000009298324584960938`. P2 report and tracked result SHA-256 values are
`be0545b4029fd7825912bb04524198ed75e003417e5a8d52fa0935bd7f37c478`
and `1e96842539994bfb84a24810cda2113f8a221427a339388b27d73051606edf3d`.
P2 is consumed. A checksum-bound training diagnostic reached at most 303/384
exact training scenes and therefore identifies incomplete convergence without
public evidence. Final candidate P3 continued the exact P1 checkpoint for 32
fixed lower-rate epochs and consumed 1,536 optimizer steps. It failed visible
selection at 50/96 exact scenes, 11 false positives, 85 misses, four prohibited
arrowhead hits, one marker-artifact hit, artifact precision
`0.8523057467207811`, and recall `0.9580230017289313`. CPU ONNX parity also
failed at `0.000011861324310302734` above the frozen `0.00001` limit. P3 report,
tracked result, checkpoint, and ONNX SHA-256 values are
`116e7ba0d5c0253d5acba1e4835fb5eca13af408014a5ab867e81f831b79d33d`,
`b4ac9d69b45971e90f6a91317ffae58e3caf07b16959002e4381fe20dcfe3abc`,
`c0377c90d5d10f2d0b9647e541adaad98a6c6168faf0bea0427faa2a6c1e6567`,
and `73ccb797c36751c1bb47f998bbe5ebe12072862e8c92decb76d14f10a485a5c1`.
P1 through P3 and the V7 budget are exhausted. The public archive remains
locked and unopened at zero evaluations. No production asset or approval
exists.

### Marker-center mask-consensus V8

V8 is a fresh defect class based only on the aggregate terminal V7 evidence.
It does not reuse V7 case detail, pixels, fixture identity, checkpoint bytes,
Chandler, private/article images, an external dataset, or downloaded weights.
Before any model execution it froze 512 training, 128 visible validation, and
160 truth-hidden public procedural scenes with disjoint renderer, degradation,
scene-ID, and seed families. Train, validation, and public archive SHA-256
values are
`17a81be50653d291be82b3722803c14ea4c48807fbf10be0c89f88ff6f6442d7`,
`533b32a7da0c12391394cbd7e9fcda011b99f577495e47ffd7f6b1b042f18412`,
and `0f39a5aea033db930c9873e48d795da3ea6dae2bc9fc5d2052c8435ee7554002`.

The split-freeze report proves zero cross-split source overlap, zero
truth/prohibited acceptance overlap, zero truth/mask conflict, and no
prohibited hard point missing from artifact truth across every split. Its
SHA-256 is
`79263032da33b9119078ee645e41f9a5bea3f1a9103ecaee404d5139f0934938`;
the selection manifest and unopened public seal SHA-256 values are
`48f888aca91e3823444d5baf3914b5775b0de0a2c0bb00a4f55d5dcc011f1a32`
and `d1376f99650760d416952604031eb0f7bf06ea1f6893c9f516d7cb7c02ee00b0`.
The freeze records zero model executions, zero optimizer steps, and zero public
evaluations.

P1 executed exactly once from the committed authorization for 3,584 optimizer
steps. It passed 122 of 128 visible scenes with 1,193 true positives, six false
positives, 23 false negatives, zero duplicates, zero prohibited-structure
hits, and zero marker-artifact hits. Artifact precision was
`0.7864128234384776`, artifact recall was `0.9843701768692439`, and CPU ONNX
parity was `0.00002002716064453125`, above the fixed `1e-5` limit. Candidate
report, checkpoint, ONNX, tracked result, opened seal, and result seal SHA-256
values are
`223201e30363c3e150fee0fdd881625aa6b0f137c616cfd3cd4f96b5cb3e0f7e`,
`46eb64a923ff571db14156ca31cdac69adf639b27144e56d4ccf02198ed44122`,
`13b6204bec1129490a871987123bc03416c67cac3150c0efbc5c3d8dadce9f8c`,
`7ad8eb79c88ebd388bfe33873d0f0c4b8c3e6ee6f89ea83147227caf02a10946`,
`44ee0c49ea43553803e39383d35d4d355957ea58eb7865b8a79077a558b2f2d8`,
and `8b2356370907c58ae28059fa6db2aa6e003ae3f45dbe86092ad5f3e05a0cc46c`.
The public archive remained unopened at zero evaluations, and no case detail or
pixels were inspected.

P2 reused the exact P1 checkpoint and ONNX with zero optimizer steps and
changed only the artifact threshold from `0.35` to `0.45`. It still passed
only 122/128 scenes with 1,193/1,216 truths, six false positives, 23 misses,
artifact precision `0.7872885671271697`, recall `0.9829926331356857`, and
failed CPU parity `0.00001621246337890625`.

Final P3 acquired its committed single-use authorization and failed closed
before training during the exact P2 per-channel parity-reproduction preflight.
Center-channel parity reproduced as `0.000003874301910400391` instead of the
preregistered `0.0000033080577850341797`; radius and artifact reproduced
exactly. Zero optimizer steps ran. Candidate report, tracked result, opened
seal, and result seal SHA-256 values are
`a40cbdee8a332f639cfee68df974b77c53c55f5e57847f72e3fc0854cd4b9f9b`,
`fd2dfa1a196e4ddbb63d5099fbc44e734630370e211e7917509286b5ee3204f8`,
`ca153280e4cbee2ef335c53390f54d209cf2058bd17d6de6fbebdc17303247b4`,
and `e676945f68616de32d8de6e1d255e881e1849064853ebd19320f4db372bac91d`.
P1, P2, and P3 are consumed, and V8 cannot rerun. The public archive remains
unopened at zero evaluations. Public, manifest, model-store, packaging,
private Chandler, production approval, and release execution remain
unauthorized. A future attempt requires a new preregistered defect class and
fresh splits.

### Marker-center mask-consensus V9

V9 isolates the consumed V8 P3 runner-reproducibility failure. Its predecessor
diagnostic reuses the exact frozen V8 validation archive and V8 P2 weights, but
uses neither for V9 training, selection, or public evaluation. Every
predecessor per-channel parity maximum must reproduce within `1e-6` of the
preregistered V8 aggregate. This diagnostic allowance does not alter the
independent strict `1e-5` CPU ONNX parity gate applied to a newly exported V9
model.

Before any V9 model execution, the project froze 512 train, 128 validation,
and 160 truth-hidden public procedural scenes with fresh renderer,
degradation, scene-ID, and seed families. Train, validation, and public
archive SHA-256 values are
`1430642d188a18a70a57493f5f4eb154982ec6a0c0bba9434416eeffbe90dced`,
`ba8de943330be2d1bcfaa10614f6d9d4794a982abf3b8647ab555058e3b70eba`,
and `b3867736ed0cbe6b9eccd66b6021d5cc5f5bdfca6c193f3aa9f011cb0686145e`.
The split-freeze report SHA-256 is
`75fdcb45124bd75da2102590c5e2f4e58aceef20b2227c8d7a0114250607de65`.
It proves zero model executions, optimizer steps, public evaluations,
cross-split source overlap, truth/prohibited acceptance overlap, truth/mask
conflict, and missing prohibited artifact targets.

P1 retained the V8 P3 training and selection design on only the fresh V9 train
and validation archives. Commit
`20b803ae8b0f6562c22142029cdcb46eaf4de0cf` and tree
`49c15cc1d583589ad1f52fdc81e13d83387958d4` bind its frozen split, runner,
candidate configuration, and one-use public evaluator. P1 consumed its one
authorization and failed during `p2_parity_localization` because its wrapper
bound `predecessor_parity_by_output_channel` while the inherited V8 parent
runner reads `p2_parity_by_output_channel`. Zero optimizer steps ran and no
checkpoint or ONNX was created. Candidate report and tracked result SHA-256
values are `aac5cd4577b8d348fca932b1836eb5ac35a77257d7a296e00c64455a54eb774c`
and `39797fe3a4440234133b2b55207814502d0eb98fc8bcb7ec7cf159f726ede8ff`.

P2 changed only that inherited-runner alias. Both field names contained the
identical checksum-bound predecessor parity values, and P2 otherwise repeated
the unexecuted P1 training and selection design. Its preregistration commit
`f9416060111a696ca866fc496dab243fdf287c04` and tree
`d47a095abb9dc92f55ad80b4cddad9db6df0294a` were committed and pushed.
P2 consumed its one execution, completed 768 optimizer steps, and failed the
fixed selection and parity gates. The best threshold retained 1,193 of 1,216
truths with eight false positives, 23 misses, zero duplicates or prohibited
hits, artifact precision `0.7855042494371971`, artifact recall
`0.9870400316973273`, and CPU ONNX parity `1.6629695892333984e-5`.

Final P3 uses only aggregate P2 evidence. It continues the exact P2 checkpoint
with a `0.9` false-positive and `0.1` false-negative Tversky artifact objective,
stronger positive and hard-negative center margins, and a `0.5` contraction of
only learned artifact output while retaining checksum-bound seed artifacts and
the fixed `2.5`-pixel radius. Commit
`46f19785196a7fc9e902c9545a4d31540a7663eb` and tree
`e4e80eb14699509811bfcac49c2fb995a8133cdc` bind the final preregistration.
P3 consumed its one execution and completed 768 optimizer steps. It passed
predecessor parity reproduction and strict CPU ONNX parity at
`5.841255187988281e-6`, but failed fixed selection with 121/128 exact scenes,
12 false positives, 18 misses, artifact precision `0.7897035539741141`, artifact
recall `0.980064776805665`, and no passing threshold window. Report, rejected
ONNX, and tracked result SHA-256 values are
`9f36255ba2713c11865daaccc173eac8be4e0da462412a04b882713e1c8f42ad`,
`a4a2445a7fb8b574bf9398c9e5678c3370c39da54edfa0a780afeabefbddd25e`,
and `542c6093415c251256ef0cbb3e25ac97251d08b1ca1e259317352117943c6f79`.
All three candidates are consumed. The public archive remains unopened,
unauthorized, and at zero evaluations. No approved model output,
manifest, model-store entry, package payload, private Chandler validation,
production approval, or release is authorized.

### Marker-center decoupled heads V10

V10 is a separate defect class based only on V9 P3 aggregate counts and
metrics. It does not reuse the V9 checkpoint or any V9 fixture bytes. The
network trains independent full-resolution center and artifact towers from
scratch, and detaches only the artifact tower's gradient from the center loss.
Inference still applies the exact two-pass artifact output before detecting
centers. The fixed three-channel input, three-head output, `2.5`-pixel radius,
postprocessing, thresholds, and production gates remain unchanged.

Before model execution, the project froze 512 training, 128 visible
selection, and 160 truth-hidden public procedural scenes. Train, validation,
and public archive SHA-256 values are
`102dc517eee651d9700ce4d3b51750727f4393ea58aba7a102ddd2d735bc3972`,
`ca522adf45837e53fbd22f39d2d2e6e2379304536ddbb84eb1e19774a6307cda`,
and `27050b012fd9371e95d7f224160c61a87d0373f16637604bdc24ef8dc35c8c4a`.
The split-freeze report SHA-256 is
`59c3390af6bd2c850a5ad0ea12371667cab6747ad1f634c972f091a225d12dfb`.
It records zero model executions, optimizer steps, public evaluations,
cross-split source overlap, truth/prohibited acceptance overlap, truth/mask
conflict, and missing prohibited artifact targets.

P1 executed once and is consumed. It completed all 1,792 optimizer steps,
passed strict CPU ONNX parity at `9.894371032714844e-06`, and produced ONNX
SHA-256 `6b9ba8ee1e149b02d1053b545260e4cc3c6fcd91681a4d21f9330177edcfe9ec`.
It failed fixed visible selection with 120 of 128 exact scenes, 11 false
positives, 23 false negatives, zero duplicates, prohibited-structure hits, or
marker-artifact hits, artifact precision `0.7857918313961029`, artifact recall
`0.9770655093456437`, and no passing threshold window. Candidate report
SHA-256 is
`4939a566fa5058d939a41486ea92436d3db92d91008138a940f3bcacf370da75`.
Only aggregate metrics were inspected.

P2 executed once from the runner bundle and configuration bound by commit
`898d46ce99acfe9c24ef6e55f5af7aaac36fea6b` and tree
`3ebfbfa9affddbdcec4ff0f63d9388c9a94e8120`. It completed all 1,792 steps and
passed CPU ONNX parity at `8.881092071533203e-06`, but failed fixed visible
selection with 121 of 128 exact scenes, 3 false positives, 28 false negatives,
one marker-artifact hit, artifact precision `0.7819652572390059`, artifact
recall `0.9677843660304165`, and no passing threshold window. Report,
checkpoint, and ONNX SHA-256 values are
`a9ac118fe55c801941ad558b02f8d2a75c9f506023ae394ad903a723418b443b`,
`e64c6bf08ccc3f7e60bcf48710230d3081c3cd8f44d0a474d8e303224bf74a40`,
and `bce86a20624802539b428a349e13710c88e8e8c6329175f758ed846023435b24`.
Only aggregate metrics were inspected.

Final candidate P3 was preregistered with runner bundle SHA-256
`45a8d43d143f1fb6125504b11dbaf720f362bbccc07e46587524f8a6cc16d455`
and configuration SHA-256
`a4b7e44e46f3ef23be4ba62942c2b90a6cdb15a172dddfcb667d93b6a0f554ba`.
It retained the exact P2 architecture, optimizer, seed, frozen split, reflection
schedule, loss form, and 1,792-step budget while changing only the artifact
Tversky false-positive and false-negative balance from `0.80/0.20` to
`0.95/0.05`. It reused no P2 checkpoint. Preregistration commit
`1e5316aba72771a0275339f2806b213d3097dbce` and tree
`5d15188227fa2b8e27ab0f0331af8e56c971f05f` bind its exact bytes. Its one
authorized execution completed 1,792 steps and passed CPU ONNX parity at
`7.092952728271484e-06`, but failed visible selection with 119/128 exact scenes,
2 false positives, 29 misses, zero duplicates or prohibited hits, one
marker-artifact hit, artifact precision `0.7822080285166708`, artifact recall
`0.9665081800297624`, and no passing threshold window. Report, checkpoint,
ONNX, and tracked result SHA-256 values are
`06f7641c68e7a1c01909237c1795337ac59a0a4c7fd08326108914c4bae762a9`,
`a838cdd67a215883678289347000b1038942dc3328cfbceac576f02be6364db6`,
`8ea3288c0828722286f61b883e1c8b421a18d0fffcf9d03a5732d7f93511ad32`,
and `e55b0279c4736d791fdf8b51f422852d845d5e5aea2a5bd51606a3da7e65fddf`.
Only aggregate metrics were inspected. All three V10 candidates are consumed.
An aggregate-only feasibility check then proved the architecture cannot satisfy
the unchanged artifact gate: the visible-validation seed contains 289,107
predicted pixels, 178,105 true intersections, 111,002 false-positive pixels,
and 256,001 missing truth pixels. V10's monotonic
`maximum(seed_artifact, learned_artifact)` union therefore has a theoretical
maximum artifact precision of only `0.796366958474284`, below the required
`0.90`. Feasibility report SHA-256 is
`c0b580c68346124b878521dc6ef46f1e3ed4fe587c29be35be66d5bb8992b62f`.
No case detail or pixels were inspected or emitted.
The truth-hidden public archive is locked and unopened at zero evaluations. No
rerun, tuning, manifest, model-store entry, package payload, private Chandler
validation, production approval, or release is authorized. Any future defect
class must permit checksum-bound non-monotonic seed refinement and freeze fresh
renderer, degradation, scene-ID, validation, and sealed-public families before
training.

### Marker-center seed refinement V11

V11 is a new three-candidate defect class based only on the terminal V10
aggregate feasibility proof. Its isolated change replaces the monotonic
artifact union with a bounded logit correction that can remove false seed
pixels as well as add missing artifact pixels. The refined artifact output is
used once through postprocess profile `nonmonotonic_seed_refinement_v1`. The
independent full-resolution towers, scratch initialization, exact four-way
reflection schedule, V10 P3 `0.95/0.05` artifact Tversky balance, 1,792-step
budget, fixed `2.5`-pixel radius, thresholds, CPU provider, `1e-5` parity
limit, and all zero-error gates remain fixed.

Before any candidate execution, the project froze 512 training, 128 visible
selection, and 160 truth-hidden public synthetic scenes. Train, validation,
and public archive SHA-256 values are
`2039ca41c27eee65f561780c93fcb2a64b726567e1e86679423033a805abb058`,
`d3f2cdbcfd9872e066b6935f7c8464a19e06f16d94bc5ca262dc9ebf15762868`,
and `046c5269104b15f3921ec7bdd3f5305cc4a4cb61a15d86f2ae7fcae8b74e4ca9`.
All 800 source hashes are unique, and zero overlap with all 800 V10 source
hashes was observed. The split-freeze report SHA-256 is
`c76612c1cc5c95f84d097fb30311b9d8ed62883bfa61673d8a5e93648eaf23d2`.
It records zero optimizer steps, model executions, public evaluations,
cross-split overlap, truth/prohibited overlap, truth/mask conflict, private
data, and Chandler use.

P1 consumed its only 1,792-step CPU training and visible-selection execution.
The candidate report SHA-256 is
`415e0a42e2e9f85e79686061c44bb128e49b35d731cf0860cbf78dfd48451244`;
checkpoint and ONNX SHA-256 values are
`1d7d3caf8738425f61622d61d181e748bb808371dc199ae565e9892a11beb3a3`
and `ae0ae242fef91d13d688b8d4d37dd1700889dda4e37c74cb915aedb845743bc4`.
CPU ONNX parity passed at `6.377696990966797e-06`. The selected threshold had
122 of 128 exact scenes, 1,190 true positives, zero false positives, 26 false
negatives, zero duplicates, zero prohibited-structure hits, and zero
marker-artifact hits. Artifact recall passed at `0.9687703003413912`, but
artifact precision failed at `0.7815472274567087`; no three-threshold passing
window exists. Aggregate output counts show 250,166 seed-negative pixels added
and 1,175 seed-positive pixels removed. The tracked result SHA-256 is
`8fb959d27f11d501ba6769acdb5e7230a011b751f731cc0bc2382a427e12e190`.
Only aggregate evidence was used, with no case detail or fixture pixels
inspected. P1 is consumed and cannot rerun.

P2 consumed its one training and visible-selection execution from exact
authorization commit `7a29f00517d2cb96be13320ca6b805aef3738e1d`. It completed all
1,792 optimizer steps in `5151352.092` ms and passed CPU ONNX parity at
`9.566545486450195e-06`. The selected threshold again had 122 of 128 exact
scenes, now with 1,187 true positives, zero false positives, 29 false
negatives, zero duplicates, zero prohibited-structure hits, and zero
marker-artifact hits. Artifact precision was `0.782828150056521` and artifact
recall was `0.9603460905861702`; no passing threshold window exists. Its
aggregate output added 244,692 seed-negative pixels and removed 1,253
seed-positive pixels. The report, checkpoint, ONNX, opening seal, result seal,
and tracked result SHA-256 values are `b886281d...24ec`, `d6c22bb1...03af`,
`8ad7439d...2a9`, `ff7bca7b...bb7`, `bcbf3a72...195e`, and
`563197cf...64a3`. Only aggregate evidence was used, with no case detail or
fixture pixels inspected. P2 is consumed and cannot rerun.

P3 consumed the final authorized from-scratch execution. It completed all
1,792 optimizer steps in `5077483.074` ms and passed CPU ONNX parity at
`3.2782554626464844e-06`. Its weight-6.0 false-seed-retention loss collapsed
the refined artifact mask at the fixed `0.35` artifact threshold, removing
289,107 seed-positive pixels and adding none. Artifact precision and recall
were both `0.0`. At the selected `0.30` center threshold, P3 passed only 112 of
128 scenes with 1,178 true positives, two false positives, 38 misses, zero
duplicates, zero prohibited-structure hits, and zero marker-artifact hits. No
passing threshold window exists. Report, checkpoint, ONNX, opening seal,
result seal, and tracked result SHA-256 values are `688b1b18...64f2`,
`2c134d68...84e2`, `070ea4c7...4706`, `9454eade...d42f`,
`dfd1eefa...cba0`, and `4276a6ba...69ac`. Only aggregate evidence was used,
with no case detail or fixture pixels inspected.

P1, P2, and P3 are consumed. V11 is exhausted and cannot rerun or tune. The
one-use public evaluator remains unauthorized and its truth-hidden archive
remains locked and unopened at zero evaluations. Any future marker-center
attempt requires a separately preregistered defect class with fresh unexposed
splits. No manifest, model-store entry, package payload, private Chandler
validation, production approval, or release is authorized.

## Development Portable Preview

This is a local development preview, not the mandatory portable release. The
current checkpoint is version `0.0.21`. The exact post-commit development
portable is recorded in ignored `artifacts/dev-portable/latest.json` after the
checkpoint commit is integrated and pushed.

- latest checkpoint version: `0.0.21`
- the authoritative exact executable, commit, and SHA-256 after every stable
  checkpoint are selected atomically by ignored `artifacts/dev-portable/latest.json`
- full-test status: PASS, 901 passed and 24 expected hardware, local-payload, or opt-in skips in the clean Release checkpoint; all 96 App tests passed
  build; the harness remains fail-closed if Windows supplies a transparent
  pixel surface
- manual workflow status: authorized Chandler WPF composition PASS on immutable
  original bytes; the historical local `realesr-animevideov3` comparison remains
  evidence only
- automatic stage status: the configured enhancement is rejected with the exact
  manifest blocker and is no longer advertised from environment-variable
  presence alone; axis, OCR, markers, legends, and phases remain unavailable,
  and normal Production composition accepts approved assets only
- packaged automatic stage status: 0 production-approved stages
- known missing release models: OCR detection, OCR recognition, and marker
  center lack approved payloads; marker classification is approved
- last maintainer graph validation date: 2026-08-05

| Development preview gate | Status | Evidence or blocker |
| --- | --- | --- |
| Self-contained Windows x64 WPF preview builds | PASS | `packaging/Build-DevPortable.ps1` publishes the clean committed checkpoint and runs its WPF process smoke. |
| Reviewed source-built OpenCV replaces the internal preview payload | PASS for opt-in internal build and public provenance; production FAIL | `Install-ReviewedRuntime.ps1` validates retained evidence, review policy, private attestation, exact SHA-256, single destination, and reparse-point safety before replacement. `Install-ReleaseRuntime.ps1` additionally requires a directly passing checksum-bound clean-machine gate before it can set release approval in the one common publish. The 6/6 promotion suite rejects tampered or blocked evidence. The current self-contained WPF preview correctly retains `releaseApproved=false`; clean-machine evidence remains blocked. |
| Latest-success metadata and executable identity are fail closed | PASS | Schema-v2 `latest.json` records the exact executable SHA-256. The Windows PowerShell 5.1-compatible durable `.cmd` launcher rehashes the selected immutable executable before process start and rejects missing, malformed, stale, or tampered metadata. Behavioral development-portable packaging regression passed 8/8 and leaves no temporary or backup metadata files. |
| Failed builds preserve the last good preview and diagnostics | PASS | Behavioral packaging regression verifies last-success preservation and captured child-process diagnostics. |
| Multi-file model payloads are independently checksummed and verified | PASS | Release packaging regression validates a two-file param/bin model through build metadata, portable archive, SBOM, and deep artifact verification. |
| Preview model discovery fails closed | PASS | Behavioral regression verifies single-file and multi-file discovery, release-eligible benchmark gating, malformed checksum-map rejection, and exclusion of unapproved models. |
| Enhancement availability uses resolved evidence | PASS for fail-closed composition; production FAIL | Async startup resolves the exact configured manifest, notice, runtime dependency, and model checksums before marking enhancement Experimental. A rejected manifest records `MODEL_RUNTIME_INCOMPATIBLE`, disables the control, preserves the real Chandler manual workflow, and makes no production approval claim. Direct NCNN timing proves the previous ten-second test failure was status misclassification, not runtime latency. |
| Linked-worktree commit changes regenerate an exact-commit build | PASS | Watcher regression observed four expected builds with a measured 2.63-second debounce. |
| Paths with spaces and Unicode use the configured shared preview Data root | PASS | A copied preview launched from `portable preview 두번째`, exited successfully, created the configured shared Data root, and did not create a local Data directory. |
| Empty real-data WPF shell renders without canned graph data | PASS in the latest clean portable build | The live Chandler capture after invoking the actual Zoom In and Fit Graph controls is `artifacts/dev-portable/evidence/live-chandler-e87ea7f-after-fit.png`, SHA-256 `b3f85b6ef8e02fd7caef701fa6fe72304fef685c81582f9573c42a7fc46ccf0c`. It shows the real source image, no detected series, and the `ManualPreview` runtime. The harness rejects transparent surfaces instead of emitting false evidence. |
| Manual service supports import, calibration, series and point edits, explicit baseline/probe relations, save, autosave, recovery, reopen, and real export | PASS | Focused manual workflow regression passed 8/8, including ViewModel-authored relationships and original-byte preservation. |
| WPF command composition covers the manual workflow | PASS | A bounded STA WPF scenario passed 1/1 through import, tabs, calibration clicks, series and relation commands, point and phase edits, Save As, Save, recovery-to-new-file, reopen, and export. Its generated test-only images are not maintainer evidence. |
| Goal 21 refined preview is launchable | PASS | The native WPF preview exposes the graph tab strip, left series panel, resizable right inspector, project summary, complete manual tool row, corrected fit/zoom behavior, and original/enhanced/comparison views. Exact clean artifact evidence is recorded after commit. |
| Local portable startup/path harness | PASS, 8/8 local gates | The durable Windows PowerShell launcher now verifies schema-v2 executable identity and rejects a tampered payload before launch. Purpose-aware classification passed with complete process/component/purpose evidence and a fail-closed application persistence negative test; `cleanVmEvidence=false`. |
| Real maintainer graph is manually imported, calibrated, edited, saved, reopened, exported, and visually checked | PASS | The user-supplied Chandler PNG passed the actual `ManualPreview` WPF composition with 3 anchors, 2 series, 6 points, 2 dividers, explicit Fit and zoom, save/reopen, export, portable autosave/recovery, pan, fixed magnifier, unchanged source hash `65bd6e...342e1`, no fake graph data, and retained rendered evidence. |
| Promote checkpoint to `0.0.21` | PASS | The internal manual and local-enhancement checkpoint has direct evidence. This does not approve any automatic production stage or public release gate. |

## Session 18 implementation gates

| Gate | Status | Evidence or blocker |
| --- | --- | --- |
| Merge production modules without rewriting internals | PASS for foundation | App references and real adapters connect PDF, Inference, OCR, Markers, Legends, Phases, and Export without changing frozen schemas. |
| Select runtime environment and fail closed per automatic stage | PASS | Production composition now uses the real-data production workspace, keeps manual operations available, and declares enhancement, axis, OCR, markers, legends, and phases independently unavailable. The common Windows publish compiles an immutable Production default and directly probes the published executable; development builds remain ManualPreview, and RecordedFake cannot be compiled or selected by ordinary configuration. |
| Register and execute actual production stage adapters | PASS for the complete approved-component composition boundary; real execution FAIL | `ProductionAutomaticDetectionAdapter` consumes the existing concrete raster, axis, OCR, artifact-mask, marker-center, marker-classifier, grouping, legend, and phase components and writes exact calibration, OCR, marker, series, point, phase, participant, transform, provenance, and export evidence for WPF projection. Its `IsApproved` is the conjunction of every component approval. Application composition passes this adapter to the real detection stage only when every dependency and the artifact-mask provider are approved; otherwise it leaves the full adapter null. The concrete marker artifact-mask bridge requires the existing frozen `marker_center` task, the exact three-plane input and output contract, the production store's selected evidence checksum, mandatory CPU support, and a separate direct artifact-mask public gate. It maps only normalized dense evidence to original pixels, reuses the bounded shared inference runtime, preserves cancellation, and rejects fake providers or invalid model output. Deterministic tests prove dense mask mapping and seed preservation, unapproved and cancelled runs stop before inference, invalid output fails closed, and a real direct report can compose the lazy bridge without promoting the automatic workflow. Nine negative cases prove opaque status, missing resources, model mismatch, manifest/report disagreement, arrow-shaft hits, invalid dataset schema, an unsealed split, fixture-identity drift, and evaluator-source drift all fail closed. The committed evaluator has three Python contract tests. A 2048 by 2048 regression verifies seed construction and repeated marker frames add less than 1 MB of thread allocation while reusing immutable full-resolution planes. The current real manifest lacks that passing scope evidence, so the availability registry still refuses to promote OCR, marker, legend, or phase execution. These tests do not promote the unavailable real OCR, rejected marker-center payload, or absent approved production artifact-mask evidence. |
| Wire `Import -> Prepare -> Detect -> Review -> Export` into the WPF shell | PASS for concrete composition; production execution FAIL | The WPF automatic command reaches the real orchestrator, concrete approved-component detector, all-or-nothing projection, and export evidence when every dependency is present. Normal composition remains fail-closed before detection because approved real OCR and marker-center assets are absent. |
| Wire project save, autosave, recovery, and tabs into the WPF shell | PASS | The real-data manual composition exposes these WPF commands. The focused manual workflow passed save, autosave, recovery, reopen, close-tab, export, and cancellation-safe batch behavior. Cancellation during the second image retained exactly one visible and persisted completed image while both original checksums remained unchanged. Production automatic orchestration now also checkpoints each completed review panel before advancing, so later cancellation preserves completed automatic work and rerun completion. PDF panel tabs preserve page number, stable panel identity, source-document checksum, panel-image checksum, and immutable bytes through save/reopen. A deterministic approved-adapter fixture additionally proves that a production result retains its complete runtime envelope and automatic panel evidence through save, reopen, and CSV/audit export. |
| Run original/enhanced consensus | PASS | Enhancement on/off and deterministic original/enhanced consensus pass in the focused integration smoke suite. |
| Preserve manual corrections across reruns | PASS | Move, delete, add, reassign, calibration, and phase corrections survive save/reopen and a second approved-adapter rerun; untouched detections remain `Accepted` while corrected points remain `Corrected`. Save and export no longer rewrite untouched automatic points as manual or discard their marker link, source stage, model version, confidence, and review status. This is deterministic adapter evidence, not approval of the missing production payloads. |
| Initialize installed and portable runtime paths at WPF startup | PASS | Startup consumes `RuntimePathBootstrapper`; focused tests cover installed paths, portable Unicode paths, and structured read-only failure. |
| Run full public validation | FAIL | The 37/37 public result is explicitly synthetic metric-contract smoke, not full production-pipeline validation. |
| Run local private validation when available | PASS for manual workflow; automatic FAIL | The authorized private Chandler case completed the real manual WPF workflow. No production automatic detector run or accuracy claim exists. |
| Run supplied-example local validation | PASS for manual workflow; automatic FAIL | The supplied Chandler PNG is ignored and unchanged. Manual workflow evidence passed; the false-marker automatic acceptance criteria remain unmeasured. |
| Run full installer smoke | FAIL | No clean-machine installation, launch, repair, reinstall, upgrade, or uninstall evidence exists. |
| Run full portable ZIP smoke | FAIL | No final ZIP exists and no clean-machine extract-and-run workflow was performed. |
| Verify identical app build, contracts, manifests, and notices in both artifacts | FAIL | Final installer and portable artifacts were not emitted. |
| Produce a per-gate readiness matrix | PASS | This document records every mandatory Session 18 and Windows distribution gate without waiver. |
| Promote only after every mandatory gate passes | PASS | Central version was only the internal `0.0.21` checkpoint; no `1.0.0` promotion, release tag, or public artifact was created. The corrected policy permits an explicitly authorized jump from any `0.y.z` checkpoint to `1.0.0` only after these gates pass. |

## Session 18 required scenario gates

| Required scenario | Status | Evidence or blocker |
| --- | --- | --- |
| Single-image import end to end | FAIL | Real image import and immutable panel retention are connected, but approved detection and export cannot run. |
| Batch-image import end to end | PASS for real WPF import and checkpointed production-composition cancellation; approved packaged downstream FAIL | Ordered real-image import is connected. A deterministic WPF scenario cancelled while the second image was blocked and proved that exactly the completed first image remained visible and persisted, the UI reported cancellation, and both source hashes were unchanged. Production import retains source-one immutable panel evidence when source two is cancelled. A separate two-panel executable production-composition scenario completed detection and review for panel one, cancelled at panel two, retained the projected panel-one automatic result and both unchanged source hashes, then reran to a complete ordered two-panel result. The mandatory gate remains blocked because the approved packaged OCR and marker-center set is incomplete, so this cannot yet be sealed as normal production end-to-end evidence. |
| PDF import and panelization end to end | PASS for born-digital and exact local scanned-page workspace import; complete production workflow FAIL | The WPF picker and real-data workspace open detector-ready panels from a born-digital PDF through the real PdfPig inspector and deterministic panelizer. A direct integration scenario verified a real 640x480 panel tab, page 1, immutable source-document checksum, no fake graph workspace data, stable panel identity, and save/reopen behavior. The ignored reviewed PDFium approval now also passes an exact local annotation-only scanned-page fallback through production composition into a real workspace tab, with deterministic crop bytes and reversible page provenance. Clean-machine execution, final-distribution testing, and downstream approved detection, review, and export remain unavailable. |
| Enhancement off | PASS for original-first foundation; end-to-end FAIL | The real prepare adapter preserves and forwards immutable original evidence without enhancement. Approved detection and export cannot run. |
| Enhancement on | PASS for explicit historical local evaluation; current production FAIL closed | The official anime x2 model previously ran through the Chandler comparison view with exact 2x dimensions and immutable original bytes. The new authorized-vcomp profile also passes direct public synthetic execution, but local-adapter and scientific approval are false, so normal production composition rejects it. |
| Manual calibration | PASS | The authorized Chandler run persisted and reopened anchors `(1,0)`, `(1,100)`, and `(24,0)` in the actual ManualPreview WPF composition. |
| Automatic calibration | PASS for concrete approved-component composition; production execution FAIL | A direct 2-point composition regression derives x/y ticks from OCR, fits the session-first lattice and three required anchors, maps graph coordinates, and persists exact export calibration. No approved real OCR or marker-center payload can execute this path yet. |
| Marker correction | PASS for manual points | The authorized Chandler run added, moved, deleted, and re-added manual point centers with updated counts and persistence. This is not automatic marker accuracy evidence. |
| Phase correction | PASS for manual dividers | The authorized Chandler run added and persisted the 9/10 and 14/15 dividers and labels. Generalization and Maintenance phase semantics export as `g` and `m`. |
| Shared baseline export | FAIL | Explicit same-tab baseline relations and exported shared rows pass in the manual workflow regression, but no live maintainer graph export has been checked. |
| Generalization probe export | PASS | The authorized Chandler export includes both open-circle probes as applicable probe rows with phase `g`. |
| Save and reopen | PASS | The authorized Chandler project passed Save As, existing-path Save, close, reopen, and retained calibration, series, points, and dividers. |
| Autosave and recovery | PASS | A real Chandler edit wrote autosave under portable `Data` and recovered to a new project file with the edit preserved. |
| CSV export | PASS for manual workflow | GraphReader.Export wrote nonempty output with exact minimal header `x_value,y_value,phase` from the real edited Chandler state. |
| Keyboard-only operation and UI Automation | PASS for direct WPF controls; complete production workflow FAIL | The graph canvas supports arrow-key original-pixel navigation, Shift ten-pixel steps, Home centering, and Enter or Space activation for calibration, selection, point placement, movement, and phase-divider placement. All 51 shell automation IDs have explicit names and the status message is a polite live region. A clean-machine assistive-technology run through approved production detection remains absent. |
| Clean install | FAIL | Not run on a clean Windows machine or VM. |

## Session 18 acceptance gates

| Acceptance criterion | Status | Evidence or blocker |
| --- | --- | --- |
| Complete core workflow functions without network access | FAIL | No production end-to-end offline run exists; required default models are unavailable. |
| Original image remains unchanged | PASS for manual workflow | The private Chandler SHA-256 remained `65bd6e1021bd9a3f2d2564a640ff90060a34e784782f2d847b4b26f9c6c342e1` before and after import, editing, persistence, export, and recovery. |
| Every output point is editable and auditable | PASS for approved-adapter fixtures; production execution FAIL | Real projection returns editable points plus stable correction IDs and preserves corrections across save/reopen and rerun. The existing extended audit export retains point source stage, model version, and review status, while the project audit retains the complete checksum-bound provider and timing envelope through save/reopen. No approved model can yet produce a normal production result. |
| Supplied example has no text, axis, divider, or arrow false markers | FAIL | Authorized private manual evaluation ran, but no automatic detector evaluation ran, so the false-marker criterion remains unmeasured. |
| Packaging and license gates pass | FAIL | OpenCV and PDFium provenance, notice review, and exact clean-machine component gates pass, but the complete release-approved production model set, artifact pair, and end-to-end clean-Windows workflows are absent. |
| Installer and portable artifacts pass the same workflow | FAIL | Neither final artifact exists or passed clean-machine testing. |
| Installed mutable data stays under `%LOCALAPPDATA%\GraphAutoReader` | FAIL | Startup bootstrap and injected path tests pass, but application-wide settings, cache, logs, autosave, and recovery consumption lacks direct runtime proof. |
| Portable mutable data stays under `.\Data` | FAIL | Startup bootstrap, Unicode-root selection, and structured read-only tests pass, but application-wide mutable-service consumption lacks direct runtime proof. |
| No mandatory criterion is waived silently | PASS | All unavailable or failed evidence is recorded as `FAIL`. |

## Windows platform and common-build gates

| Distribution contract gate | Status | Evidence or blocker |
| --- | --- | --- |
| C# and .NET 10 Windows desktop application | PASS | `GraphReader.App.csproj` targets `net10.0-windows`. |
| Native WPF UI | PASS | `UseWPF` is `true`; `OutputType` is `WinExe`. |
| Windows x64 first target | PASS | Application and packaging definitions use `win-x64` or `x64`. |
| No Electron, browser, WebView, Blazor Hybrid, or local web-server shell | PASS | App architecture tests and project references contain no prohibited shell dependency. |
| One self-contained common publish feeds both packages | PASS | Packaging regressions verify the common-publish and no-rebuild contract. That publish now compiles Production mode into the application assembly, runs the exact published executable probe, and records the passing result for deep verification. |
| Installer filename is `GraphAutoReader-<version>-win-x64-setup.exe` | PASS | `packaging/artifacts.json` contains the exact template. |
| Portable filename is `GraphAutoReader-<version>-win-x64-portable.zip` | PASS | `packaging/artifacts.json` contains the exact template. |
| Both final release artifacts exist | FAIL | The fail-closed audit emitted no production release artifacts. |
| Release version is eligible | PASS for cadence only | `0.0.21` is on an allowed release cadence, but cadence never overrides the failed mandatory gates. |
| Release is built from a clean tree | FAIL | No release build was attempted or proven from a clean tree. |
| No automatic updater is required for 1.0 | PASS | No updater dependency or update service is present. |
| No silent executable or model download | PASS | Source scan found no application or installer network-download API. |
| Project-file upgrade is versioned, tested, and backup-reversible | FAIL | Domain migration tests do not prove an installed or portable upgrade and backup workflow. |
| Portable folder replacement preserves or migrates `Data` | FAIL | No portable upgrade test was run. |

## Installer gates

| Installer requirement | Status | Evidence or blocker |
| --- | --- | --- |
| Self-contained .NET desktop runtime | FAIL | Fixture packaging passed, but no production installer was emitted and tested. |
| Installs the application and required default models | FAIL | No release-approved default model set exists. |
| Per-user install without administrator rights | FAIL | Definition declares it; no clean-machine install proves it. |
| Installation root supports paths with spaces | FAIL | No clean-machine test. |
| Start Menu entry | FAIL | Definition and code exist; no runtime verification. |
| Normal uninstall entry | FAIL | Definition and code exist; no runtime verification. |
| `.garproj` is not registered before format stability | PASS | `packaging/installer/installer.json` sets `projectFileRegistration` to `false`. |
| Repair or same-version reinstall | FAIL | No clean-machine test. |
| Upgrade behavior | FAIL | No upgrade test. |
| Uninstall behavior | FAIL | No clean-machine test; failure atomicity is also a known limitation. |
| Preserve user data during uninstall | FAIL | No clean-machine test. |
| Mutable data under `%LOCALAPPDATA%\GraphAutoReader` | FAIL | The installed application has not directly demonstrated path-policy consumption. |
| Logs, cache, autosaves, settings, and recovery remain outside install root | FAIL | No installed runtime path-isolation trace exists. |
| No internet required for core workflow | FAIL | No offline installer workflow and no releasable default models. |
| Includes licenses, notices, manifests, and checksums | FAIL | Verifier behavior passed, but no production installer exists to inspect. |

## Portable gates

| Portable requirement | Status | Evidence or blocker |
| --- | --- | --- |
| Extract-and-run ZIP | FAIL | No production portable ZIP was emitted or launched. |
| Self-contained .NET desktop runtime | FAIL | Fixture packaging passed, but no production ZIP was emitted and tested. |
| No installer or administrator rights | FAIL | Definition declares it; no clean-machine launch proves it. |
| No Start Menu or uninstall entry | FAIL | Definition declares it; no runtime verification. |
| Contains required default models | FAIL | No release-approved default model set exists. |
| Contains license notices | FAIL | Verifier behavior passed, but no production ZIP exists to inspect. |
| `portable.mode` beside executable | FAIL | Packaging regression covers the layout, but no production ZIP and runtime launch prove it. |
| Mutable data under `.\Data` | FAIL | Application-wide consumption lacks direct runtime proof. |
| No implicit `%LOCALAPPDATA%` writes | PASS locally; clean-VM FAIL | The local harness passed with no Graph Auto Reader persistence outside configured Data. Exact LocalAppData and RoamingAppData application roots are snapshotted around every scenario, and initially absent root negative tests fail closed. Attributed system/runtime/driver caches remain warnings. Clean-VM evidence is absent. |
| No registry configuration dependency | FAIL | Startup succeeded while `HKCU\Software\GraphAutoReader` remained absent, but this is not a complete registry trace or clean-machine result. |
| Clear read-only-folder diagnostic | PASS | A deny-write ACL produced the exact visible WPF message: `This portable folder is read-only. Move it to a writable folder and try again.` |
| No internet required for core workflow | FAIL | No offline portable workflow and no releasable default models. |
| Launch from path containing spaces and Unicode | PASS | The live portable harness launched the self-contained payload from a path containing spaces and Korean Unicode. |

## Shared release-content and model gates

| Shared requirement | Status | Evidence or blocker |
| --- | --- | --- |
| Installer and portable contain the same application version | FAIL | No final artifacts exist for comparison. |
| Installer and portable contain the same Git commit | FAIL | No final artifacts exist for comparison. |
| Installer and portable contain the same contract version | FAIL | No final artifacts exist for comparison. |
| Installer and portable contain the same model manifests | FAIL | No final artifacts and no release-approved production model set. |
| Installer and portable contain the same model checksums | FAIL | No final artifacts and no release-approved production model set. |
| Installer and portable contain the same notice set | FAIL | No final artifacts exist for comparison. |
| Every required model has exact ID and version | FAIL | Candidate manifests do not form a complete approved default set. |
| Every required model has checksum and source | FAIL | No complete checksum-approved default set exists. |
| Every required model has exact license, commercial-use, and redistribution approval | FAIL | Zero production model files are release eligible. |
| Every required model has a safe notice path | FAIL | No complete approved default set exists. |
| Every required model declares supported execution providers | FAIL | No complete approved default set exists. |
| Every required model has release-eligible benchmark evidence | FAIL | OCR and marker production acceptance is incomplete; GraphSR failed fixed gates. |
| OpenCvSharp native redistribution is approved | FAIL | The reproducible source profile, notices, 15-entry inventory, ignored scoped Microsoft attestation, exact four-family axis parity, source-runtime integration suite, WPF smoke, common-publish replacement seam, and runtime checksum probe validate locally. Clean-machine loading and workflow evidence remain absent, so production metadata cannot be promoted. |
| Core digitization works offline immediately after install or extraction | FAIL | Required default models are unavailable and no offline artifact smoke was run. |

## Required distribution checks

| Required check | Status | Evidence or blocker |
| --- | --- | --- |
| Clean Windows VM launch | FAIL | Not run for either distribution. |
| Image import | FAIL | Not run from either final distribution. |
| Batch import | FAIL | Not run from either final distribution. |
| PDF import | FAIL | Not run from either final distribution. |
| Default model discovery | FAIL | No release-approved default model set. |
| CPU fallback | FAIL | Component tests do not prove packaged production workflow fallback. |
| Save | FAIL | Not run from either final distribution. |
| Autosave | FAIL | Not run from either final distribution. |
| Recovery | FAIL | Not run from either final distribution. |
| CSV export | FAIL | Not run from either final distribution. |
| License and notice presence | FAIL | No final artifacts exist. |
| Forbidden-file scan | FAIL | Verifier regression passed, but no final artifacts exist to scan. |
| Checksum verification | FAIL | Verifier regression passed, but no final release checksum set exists. |
| Settings-path isolation | PASS locally; installed/clean-machine FAIL | The local portable harness passed purpose-aware isolation with zero application-owned or unattributed failures. Installed and clean-machine traces remain absent. |
| No network requirement | FAIL | No network-disabled full workflow was run. |

## Direct C# OCR-marker composition V2

The failed V1 archive remains immutable and cannot rerun. V2 is sealed from its
committed preregistration with a distinct 40-scene synthetic-only generator,
fresh non-serialized secret,
new renderer, degradation, graph, label, and scene identities, exact five
candidate hashes, direct C# CPU execution, per-call tensor hashes, and mandatory
zero-error marker safety counts. Its source seal includes the repaired marker
runtime with the frozen `1e-5` activation-boundary tolerance. It excludes V1
fixture bytes and truth, Chandler, `Generalization`, private and article data,
and external data. Focused Python protocol tests pass 5/5 and the opt-in runner
remains skipped before inference. The archive contains 200 text truths and 328
marker truths. Archive SHA-256 is `f7873bc6fca1bd42c216b94d89d267c843ec2133375bd41f13ab4b887538f301`,
manifest SHA-256 is `8167cdfbb7d24cd7d3201ed1eeb7c63bb676029f9f85fb7006043fcdaa9bf607`,
and tracked split-seal SHA-256 is
`e348d645c85156fa5696de9ea9107031990b20d99832a9172cf9a2cf2daeef57`.
The hash-only split seal must be committed before its single authorized run.
Even a pass cannot waive artifact-mask, manifest, production
model-store, packaging, private Chandler, clean-machine, or release gates.

Current verification is 888 passed with 18 expected skips, including the
consumed gates that remain opt-in and skipped by default. The public scoreboard
smoke passes, while the release
audit remains closed with `ReleaseReady=False` and `ArtifactsEmitted=False`.

## Direct C# OCR-marker composition V3

The one authorized V3 CPU execution ran from exact source commit
`458ec387076344229490e58bc98fa0bd07d36af7` and failed closed. Its ignored
report SHA-256 is
`a5fc84a4a02c10f39abb156cb33895bb7b6a21c32a98137586b31b9058d7e9a4`,
the opened-seal SHA-256 is
`a09a3aa3dece30131d2814aa46262e015f65457956b8fe637bdd3f37b5b5f5d7`,
and the fail result-seal SHA-256 is
`ce9c1d30ffbd9126ac9bea9fcbf7f3bfdfdc5fe2abd5d77e347e8f358df2c4a1`.
All five checksum-bound payloads executed through the C# CPU composition with
per-call tensor hashes. OCR retained 373/384 truths, with 95 false regions, 11
missed regions, and zero duplicates. Recognition exact match was
`0.8880208333333334`, CER was `0.02302158273381295`, role accuracy was
`0.5260416666666666`, numeric exact match was `1.0`, word exact match was
`0.8819188191881919`, and ambiguity exact match was `1.0`. The marker stage
retained 379/392 truths, with eight false markers, 13 misses, zero duplicates,
eight text-derived marker creations, and exact marker counts in 30/48 scenes.

This execution consumed V3. It may not rerun or be repaired against the exposed
fixtures. No manifest, model-store promotion, production artifact-mask
approval, packaging approval, private Chandler validation, production
approval, or release eligibility was created. The next OCR-marker attempt must
be a separately preregistered defect class with new unexposed families that
addresses full-scene proposal precision, role generalization, derived text-mask
safety, and marker recall without weakening any mandatory gate.

## Direct C# OCR-marker composition V4

The single authorized V4 run executed from exact source commit
`e70eb014b0436f22abd811a243b85a13cbd72f9e` and failed closed. The ignored
report SHA-256 is
`ac6dfc56095f1d010330fef604bae8e7e0f880763b64ad0d02eb923ff03cc0d3`,
the opened-seal SHA-256 is
`32b0f796e5162e252887b7ded0021702a5d1e70bd2677da4e7625c602ed305b1`,
and the fail result-seal SHA-256 is
`b8a7c7672ddf3e6ebc2e7e5de923f4ca07b3da10352a0e8e116dd63c46acfb18`.
All five checksum-bound payloads executed on CPU and recorded direct input and
output tensor hashes for every call.

Across 64 fresh sealed synthetic scenes, OCR found all 320 text truths with no
misses or duplicates but produced 192 false regions, so zero scenes were OCR
exact. Recognition exact match was `0.99375`, CER was
`0.0011695906432748538`, role and numeric accuracy were `1.0`, word exact match
was `0.988950276243094`, and ambiguity exact match was `1.0`. The marker stage
retained 465/522 truths, with two false markers, 57 misses, zero duplicates, and
exact counts in 14/64 scenes. Text-derived marker creation was zero and every
prohibited category remained at zero hits.

V4 is consumed and may not rerun, be repaired, or be tuned against its exposed
fixtures. It covers only five roles, and its checksum-bound fixture-authored
structure mask is not an approved production artifact-mask provider. No
manifest, model-store promotion, packaging approval, private Chandler
validation, production approval, or release eligibility was created.

## Full-scene OCR proposal and role V12

V12 is exhausted and failed closed. It was preregistered from aggregate-only
V3 composition evidence, then used new variable-layout procedural scientific
graphs with disjoint 480-scene training, 120-scene visible validation, and
160-scene truth-hidden public families. It did not use Chandler,
`Generalization`, private or article images, V3 fixture bytes or truth, external
data, or predecessor split bytes.

P1 retained all 960 visible truth regions and all eight roles but left one
structural false region and exceeded the fixed ONNX parity limit. P2 reused the
exact checkpoint with zero optimizer steps and passed CPU parity at
`4.291534423828125e-06`, but retained that false region. P3 reused exact P2
ONNX SHA-256
`8d1fc526060cc0be455eaa25286d56319d566645ef2d2f9cebd92029cdd7a813`
and added only a preregistered source-geometry guard for a single-component,
low-density horizontal stroke group. The guard rejected zero truth-matched
proposals across train and validation. P3 passed all 120 visible scenes with
960/960 truths, zero false regions, misses, duplicates, or structure hits, and
perfect role accuracy. Selection report SHA-256 is
`4dc2bc885a6f5d52c88219939103ddb7e28b61bd02df1cbd527ccdc69fd7c323`.

The single checksum-bound hidden-public run then directly executed all 160
frozen fixture images on CPU. It passed 155 scenes, retained 1279/1280 truths,
produced four false regions and one miss, produced zero duplicates, and retained
perfect role accuracy for accepted truths. The structural guard rejected zero
public truth-matched proposals. Public report SHA-256 is
`412ffe748d7aacc927fc5c5f19109c68937954a14ed7e10ccbb34276063630b0`;
opened and result seal SHA-256 values are
`ac652b882ad80dc6f101e52457559a8be1c92bd7796eb15f7c3ba4cac080c028`
and
`459348e979c49164f818025342fab252a0e309a05dd81da93869ab06ae8a4d7c`.
All three candidates and the public evaluation are consumed. No rerun,
case-level public tuning, manifest, model-store promotion, packaging, Chandler
automatic validation, production approval, or release eligibility is
authorized.

## Morphology-aware OCR proposal and role V13

V13 is exhausted and failed closed. It used a fresh synthetic-only renderer and
disjoint 600-scene training, 160-scene visible validation, and 224-scene
truth-hidden public families. It did not use Chandler, `Generalization`,
private or article images, V12 public fixture bytes or case identity, external
data, or downloaded training data.

P1 retained all 1,280 validation truths with perfect role accuracy, but one
prohibited false proposal remained at every fixed threshold and CPU ONNX parity
was `1.52587890625e-05`, above the fixed `1e-5` maximum. P2 reused the exact P1
checkpoint with zero optimizer steps and applied only a fixed output scale. It
passed all 160 validation scenes with zero false, missed, duplicate, or
prohibited regions, perfect role accuracy, selected threshold `0.94`, and CPU
ONNX parity `7.62939453125e-06`. Its ONNX SHA-256 is
`8d7fd1358508da29620e622d72bb9fb5b5286516cede426843fdfe3ce8ca4b60`.

The one authorized public evaluation directly executed all 224 frozen fixture
images on CPU. It passed 223 scenes, retained all 1,792 truths, produced one
false prohibited structural region, zero misses, and zero duplicates, and kept
perfect role accuracy. It emitted no case-level detail. Public report SHA-256
is `fe0a0d63d0c00c35c463e45c11a87f97aa81bb625d0744c8c2307537b6d6d2ff`;
opened and result seal SHA-256 values are
`ad0aa3b96792972f704f550fa94027b34d46d8c15805715b1507e6ce83a55770`
and `22be75da407b45f5e17c3e17293e3a9f3d9e8684d04c77489ea01a039db49a3f`.
P1, P2, and the public evaluation are consumed. Unregistered P3 is retired.
No rerun, case-level tuning, manifest, model-store promotion, packaging,
private Chandler automatic validation, production approval, or release
eligibility is authorized.

## Structural-graph OCR proposal and role V14

V14 is preregistered from the V13 aggregate result only. It introduces a new
topology-spectrum residual encoder over tight and contextual ink, fixed edge
magnitudes, row and column occupancy spectra, and the unchanged 16 production
geometry values. Its 480 train, 144 validation, and 208 truth-hidden public
source scenes use new renderer, degradation, seed, graph, and label families.
An exhaustive source audit found exactly one production proposal for each of
3,840 train, 1,152 validation, and 1,664 public text truths. Train, validation,
and public split fingerprints are
`29fd2e0274aaaddd3a95b228fd46e06ddcdc2570139538b11a9b878b9e3ba263`,
`f1744da5c00ffe6c8f74e3a084c10e93926f05d25ddaebd8f0a9f0eac7593b00`,
and `dbdad5ab4634be014a920350ab39d078609b63f6e827f2fd6df7956f9dd0cadd`.
The frozen training tensor contains 29,518 proposals. The truth-hidden fixture
archive SHA-256 is
`57d8dbf2435f1cff415a1ef7641db59de0556c416532f11f6790164e3db66ebf`.
P1 executed once and is consumed. It completed all 1,616 fixed optimizer steps
and wrote checkpoint SHA-256
`e09c250e567a00b7c79e4995235a9ac3aa5ecb84fdf5f3eda44254efda36ce28`.
The legacy PyTorch ONNX exporter then rejected the fixed-width column occupancy
adaptive average pool from 128 columns to 18 bins because the requested output
size is not a factor of the input size. Candidate report SHA-256 is
`6019b0612cd968248bd1c7379dfbf4f527c0b79787c97e0775c4d35129eb6c45`.
No ONNX or selection metrics were produced. The public archive remains unopened
with zero evaluations. P1 cannot rerun. P2 is now preregistered as a
zero-optimizer repair that loads the exact P1 checkpoint and replaces only the
non-exportable column mean and maximum pools with exact fixed-input bin slices.
Its configuration SHA-256 is
`4b783027b7ec37cb9d5feaa4630316dcb0ed0fa68570d9f042d86983baf95ee1`
and runner source bundle SHA-256 is
`ff8cce58b2c75465583676e11307aee98d0244f4971f3b7bc195cd13808c79ec`.
P2 executed once with zero optimizer steps and is consumed. It retained all
1,152 visible validation truths with zero misses or duplicates, but left two
prohibited false proposals. Overall role accuracy was
`0.9444444444444444`; PhaseHeading at `0.7847222222222222` and YTick at
`0.7777777777777778` failed the fixed per-role gate. The static occupancy
replacement differed from P1 PyTorch output by `7.62939453125e-06`, above the
fixed `1e-06` equivalence limit, and CPU ONNX parity was
`1.52587890625e-05`, above `1e-05`. P2 ONNX SHA-256 is
`2e429dc0c58bf587385bc20a03fa542bbe5e39f41972d6921ff96d6f970b5232`
and report SHA-256 is
`66db4c1819bbb62ec5a1d900285f612f4e57aba692630f10c67cad3a62dfc6c2`.
P1 through P3 are consumed and cannot rerun. P3 completed all 808 optimizer
steps, passed CPU ONNX parity at `9.5367431640625e-06`, eliminated false
proposals and prohibited hits, and retained 1,150/1,152 truths. Two misses left
89/144 scenes exact. Overall role accuracy was `0.9539130434782609`, but
PhaseHeading `0.7638888888888888` failed the fixed `0.85` per-role gate.
Candidate report SHA-256 is
`89b66d006166a8a2efb770c029e0a3f9dc76a5ed0425a2ee16a9f0900da262a4`;
rejected ONNX SHA-256 is
`6fc8ec245262eb99aaa2c6fd315be97353dea569fc336aeb248b2a808f9014d6`.
V14 is exhausted before public evaluation, and the public archive remains
unopened with zero evaluations.

## Layout-conditioned OCR proposal and role V15

V15 is preregistered from V14 P3 aggregate selection evidence only. It does not
inspect or reuse a V14 validation image, truth, case identity, failure case, or
fixture byte. The input contract extends the unchanged proposal crop with eight
plot-relative geometry values derived from verified axis-stage plot bounds and
adds a separate layout-conditioned role branch. Output remains two proposal
logits plus the same eight role logits.

All 512 train, 160 validation, and 224 truth-hidden public renderer,
degradation, seed, label, and layout families are newly frozen. Every one of
7,168 truths has exactly one unchanged production proposal, and the export-safe
model passed direct dynamic CPU ONNX parity. Selection manifest SHA-256 is
`c1ba44c6a67d85f1d52de4f8861ab8703a5ca41f9c9256285e34398cc1bb0fef`;
sealed public archive SHA-256 is
`f2c6783ed0269aa794448b61b09cfca767a82e274247c98bd14d0ab3fcfcf722`.
P1 is consumed after 1,264 optimizer steps. It retained 1,279/1,280 validation
truths with zero false regions and perfect roles at threshold `0.96`; the
threshold `0.88` retained every truth but left three prohibited false regions.
P2 is consumed after 128 optimizer steps. At threshold `0.66` it retained all
1,280 truths across 160/160 exact scenes with zero false regions, misses,
duplicates, or prohibited hits and perfect roles. Its frozen role logits were
byte-exact to P1. CPU ONNX parity was `1.1444091796875e-05`, above the fixed
`1e-05` maximum, so P2 failed closed. Report SHA-256 is
`e87e520a0e634afbc0d1bb1091e4329dd418ba5c87547d57b7cc99305ecd35fc`.
Final P3 reused the exact P2 checkpoint with zero optimizer steps. Its fixed
`0.8` affine output scale and proposal bias anchored at threshold `0.66`
preserved every P2 acceptance decision and role argmax. It passed all 160
validation scenes with all 1,280 truths, zero false regions, misses, duplicates,
or prohibited hits, perfect roles, and CPU ONNX parity
`5.7220458984375e-06`. Report SHA-256 is
`1c89f43258a561e77421244deb5a3f093cb5ce68bd567dc7320ebd86ada0cc12`;
ONNX SHA-256 is
`ff145e14a6f84170e1d727dbd89f57316cb7ee2eaccb6c7f3df5013271d7c948`.
P1 through P3 and the single public evaluation are consumed. The public run
passed 217/224 exact scenes with 1,791/1,792 truths, one false region, one miss,
one prohibited hit, zero duplicates, role accuracy `0.9972082635399219`, and
minimum role accuracy `0.9820627802690582`. No case-level details were emitted.
Public report SHA-256 is
`8bd7170db115f6fccbfc9b998bd5f6fce0d8ae001469b692fa07e8392068553d`.
V15 is exhausted and cannot rerun or be tuned from public cases.

## Selected-confidence OCR P2 eight-role public gate

The candidate first passed its consumed five-role selection, then froze a fresh
160-scene and 1,280-truth public corpus covering YTick, XTick, AxisTitle,
PhaseHeading, LegendText, Participant, Annotation, and Other. The corpus reused
no P1 or P2 fixture bytes, truth, or scene IDs and contained no private or
article image, Chandler, or `Generalization`. Seal commit
`209ca6ffb917eada1657e90a00cb59fc84e685e0` bound archive SHA-256
`b666230e0cce4f1bf539c3210741d8379d2a95e919ed4d2d955f87bd975c876a`,
manifest SHA-256
`faf0e66effbafa7b710a65a31cef81044e2b3d40dbd69b640d18e05f6effa8d9`,
and zero model executions before authorization.

The only authorized C# CPU run is consumed. Direct runtime evidence passed:
detector, official recognizer, and numeric recognizer each recorded 160 input
and output tensor hashes; the ambiguity recognizer recorded 54. The mandatory
public gates failed at 87/160 exact scenes, 1,206 true positives, one false and
prohibited region, 74 misses, zero duplicates, recognition exact `0.85625`, CER
`0.02385599653003687`, overall role accuracy `0.6921875`, and word exact
`0.8781838316722038`. Numeric and ambiguity exact match were `1.0`.
PhaseHeading and Participant role accuracy were `0.0`; the other six role
accuracies were `1.0`. This is aggregate evidence only and does not authorize
case-level inspection or tuning. Report SHA-256 is
`9a8d715f86675c66c9f02a5a44289533751f25f7d007d93743b088a2c039efbf`;
tracked consumed-result SHA-256 is
`11d448e69b5aa972139d518d8e920e3eeeec78ca7d66d2b0a9b0f7fe35c7be78`.
The candidate cannot rerun, enter a manifest or model store, reach the marker or
private Chandler gates, or receive production or release approval.

## V8 C# validation parity repair

The production C# OCR composition was aligned at exact source commit
`58e2576b17df682d5ce55a1eda1d55ca19365e45` without retraining or changing
model bytes. The pipeline no longer infers vertical orientation for the
horizontal graph-label composition, the exact frozen `Followup` phase word is
classified, and ambiguous `O`, `I`, and `l` glyphs no longer become numeric
roles unless graph geometry explicitly expects a number. Cache identity binds
the orientation policy.

One checksum-bound visible-validation run executed all 128 stored synthetic
scenes through the production C# `CPUExecutionProvider`. It passed 128/128
scenes and 640/640 text regions with zero false positives, misses, duplicates,
prohibited-structure hits, or role confusions. Recognition exact match was
`0.9953125`, CER was `0.000873871249635887`, role accuracy was `1.0`, numeric
exact match was `1.0`, word exact match was `0.9917355371900827`, and ambiguity
exact match was `1.0`. The report records every detector, official-recognizer,
numeric-recognizer, and ambiguity-classifier input and output tensor hash. Its
SHA-256 is
`24e02fc68be0c4338b8d5b10061418308a7a4b2f6b765fd713be096da788b3e0`.

This checkpoint is direct visible-validation evidence only. The subsequent
fresh V4 one-run C# OCR-to-marker gate executed and failed closed. It found all
320 OCR truths but admitted 192 false regions, while marker composition retained
465/522 truths with two false markers and 57 misses. The repair target is now a
checksum-bound upstream detector-mask provider plus marker robustness on new
disjoint families, not tuning against V4. Full eight-role coverage, approved
manifests and model-store discovery, packaging, clean-machine execution, and
private validation remain mandatory.

## Morphology-veto OCR proposal V27

V27 is preregistered from aggregate terminal V26 P3 metrics only. It freezes
the exact V26 P3 parent and adds a project-owned residual veto head over 24
explicit tight/context binary projection and morphology values. A fixed `0.5`
output-logit scale preserves parent role argmax while targeting deterministic
float32 CPU parity with ONNX graph optimization disabled. A direct generated-
tensor feasibility test passed the unchanged `1e-5` maximum. Fresh 256-scene
training, 128-scene visible-selection, and 192-scene truth-hidden public
families are registered under new seed offsets and identities. The one-time
freeze completed from exact source commit
`205e28adc42cccd7af323989945d85e713b73e70`; split seal SHA-256 is
`01e4388ab05e3b872943b7a04a22cd12b2a1676481f65711a1e40171c9ab3097`.
The train, visible-selection, and truth-hidden public archives contain 256,
128, and 192 scenes with zero cross-split fixture-byte overlap. Their SHA-256
values are `b48733b08ccf8a19fbd8138bbe312e9c705046b307be70d474a871ede0d7eccd`,
`aed343b56081bb52491d6744fa20aa27ba34695d89936bb9d1d135a84a887b33`,
and `c170c183f106cce665389844522366db378419979aba501dcad0e616813380c4`.
The checksum-bound P1 runner executed once for exactly 1,024 optimizer steps
and consumed its only visible-selection evaluation in 3,770,670.526 ms. Its
checkpoint SHA-256 is
`c2659530e72334887af3e979558a6c95059d46cc7a18302cd41dc36660e85b4c`,
its ONNX SHA-256 is
`3b68909f53abacb22de9130fe856761ff714d00cd0d3e3b88135d5c7147964de`,
and its aggregate report SHA-256 is
`01d4d61658a5c2364946af775a1f615248e7987f3fd08e69fa07ae8dd004818e`.
Strict CPU ONNX parity passed at `4.76837158203125e-06` and all parent role
argmax values were preserved, but visible selection failed. At thresholds
`0.35`, `0.45`, and `0.55`, P1 retained all 1,024 truths but admitted three
false regions, all three prohibited-structure hits, and produced 123 of 128
exact scenes. Thresholds `0.65` and `0.75` retained the three prohibited false
regions and also missed one truth. No passing threshold window exists. The
tracked aggregate-only P1 terminal record SHA-256 is
`97f333f684871f52484b4af6f764589a08c4fd9241299ec82170e0cf4664ccb5`;
it contains no case identity, truth row, prediction, or fixture bytes.

P2 executed once from those aggregate terminal metrics only. It froze the
exact P1 checkpoint and trained only `veto_head.4.weight` and
`veto_head.4.bias` for exactly 512 optimizer steps in 3,803,920.773 ms. The
frozen P1 parameter stream, role argmax values, and strict CPU ONNX parity at
`4.76837158203125e-06` all passed. Selection still failed with the same 123 of
128 exact scenes, 1,024 true positives, three false and prohibited regions,
zero misses through threshold `0.55`, and no passing threshold window. P2
checkpoint, rejected ONNX, aggregate report, and tracked aggregate result
SHA-256 values are
`5cc770cdf431d754d52598364d2c127390f65d85a52517a1ce0e0dd7e2e284f0`,
`bb57e22d61b7a7127eac09a7ddac716bc0456ae38e73d9fbaab283977fd190f5`,
`d1ccc39bcbe92a50773564059d8b9eb8521d1d0b8a13286c7b0f713dded33420`,
and `170be74dd092c030d07437d42e74dbe089892974cedfa38eceb66ccffd5409bb`.
P2 is consumed and cannot rerun.

Final P3 executed once from P2 aggregate terminal metrics and aggregate loss
checkpoints only. It froze exact P2 and added a new four-tensor monotonic veto
head over the existing 24 morphology values plus the two P2 proposal logits.
The head could only raise reject and lower accept logits; every P2 role logit
was returned unchanged. The fixed objective directly targeted the maximum
negative margin, protected the minimum positive margin, capped positive
suppression, and enforced scene separation for four epochs and exactly 1,024
optimizer steps. Candidate configuration SHA-256 is
`eca497e7a20583f4ec6f1c2bf63d3bfdc623309ea197c991adefd4eb46dd5cb1`
and runner-source bundle SHA-256 is
`0464799695667037507a08291e4c1fa91a4318c57c4cb79351ee0d157f5f1655`.
It passed frozen-P2 preservation, zero monotonic violations, zero role-argmax
mismatches, and strict CPU ONNX parity at `4.76837158203125e-06`, but visible
selection failed. Thresholds `0.35` and `0.45` retained all 1,024 truths while
admitting the same three false and prohibited regions. Thresholds `0.55`,
`0.65`, and `0.75` retained those three false regions and missed one truth. No
passing threshold window exists. P3 checkpoint, rejected ONNX, aggregate
report, and tracked aggregate result SHA-256 values are
`33e8dbff01e69bc8a0b2b73219bdbac5ad628988852b79c0e95c1ffb8e0c4d89`,
`f3e2b97d0bc3857e7cb785bf8def2fc7c0660990f586215d37cfa7dad3a38839`,
`e7eda2ccc845ef2dd27bedc9d210d7e7900fb12668358251b310c9742499e862`,
and `8989a7765d2f64b10039ef26359d95534281be0c788cb4dc20a28ac3067e566d`.
P1 through P3 are consumed and V27 cannot rerun or tune. No predecessor case
identity, truth row, prediction, tensor content, fixture byte, selection pixel,
public pixel, private image, Chandler image, or `Generalization` label informed
P3. The public archive remains unopened with zero evaluations. Public
evaluation, marker composition, manifests, model store, packaging, private
validation, approval, and release remain unauthorized. A subsequent attempt
requires a genuinely new defect class and fresh disjoint splits.

## Relational-neighborhood OCR proposal V28

V28 is preregistered from aggregate terminal V27 P3 evidence and aggregate
loss checkpoints only. The three V27 candidates each retained the same three
prohibited false regions while preserving roles and passing strict CPU ONNX
parity, demonstrating that another per-proposal morphology threshold or veto
repair would not address the defect class. V28 instead trains a new proposal
head from scratch over 19 truth-independent ordered-pair geometry values and
two permutation-equivariant scene message blocks. The exact consumed V24 role
anchor is frozen only to preserve its role argmax boundary. The candidate is
float32, CPU-only, uses `ORT_DISABLE_ALL`, and retains the strict `1e-5` ONNX
parity maximum.

Fresh 256-scene training, 128-scene visible-selection, and 192-scene
truth-hidden public split families were frozen once from source commit
`d49a7b469ea787d2c991383608dd93e6565e4439` with new seed offsets, renderer
identities, degradation identities, case identities, and fixture bytes. The
split seal SHA-256 is
`c968aeb5ec0a3440a9fa76b3a346d3652238230599043f801b4fa46ed9eef9bf`.
Train, visible-selection, and public archive SHA-256 values are
`224b5c4025d6ffccbd51b6c0a72aee85cc14e9fa5b2367a3aae4b614523045ba`,
`2968e75bcb09b728785713c87a774bb58325fd9c8371a2bd921689520b697d35`,
and `db00a6bffda5cefe3ecd747d89f930946782f05e7c5a5f013abf06d2a07e0946`.
All cross-split source-byte overlap counts are zero. The checksum-bound P1
configuration SHA-256 is
`26cdda410c2c19efaf5dd1112e8aee91c3cb82210b0c1b7b875720be3a5a7b3c`;
P1 executed once for four epochs and exactly 1,024 optimizer steps. It retained
all 1,024 truth regions at every fixed threshold with zero false regions,
misses, duplicates, or prohibited hits. Recognition exact was `0.97265625`,
CER was `0.004634994206257242`, role accuracy was `0.99609375`, the frozen V24
role argmax was preserved exactly, and strict CPU ONNX parity passed at
`4.76837158203125e-6`. Selection nevertheless failed at 124 of 128 exact
scenes with no passing threshold window because the frozen role parent retained
four aggregate role errors. No case identity or case-level result was emitted
or inspected. The tracked result, ignored candidate report, checkpoint, and
rejected ONNX SHA-256 values are `aa680630...dc0be`, `af7d6ca2...5c618`,
`7c42fff4...930c`, and `788fe3ff...4867`. P1 is consumed. P2 is now
preregistered from those aggregate P1 facts only. It freezes every P1 proposal
and representation parameter and trains only a zero-initialized relational
role residual for four fixed epochs and at most 1,024 optimizer steps. Its
future selection must reproduce the exact P1 PyTorch and ONNX proposal logits,
acceptance decisions at all five thresholds, and full P1 output-stream hash in
addition to the unchanged public gates and `1e-5` parity maximum. The committed
runner bundle SHA-256 is `a1322bf4...dae1`, and the checksum-bound P2
configuration SHA-256 is `0d2ac3c5...77a9`. P2 consumed its single execution
for four epochs and exactly 1,024 optimizer steps. It preserved exact P1
PyTorch and ONNX proposal logits, all five fixed-threshold decisions, the full
P1 output stream, and every frozen P1 parameter; CPU ONNX parity passed at
`4.76837158203125e-6`. All thresholds retained 1,024/1,024 truths with zero
false regions, misses, duplicates, or prohibited hits, but only 123/128 scenes
were exact and aggregate role accuracy regressed to `0.9951171875`. Minimum
per-role accuracy was `0.9765625`, recognition exact was `0.97265625`, and CER
was `0.004634994206257242`. The tracked aggregate result, ignored report,
checkpoint, and rejected ONNX SHA-256 values are `c66beaf6...c3c2`,
`2fce04c6...39359`, `ab3b2fcd...67d56`, and `e3438bb3...bae4f`. No case identity
or case-level result was emitted or inspected. P2 is consumed and cannot
rerun. P3 then froze the exact P1 proposal model, performed zero optimizer
steps, and replaced only role logits with the source-declared plot-relative
geometry partition. It passed 128/128 visible-selection scenes at all five
fixed thresholds with 1,024/1,024 truths, zero false regions, misses,
duplicates, or prohibited hits, perfect accuracy for all eight roles, and CPU
ONNX parity at `3.337860107421875e-6`. The selected ONNX, tracked result,
ignored report, and checkpoint SHA-256 values are `4179534c...b45af`,
`0b56a7a7...bcf2`, `16d60e8d...11fd`, and `3d3f9e14...4e46b`.

The exact P3 payload then consumed its one authorized 192-scene truth-hidden
public execution. It failed all three evaluated thresholds. At the selected
`0.55` threshold only 188/192 scenes were exact, with 1,534/1,536 true
regions, three false regions, two misses, zero duplicates, and three
prohibited hits. Recognition exact was `0.978515625`, CER was
`0.003940553929295204`, role accuracy was `0.9986979166666666`, and minimum
per-role accuracy was `0.9895833333333334`. The exact P1 proposal stream was
preserved. The tracked public result and ignored sealed report SHA-256 values
are `e5ed3ed2...630b` and `2e61340b...6b435`. The report emitted no case
identifiers, fixture bytes, truth text, truth rows, predictions, boxes, or
per-scene pass/fail labels. It retained only ordered per-scene proposal-relation
tensor dimensions, which is explicitly disclosed and was not used for failure
analysis. V28 P1 through P3 and the public gate are consumed. V28 cannot rerun,
tune, inspect public cases, manifest, enter the model store, package, privately
validate, approve, or release.

## Dual-route consensus OCR proposal V29

V29 is preregistered from the tracked aggregate V28 public result only. It
does not use any V28 case identity, truth row, prediction, tensor content,
fixture byte, private image, Chandler image, or `Generalization` label. The
new proposal model uses two independently initialized project-owned routes
trained from scratch: pairwise attention and permutation-invariant relation
summaries. Only their proposal logits are averaged. Roles use the deterministic
source-declared plot-relative partition. P1 freezes a 1,280-step maximum,
fixed dual-route ensemble, per-route, worst-route, and agreement losses,
unchanged five thresholds, strict CPU execution, and `1e-5` ONNX parity.

Fresh split registrations contain 320 training, 160 visible-selection, and 224
truth-hidden public scenes with new seed offsets and disjoint renderer and
degradation identities. Exact source commit `d3f8be4` froze all three archives
with zero source-byte overlap. Split seal SHA-256 is `e0f30b2d...df41bf`;
train, visible-selection, and unopened public archive SHA-256 values are
`0a102f9b...cbef2e`, `a7b7a79e...452fa89`, and `b642bd38...04ae7e9`.
P1 configuration SHA-256 is `ddcae684...e846ee`. Its one authorized execution
completed four epochs and exactly 1,280 optimizer steps. It passed all 160
visible-selection scenes at every frozen threshold: 1,280/1,280 truths, zero
false regions, misses, duplicates, or prohibited hits, recognition exact
`0.97109375`, CER `0.004912373871481678`, and overall and per-role accuracy
`1.0`. Direct stored fixture bytes drove 160 detector and candidate calls and
3,310 recognizer-region calls. CPU ONNX parity passed at
`0.00000286102294921875`. Report, checkpoint, and ONNX SHA-256 values are
`49b7cd6d...ee4b`, `1dd8fc61...336f`, and `a1ce7258...f223`. P1 is selected
and consumed. Its truth-hidden public archive and single evaluation are now
consumed. Marker composition, manifest, model-store entry, packaging discovery,
private validation, production approval, and release remain unauthorized.

The one-use public evaluator completed and failed closed.
Evaluator source bundle SHA-256 is `03523386...fbe`; the authorized
configuration SHA-256 is `d3c16552...a7fc`. It seals before its only archive
read, binds the exact selected payload and split, executes CPU detector,
recognizer, and candidate tensors directly, and emits aggregate evidence only.
The runner source is exact commit `8667c6d5e915d6aac56f03012f852c9dd053fe3b`.
Its single 224-scene direct CPU run passed only 222 scenes at all three fixed
thresholds. It retained 1,792/1,792 truths with zero misses or duplicates, but
added two false regions and two prohibited hits. Recognition exact was
`0.98046875`, CER was `0.003376748673420164`, and every role was `1.0`.
Tracked aggregate result SHA-256 is `db2ceff2...7d0f8`; ignored report SHA-256
is `b764c062...0331`. No case identifiers, truth rows, predictions, or fixture
bytes were emitted or analyzed. V29 and its public archive are consumed and
closed. Marker composition, private validation, approval, and release remain
unauthorized.

## Unanimous structure-veto OCR proposal V30

V30 was designed only from the tracked aggregate V29 public failure. It used no
V29 case identity, truth row, prediction, tensor content, fixture byte,
checkpoint, private image, Chandler image, or `Generalization` label. The
project-owned candidate replaces averaged proposal logits with three
independently initialized experts: relational attention, invariant relation
summary, and local crop structure veto. A proposal is accepted only by the
minimum positive-vs-negative route margin, so any route can veto it.

Fresh 384/192/256 train, visible-selection, and truth-hidden public archives
were frozen from exact source commit
`380d4ece1b48623e60b4d82720d8b421d97349f3`; every cross-split source-byte
overlap count is zero. Split seal SHA-256 is `8f75a388...6b116`. P1 consumed its
one authorized 1,536-step CPU run and visible-selection evaluation. It passed
all 192 scenes at all five thresholds, with 1,536/1,536 truths, zero false
regions, misses, duplicates, or prohibited hits, recognition exact
`0.9713541666666666`, CER `0.004869411243913236`, every role at `1.0`, and
dynamic ONNX parity `3.337860107421875e-06`. The selected threshold is `0.55`.
Tracked aggregate result SHA-256 is `0b5fa9c7...87df5`; the ignored exact
report, checkpoint, and ONNX hashes are `4c7198d3...a522`,
`a91044ee...9ceb`, and `78425c5b...5c18`.

A separate truth-hidden public runner and aggregate-only gate was bound to
public configuration SHA-256 `2fdc26d4...d8136`, runner bundle SHA-256
`97ed352a...49a5`, and exact source commit
`3d4714fc239f8b6832ef223e92da9614efe31abf`. Its single authorized CPU run is
now consumed. It opened the 256-scene archive exactly once and passed 255
scenes with 2,047/2,048 truths, zero false regions, one miss, zero duplicates,
and zero prohibited hits at all three fixed public thresholds. Recognition
exact was `0.98095703125`, CER was `0.0033769523005487546`, role accuracy was
`0.99951171875`, and minimum per-role accuracy was `0.99609375`. The single miss
failed the mandatory all-scenes-exact and zero-miss gates. Aggregate report
SHA-256 is `b04db1e2...3a1d`; opened and result seal SHA-256 values are
`b77f16e8...423d` and `5253429b...c4d8`. No case identifiers, truth rows,
predictions, fixture bytes, or case-level failure analysis were emitted or
used. V30 and its public bytes are closed against rerun, reuse, or tuning.
Marker, private, manifest, model-store, packaging, approval, and release gates
remain closed.

## Robust quorum recall OCR proposal V31

V31 is preregistered only from the tracked aggregate V30 terminal result. It
uses no V30 public case identity, truth row, prediction, tensor content, fixture
byte, private image, Chandler image, or `Generalization` label. The isolated
change reuses the exact project-owned V30 route checkpoint SHA-256
`a91044ee...9ceb` with zero optimizer steps and replaces strict minimum-margin
unanimity with the median positive-vs-negative margin across the attention,
invariant relation-summary, and local-structure routes. Two independent routes
must therefore accept a proposal, while one underconfident route cannot create
a miss by itself.

Fresh 384/192/256 train, visible-selection, and truth-hidden public renderer,
degradation, seed, case, and fixture identities are frozen from exact source
commit `d3a53bfeeefdce69204d26a4bb9962f2cf659a3b`. Protocol SHA-256 is
`1ae5a73f...311c`, split-seal SHA-256 is `f6f07780...6e113`, and the
source-bundle SHA-256 is `fc36598e...00b54`. Train, selection, and public
archive SHA-256 values are `dabc0bf1...b57f`, `b45f35b9...ecf3`, and
`529badd8...d9da`. All 384, 192, and 256 source hashes are unique within their
splits, every scene has exactly one production proposal for each of eight
truths, and all three cross-split source-byte overlap counts are zero.

P1 is consumed. It opened visible selection once and failed before scoring
because raw ONNX Runtime sessions were passed where the frozen evidence
pipeline requires callable tensor adapters. It completed zero optimizer steps,
created no ONNX, emitted no case detail, and caused no case or pixel inspection.
Aggregate result/report SHA-256 values are `b3087ce9...f2eed` and
`1f6adbbf...66a8b`; opened/result seal hashes are `a9f2d137...d3ffc` and
`5ff48127...e442`. The truth-hidden public archive remains unopened.

P2 changed only the runner boundary to a contiguous-float32 callable adapter
around the unchanged detector and recognizer sessions. Exact weights, fixtures,
preprocessing, postprocessing, thresholds, metrics, and gates remained fixed.
P2 runner-source and authorized-config SHA-256 values are
`630336a2...92ae` and `3ccf3b09...ad04`.

P2 is consumed and failed visible selection. Its single direct stored-byte CPU
run completed zero optimizer steps, preserved the exact predecessor checkpoint,
and passed ONNX parity at `4.76837158203125e-06`. Threshold `0.75` passed all
192 scenes with 1,536/1,536 truths, zero false regions, misses, duplicates, or
prohibited hits, recognition exact `0.9713541666666666`, CER
`0.004862953138815208`, and overall and minimum per-role accuracy `1.0`.
Thresholds `0.35`, `0.45`, `0.55`, and `0.65` each retained all truths but
admitted one false prohibited region. Only one threshold passed, so the
mandatory three-consecutive-threshold robustness gate failed. Tracked aggregate
result, ignored report, rejected ONNX, and opened/result seal SHA-256 values are
`34106e7a...c4a3`, `e639be98...b089`, `98ff06ae...976a`,
`6946d16d...7c4b`, and `d3c614be...34a0`. No case detail or pixels were
emitted or inspected. That terminal checkpoint authorized no P3 execution.
Public, marker, private, manifest, model-store, packaging, approval, and
release remain closed.

Final P3 is consumed and V31 is exhausted. Its one authorized invocation ran
from commit `5485a72121a30625fb6b35810392bb56087e597c` after the clean 0.0.22
portable checkpoint. It failed before training authorization was acquired
because the committed preflight accepted historical candidate IDs `[P2, P3]`,
while the canonical acquisition contract requires only the currently
authorized `[P3]`. No output directory or training seal was created, no
optimizer step ran, and the selection archive was not opened. Tracked result
SHA-256 is `2da0952d...c8c55a`. The preflight now mirrors the canonical
single-candidate acquisition invariant, but the one P3 invocation cannot be
rerun. No case detail or pixels were emitted or inspected. Public remains
unopened and all downstream approval gates remain closed.

The latest verified clean development-portable checkpoint was rebuilt from
commit `5485a72121a30625fb6b35810392bb56087e597c`. App tests passed 98/98,
Domain tests passed 23/23, the Goal 19 integration and packaging smoke passed
113 tests with seven expected skips, the full Release solution was green, the
public scoreboard passed its 37 synthetic contract checks, packaging passed
62/62, development-portable packaging passed 9/9, localization passed 9/9,
reviewed OpenCV packaging passed 13/13, and publish completed. `latest.json`,
`portable.mode`, file and product versions, exact commit metadata,
and the executable checksum all matched. No NCNN runtime, model weight, ONNX,
parameter, or checkpoint file entered the payload; only reviewed license notices
mention NCNN and Real-ESRGAN. The external local enhancement and PDFium paths
remain outside the portable payload.

The executable is
`artifacts/dev-portable/builds/0.0.22-20260819T141931943Z-5485a721/GraphReader.App.exe`
with SHA-256
`c5764894a9a353609ce766c45d6599b264cd3982c8adfbbbf4eda70503336c3d`.
The original temporary Chandler attachment was no longer present after the
prior preview closed. The launcher therefore opened the already checksum-bound,
non-synthetic Real-ESRGAN x2 derivative of that exact source in responsive PID
77672 with tab name `chandler-x2.png` and a visible editable graph canvas. UI
Automation invoked Fit, Zoom In, Zoom Out, and Fit again; the
process remains open.
This keeps a real Chandler graph visible but does not replace the separate
original-byte acceptance evidence. Clean-profile
validation passed at
`artifacts/portable-validation/20260819T142216330Z-a0799add/portable-clean-profile-report.json`.
The direct clean audit remained `ReleaseReady=False` with 16
blockers, 13 mandatory evidence gates, only exact OpenCV and PDFium clean-
machine loading passed, and `ArtifactsEmitted=False`. This is a local `0.0.22`
ManualPreview only and does not approve an automatic stage or release artifact.

## Release decision and unblock conditions

Do not create a tag, GitHub release, installer, or portable release claim from
the recorded `0.0.22` validation or the current `0.0.23` version-policy
checkpoint. The first stable public version may be `1.0.0` only after all
`FAIL` rows are replaced with direct evidence and the maintainer explicitly
authorizes the stable promotion. The central version may then jump from the
current `0.y.z` checkpoint directly to `1.0.0`; this does not change ordinary
checkpoint increments. At minimum this requires:

1. A complete checksum-approved, redistributable production model set with
   release-eligible benchmarks and offline CPU execution.
2. Clean-machine loading and workflow evidence that permits the implemented
   common-publish promotion path to approve the exact reviewed OpenCV runtime.
3. Independent PDFium approval, a reviewed exact-binary public-audit component,
   and checksum-bound clean-machine render evidence that permits the implemented
   release-staging path to populate the common publish.
4. A production end-to-end run covering every Session 18 scenario.
5. An authorized private supplied-example run with the false-marker criteria.
6. Installer and portable artifacts built from one clean commit and verified
   for identical binaries, contracts, manifests, checksums, and notices.
7. Clean Windows VM install, portable, offline, path-isolation, repair,
   upgrade, uninstall, spaces, Unicode, and read-only-folder evidence.
